# ironpress Feature Parity Report

Overall: 91.04%  (PASS 1128 · PARTIAL 0 · FAIL 111 · UNKNOWN 0 · total 1239)
Scored coverage: 100.00% (1239 / 1239 fixtures have a reference)
Env: DPI 300 · white-tol 10 · V2 multi-gate verdict · pdftoppm yes
Breadth: 499 distinct category/feature pairs have a fixture (NOT a % of all CSS).
By expected_support: implemented 1091 · partial 37 · unsupported 111
Generated by `cargo test --test feature_parity`.

## Calibration
> The fixed page-origin correction applied to every candidate (spec §1.3). ironpress's 28.8pt margin is spec-correct; Chrome's `--print-to-pdf` rounds it, leaving a rigid offset that is subtracted once and AUDITED — a drift from the deterministic probes aborts the run rather than re-absorbing a real regression.

- offset: **0, 0 device px** (0.00, 0.00 CSS px)
- measured (from probes): 0, 0 device px · residual 0 device px
- drifted: **no**

## Ref lookup mismatches (id != ref-filename)
> A manifest id whose expected `refs/<cat>/<id>.png` is missing WHILE the category dir holds unclaimed ref PNG(s) -> the ref was committed under the wrong name and the fixture is a permanent silent UNKNOWN. Rename the ref to `<id>.png` to fix.

None.

## Suspect: unsupported-but-PASS (re-check tag or feature)
> Fixtures tagged `expected_support == "unsupported"` that nonetheless PASSed. Either the feature IS implemented (fix the tag) or the fixture/ref is not exercising it. Surfaced, not gated.

None.

## Stale references (regenerate)
> A fixture whose HTML SHA-256 differs from `refs.lock` (or is absent from it): the committed reference PNG was generated from an older fixture and is STALE. Run `scripts/parity-gen-refs.sh` to regenerate refs + the lock. (Surfaced here; CI enforces the gate.)

None — every fixture's HTML matches `refs.lock`.

## Regressions / Failures
> Real regressions only (known gaps are in their own section). `attribution` = REAL (the named feature is wrong) vs CONFOUNDED (a substrate it depends on is broken).

No failures or regressions.

## Fix these first
> Substrate probes / base fixtures ranked by how many non-PASS downstream fixtures they confound. Fixing the top of this list should unblock the most dependents.

Nothing confounded — every depended substrate PASSes.

## Coverage by Category
| category | score | pass | partial | fail | unknown |
|----------|------:|-----:|--------:|-----:|--------:|
| backgrounds-borders | 75.38% | 49 | 0 | 16 | 0 |
| backgrounds-gradients | 97.62% | 41 | 0 | 1 | 0 |
| block-box-model | 98.25% | 56 | 0 | 1 | 0 |
| clip-mask | 92.31% | 48 | 0 | 4 | 0 |
| color-opacity | 100.00% | 49 | 0 | 0 | 0 |
| effects | 83.05% | 49 | 0 | 10 | 0 |
| filters | 76.09% | 35 | 0 | 11 | 0 |
| flexbox | 96.00% | 120 | 0 | 5 | 0 |
| fonts-advanced | 80.77% | 21 | 0 | 5 | 0 |
| generated-content | 67.57% | 25 | 0 | 12 | 0 |
| grid | 98.88% | 88 | 0 | 1 | 0 |
| images-replaced | 97.06% | 33 | 0 | 1 | 0 |
| inline-text | 87.76% | 43 | 0 | 6 | 0 |
| interactions | 100.00% | 27 | 0 | 0 | 0 |
| lists-counters | 85.71% | 30 | 0 | 5 | 0 |
| multicol | 100.00% | 35 | 0 | 0 | 0 |
| overflow-clipping | 94.74% | 18 | 0 | 1 | 0 |
| paged-media | 84.15% | 69 | 0 | 13 | 0 |
| positioning | 100.00% | 22 | 0 | 0 | 0 |
| probes | 100.00% | 6 | 0 | 0 | 0 |
| selectors-cascade | 98.21% | 55 | 0 | 1 | 0 |
| tables | 96.39% | 80 | 0 | 3 | 0 |
| text-advanced | 74.47% | 35 | 0 | 12 | 0 |
| transforms | 100.00% | 41 | 0 | 0 | 0 |
| typography | 85.71% | 18 | 0 | 3 | 0 |
| units-values | 100.00% | 35 | 0 | 0 | 0 |

## Known gaps (expected_support != implemented)
> Fixtures targeting features ironpress is NOT expected to fully support. These are tracked for breadth, not counted as regressions.

| expected | status | diff% | category | feature | id | description |
|----------|--------|------:|----------|---------|----|-------------|
| unsupported | FAIL | 32.50 | backgrounds-borders | background-attachment | background-attachment-local-scroll-canvas | background-attachment:local positions a bottom stripe against the scrollable content canvas; catches aliases to scroll/static attachment. |
| unsupported | FAIL | 75.87 | backgrounds-borders | background-blend-mode | background-blend-mode-list-matching | Comma-separated background-blend-mode values keep the top red tile normal while multiplying the bottom blue layer into the yellow color; catches one global blend mode. |
| unsupported | FAIL | 50.17 | backgrounds-borders | background-clip | background-clip-list-matching | Comma-separated background-clip values clip only the top layer to content-box over a blue border-box layer; catches single-clip implementations. |
| unsupported | FAIL | 23.06 | backgrounds-borders | background-clip | background-clip-padding-box-elliptical-radius | background-clip:padding-box follows elliptical rounded corners; catches scalar radius clipping that leaks the background into wrong corner arcs. |
| unsupported | FAIL | 98.30 | backgrounds-borders | background-clip | background-clip-text-gradient | background-clip:text clips a gradient background to glyph shapes; catches engines that map text to border-box or leave transparent text invisible. |
| unsupported | FAIL | 20.29 | backgrounds-borders | background-image | background-image-image-set | background-image:image-set() paints its selected candidate; catches parsers that drop image-set() and leave the box white. |
| unsupported | FAIL | 17.64 | backgrounds-borders | background-layer-order | background-layer-order-raster-gradient | A top red gradient layer covers a bottom blue raster at the same corner; catches fixed slot painters that put raster over gradient. |
| unsupported | FAIL | 1.38 | backgrounds-borders | background-origin | background-origin-list-matching | Comma-separated background-origin values position two image layers from different boxes; catches implementations with one origin for all layers. |
| unsupported | FAIL | 7.92 | backgrounds-borders | background-repeat | background-repeat-space-round | background-repeat: space round distributes columns and rounds row tile height; catches parsers that treat unsupported repeat keywords as ordinary repeat. |
| unsupported | FAIL | 5.21 | backgrounds-borders | background-shorthand | background-shorthand-multiple-layers-color | The background shorthand accepts multiple image layers plus a final background color; catches one-layer parsers that drop a tile or the color. |
| unsupported | FAIL | 1.87 | backgrounds-borders | border-color | border-color-four-value-shorthand | border-color four-value shorthand assigns a distinct color to each side; catches single-color shorthand parsing. |
| unsupported | FAIL | 16.01 | backgrounds-borders | border-image | border-image-gradient-slice | border-image paints a linear-gradient source into a transparent physical border; catches engines that ignore border-image or fall back to a plain/transparent border. |
| unsupported | FAIL | 30.55 | backgrounds-borders | border-style | border-style-3d-bevels | groove, ridge, inset, and outset render with 3D bevel polarity; catches unsupported keywords collapsed to flat solid borders. |
| unsupported | FAIL | 8.21 | backgrounds-borders | border-style | border-style-four-value-shorthand | border-style four-value shorthand assigns solid, dashed, dotted, and double styles to different sides; catches uniform-style parsing. |
| unsupported | FAIL | 20.97 | backgrounds-borders | box-shadow | box-shadow-inset-elliptical-radius | An inset spread shadow follows an elliptical border-radius inner curve; catches rectangular or scalar-radius clipping. |
| unsupported | FAIL | 63.61 | backgrounds-borders | root-body-canvas-background-precedence | html-background-suppresses-body-canvas | An html background paints the canvas and prevents body background propagation; catches engines that fill the whole page with the body color. |
| unsupported | FAIL | 80.96 | backgrounds-gradients | multiple-backgrounds | multiple-gradient-layers | Two gradient background layers use independent size, position, repeat, and paint order; catches one-slot gradient storage or shared geometry. |
| partial | PASS | 0.00 | backgrounds-gradients | radial-gradient | radial-gradient-ellipse-corner | Elliptical radial-gradient positioned at the top-left corner; ironpress only paints centered circles so shape/position are not honored. |
| partial | PASS | 0.00 | backgrounds-gradients | radial-gradient | radial-gradient-sized-px | Radial-gradient with an explicit 60px circle radius over a solid base; ironpress ignores explicit size/extent so the radius will not match Chrome. |
| unsupported | FAIL | 11.90 | block-box-model | display | block-display-list-item | display:list-item on a div generates an inside marker box; catches treating list-item as a plain block with no marker. |
| partial | PASS | 0.39 | block-box-model | display | block-inline-block-baseline | inline-block chips align to the text baseline; vertical-align:top lifts one to the line top (CSS2 10.8). |
| partial | PASS | 0.00 | block-box-model | margin | block-margin-collapse-prevented-flex | Flex items do not collapse margins; the gap is the sum (70px), not the collapsed max (CSS2 8.3.1 + flexbox 6). |
| partial | PASS | 0.00 | block-box-model | width | block-width-fit-content | width:fit-content shrinks to wrap content rather than filling the parent (css-sizing-3 intrinsic sizing). |
| unsupported | FAIL | 4.35 | clip-mask | -webkit-mask-position | r2-webkit-mask-position-offset | -webkit-mask-position alias must behave like mask-position: Only the prefixed mask-position/size/repeat properties are authored, matching common Chromium content. Catches wrong implementation: Implementation aliases -webkit-mask-image but ignores prefixed geometry longhands. |
| unsupported | FAIL | 2.06 | clip-mask | mask-composite: subtract | r2-mask-composite-subtract | mask-composite:subtract must combine multiple mask layers: Two mask layers, a circle and a rectangle, are combined with subtract to create visible circle minus rectangle. Catches wrong implementation: Renderer keeps only the first mask layer or ignores mask-composite. |
| unsupported | FAIL | 5.68 | clip-mask | mask-origin | r2-mask-origin-padding-box | mask-origin must use the padding box as positioning area: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand. |
| unsupported | FAIL | 3.86 | clip-mask | mask-position | r2-mask-position-offset-layer | mask-position must offset mask images: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand. |
| unsupported | FAIL | 50.11 | effects | background-blend-mode comma-separated list | r2-background-blend-mode-list-matching | background-blend-mode lists must match background layers: Two background image layers use different blend modes: top red stripe multiplies, lower blue stripe screens with the background color. Catches wrong implementation: Computed style keeps one blend mode and applies it to every layer or only the first layer. |
| partial | PASS | 0.00 | effects | box-shadow | box-shadow-blur | Offset box-shadow with a soft blur radius on a non-white stage. |
| partial | PASS | 0.00 | effects | box-shadow | box-shadow-inset | Inset hard box-shadow drawing an inner band on two sides of the box. |
| partial | PASS | 0.00 | effects | box-shadow | box-shadow-multiple | Two stacked hard box-shadows in different directions and colors. |
| partial | PASS | 0.00 | effects | box-shadow | box-shadow-spread | Zero-offset zero-blur positive-spread box-shadow forming a hard symmetric halo. |
| unsupported | FAIL | 13.92 | effects | box-shadow inset blur with border-radius | r2-box-shadow-inset-blur-radius | inset box-shadow blur must fade inward inside rounded corners: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details. |
| unsupported | FAIL | 5.23 | effects | box-shadow positive spread with blur and border-radius | r2-box-shadow-spread-blur-radius | box-shadow spread plus blur and radius must grow the rounded shadow shape: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details. |
| unsupported | FAIL | 40.48 | effects | isolation | r2-isolation-isolate-descendant-blend | isolation:isolate must constrain descendant blending: Two identical groups contain a red multiply child over blue page stripes; the isolated group blends only within its own white group backdrop. Catches wrong implementation: Renderer ignores isolation so both red children multiply against the external blue backdrop. |
| unsupported | FAIL | 100.00 | effects | mix-blend-mode | mix-blend-mode-luminosity | mix-blend-mode: luminosity blends a yellow source with a blue backdrop; catches unsupported non-separable blend modes treated as normal. |
| unsupported | FAIL | 25.28 | effects | opacity group isolation with mix-blend-mode descendant | r2-opacity-isolates-descendant-blend | Opacity-created stacking contexts must isolate inner blending: A parent with opacity:.99 contains a multiply child over a blue page; the opacity group is isolated, so child blends with the group backdrop, not the page. Catches wrong implementation: Renderer applies alpha per draw without isolating the group, allowing the child to multiply with page content behind the parent. |
| unsupported | FAIL | 38.59 | effects | text-shadow blur with rgba color | r2-text-shadow-blur-rgba | text-shadow blur with alpha color must preserve soft transparent edges: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order. |
| unsupported | FAIL | 3.19 | effects | text-shadow currentColor default | r2-text-shadow-currentcolor-default | text-shadow omitted color resolves to currentColor: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order. |
| unsupported | FAIL | 2.99 | effects | text-shadow multiple shadow paint order | r2-text-shadow-multiple-order | multiple text-shadows must paint first listed shadow on top: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order. |
| unsupported | FAIL | 13.87 | effects | text-shadow shadows text decorations | r2-text-shadow-decoration-line | text-shadow must shadow text decorations too: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order. |
| unsupported | FAIL | 83.36 | filters | color-interpolation-filters | r2-filter-color-interpolation-filters-srgb | color-interpolation-filters must switch SVG filter color space: Two identical muted boxes use feColorMatrix saturate(0); one filter defaults to linearRGB and one explicitly uses sRGB, producing different grays. Catches wrong implementation: Implementation ignores color-interpolation-filters and produces identical gray boxes. |
| unsupported | FAIL | 64.19 | filters | filter compositing order with mask-image | r2-filter-before-mask-image-order | filter must be applied before mask-image: A blurred red rectangle is masked by a hard half-plane gradient; blur is computed before the mask clips the right half. Catches wrong implementation: Renderer masks first and then blurs, bleeding red into the transparent half. |
| partial | PASS | 0.01 | filters | filter: blur() | filter-blur-box | filter: blur() applied to a box with a solid background-color fill. |
| partial | PASS | 0.00 | filters | filter: blur() | filter-blur-img | filter: blur() applied to an <img> raster; blur of image rasters is the implemented path. |
| partial | PASS | 0.01 | filters | filter: blur() | filter-on-box-blur | filter: blur() on a bordered solid box: should soften both fill and border edge, contrasting with the img-raster blur path. |
| unsupported | FAIL | 35.94 | filters | filter: blur() on a container group | r2-filter-blur-group-descendants | blur() must apply to the whole element subtree: A container with a blue background, a yellow child, and text is blurred as one source graphic. Catches wrong implementation: Only the container background is blurred, leaving child paint and glyphs crisp. |
| unsupported | FAIL | 73.02 | filters | filter: blur() on text | r2-filter-blur-text-glyphs | blur() on text glyphs needs raster/filter coverage: Large text with filter: blur(3px) should render as softened glyph shapes. Catches wrong implementation: Text remains vector-crisp while only surrounding box paint changes or no blur is applied. |
| unsupported | FAIL | 36.19 | filters | filter: blur() visual overflow | r2-filter-blur-visual-overflow-layout | blur() visual overflow must paint outside the border box without moving layout: A blurred red square sits next to a green square; blur feathers extend left/right but the green square keeps its fixed position. Catches wrong implementation: Renderer clips the blur to the red border box or expands layout and shifts the green square. |
| unsupported | FAIL | 1.29 | filters | filter: brightness() on a container group | r2-filter-brightness-group-descendants | brightness() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:brightness(180%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered. |
| unsupported | FAIL | 2.34 | filters | filter: chained | filter-chain-order-contrast-blur | contrast(5) blur(8px) and blur(8px) contrast(5) produce different edge softness; catches implementations that reorder filter functions. |
| unsupported | FAIL | 1.35 | filters | filter: contrast() on a container group | r2-filter-contrast-group-descendants | contrast() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:contrast(220%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered. |
| unsupported | FAIL | 2.55 | filters | filter: drop-shadow() | filter-drop-shadow-currentcolor | drop-shadow() without a color uses currentColor for the shadow; catches parsers that default the omitted color to black. |
| unsupported | FAIL | 1.35 | filters | filter: saturate() on a container group | r2-filter-saturate-group-descendants | saturate() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:saturate(300%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered. |
| unsupported | FAIL | 5.60 | filters | filter: url(#id) with feTurbulence and feDisplacementMap | r2-filter-url-feturbulence-displacement | Procedural SVG filter primitives must affect static output: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive. |
| unsupported | FAIL | 10.84 | flexbox | align-items | flexbox-baseline-empty-synthesis | align-items:baseline synthesizes a baseline for an empty fixed-size item rather than falling back to cross-start; catches baseline code that only handles text baselines. |
| unsupported | FAIL | 8.22 | flexbox | automatic-minimum-size | flexbox-min-width-zero-auto-min | min-width:0 plus overflow:hidden disables the flex item's automatic min-content floor so equal flex items share the row; catches implementations that always reserve min-content width. |
| unsupported | FAIL | 53.32 | flexbox | display | flexbox-display-inline-flex | display:inline-flex creates an inline-level flex container that shares the line with surrounding text; catches parsers that only recognize block-level display:flex. |
| unsupported | FAIL | 48.82 | flexbox | display | flexbox-display-inline-flex-two-keyword | display:inline flex creates an inline-level flex container in a text line; catches parsers that ignore the two-keyword form and lose child sizing. |
| unsupported | FAIL | 0.11 | flexbox | fragmentation | flexbox-fragmentation-wrap-pages | A wrapped row flex container whose lines exceed one page fragments between flex lines; catches paginators that paint the flex layout as one overflowing atomic row. |
| unsupported | FAIL | 21.01 | fonts-advanced | font shorthand | fonts-advanced-font-shorthand | A single font shorthand declaration sets italic bold 28px/36px ParitySerif in one declaration. Catches: Parsing only longhands passes separate font-size/weight/style/family tests but drops the shorthand entirely. |
| partial | PASS | 1.70 | fonts-advanced | font-face | fonts-advanced-font-face-custom-src | An @font-face rule registering a second custom family ('ParityCustom') from the bundled ParitySerif TTF via src: url(), exercising the @font-face declaration and family-matching mechanism. |
| unsupported | FAIL | 11.29 | fonts-advanced | font-size-adjust:0.8 | fonts-advanced-font-size-adjust | Two same-size lines compare default font sizing with font-size-adjust:0.8, which changes used font size to match the requested aspect value. Catches: A font-size-only implementation passes px/em/rem fixtures but ignores x-height adjustment. |
| unsupported | FAIL | 2.69 | fonts-advanced | font-synthesis:none | fonts-advanced-font-synthesis-none | A local @font-face exposes only a regular face; font-weight:bold with font-synthesis:none must remain regular instead of fake-bolding. Catches: A renderer that always synthesizes missing bold/italic passes bold fallback tests but violates explicit synthesis suppression. |
| unsupported | FAIL | 1.81 | fonts-advanced | font-variant-caps | fonts-advanced-small-caps-synthesis | font-variant-caps:small-caps synthesizes smaller uppercase forms for lowercase letters; catches implementations that ignore font variant caps. |
| unsupported | FAIL | 2.85 | fonts-advanced | font-weight:bolder/lighter | fonts-advanced-font-weight-relative | Nested spans use bolder and lighter relative to a 600-weight parent, producing visibly different weights. Catches: A normal/bold-only parser treats bolder/lighter as normal and passes absolute weight fixtures. |
| unsupported | FAIL | 7.66 | generated-content | ::before{display:list-item} | generated-content-before-list-item-marker | A block's ::before pseudo is display:list-item with list-style-type:square, producing a marker before the generated label. Catches: Supporting inline/block generated pseudos passes existing ::before fixtures but drops marker generation for list-item pseudos. |
| unsupported | FAIL | 2.38 | generated-content | ::first-letter numeric first-letter unit | generated-content-first-letter-leading-digit | A paragraph starts with a digit and ::first-letter should style that digit before the rest of the text. Catches: An implementation limited to alphabetic letters passes prose drop-cap fixtures but ignores numeric initial-letter units. |
| unsupported | FAIL | 2.59 | generated-content | ::first-letter punctuation unit | generated-content-first-letter-leading-quote | A paragraph begins with an opening quote; ::first-letter background and color should apply to the quote plus the first letter as one unit. Catches: Styling only the first alphanumeric glyph passes simple first-letter tests but leaves the quote unstyled. |
| unsupported | FAIL | 16.20 | generated-content | ::first-line letter-spacing | generated-content-first-line-letter-spacing | Only the first formatted line gets 5px letter spacing, changing where that line wraps relative to later lines. Catches: Restyling after line breaking passes color-only first-line fixtures but misses geometry-affecting first-line properties. |
| unsupported | FAIL | 8.35 | generated-content | content:leader('.') | generated-content-leader-function | A table-of-contents row inserts a dotted leader between a title and a page number. Catches: A generated-content parser without leader() passes string and counter fixtures but leaves a blank gap before page numbers. |
| unsupported | FAIL | 6.88 | generated-content | first-line | generated-content-first-line-font-size | ::first-line font-size and line-height change the first formatted line geometry; catches implementations that only restyle first-line paint. |
| unsupported | FAIL | 3.87 | generated-content | no-open-quote/no-close-quote depth effects | generated-content-no-open-quote-depth | A hidden opener increments quote depth before a visible nested quote, so the visible quote uses the second-level pair. Catches: Treating no-open-quote as a no-op passes no-glyph tests but fails depth-dependent nested quote rendering. |
| unsupported | FAIL | 28.08 | generated-content | open-quote/close-quote quote depth across nested elements | generated-content-nested-q-quote-depth | Nested q elements use generated open-quote/close-quote with two quote pairs; the inner quote must use the second pair. Catches: Resolving quote depth independently inside each content declaration passes single open/close quote fixtures but never reaches nested quote pairs. |
| unsupported | FAIL | 5.65 | generated-content | position:running() and element() | generated-content-running-element-margin | A small heading is captured with position:running(head) and reused by @page top-left via element(head). Catches: Page margin boxes that only support strings/page counters pass simple margin content tests but cannot render running elements. |
| unsupported | FAIL | 3.06 | generated-content | string-set and string() | generated-content-string-set-running-header | A heading stores its text in a named running string and the page top-center margin prints string(chapter). Catches: @page literal/counter support passes simple page footer tests but cannot collect named strings from document content. |
| unsupported | FAIL | 23.39 | generated-content | target-counter(attr(href), page) | generated-content-target-counter-page | A link to a later section appends that target's page number through target-counter(attr(href), page). Catches: Counter() support in normal flow passes local counter tests but cannot resolve target page cross references. |
| unsupported | FAIL | 6.63 | generated-content | target-text(attr(href)) | generated-content-target-text | A cross-reference link appends the heading text of its target using target-text(attr(href)). Catches: An attr()/counter-only content implementation passes generated string tests but cannot resolve target element text. |
| unsupported | FAIL | 71.05 | grid | display | grid-inline-grid-participates-in-inline-flow | An inline-grid between two inline labels should remain on the same text line and occupy only its grid width; catches treating inline-grid as block or ignoring it still passes block-level display:grid fixtures. |
| partial | PASS | 0.05 | images-replaced | img | svg-as-img | An SVG document referenced as the src of an img via a data: URI, drawn at 160x120. |
| partial | PASS | 0.10 | images-replaced | inline-svg | svg-inline-clip | Inline SVG rect clipped to a circle via a clipPath def, leaving a red disc. |
| partial | PASS | 0.00 | images-replaced | inline-svg | svg-inline-linear-gradient | Inline SVG rect filled with a horizontal linearGradient from blue to red. |
| partial | PASS | 0.00 | images-replaced | inline-svg | svg-inline-text | Inline SVG text element rendered in ParitySans at 36px over a light fill. |
| unsupported | FAIL | 0.74 | images-replaced | object-position | img-object-position-far-edge-length | object-position:right 20px bottom 10px offsets a cover-fitted image from far edges; catches parsing right 20px as plain right alignment. |
| partial | PASS | 1.07 | inline-text | text-decoration-color | inline-text-text-decoration-color | text-decoration-color sets a line colour distinct from the text colour (css-text-decor-3 §2.2). Aspirational: not yet supported. |
| unsupported | FAIL | 14.53 | inline-text | text-decoration-line:underline overline | inline-text-text-decoration-multiple-lines | A span requests underline and overline at the same time. Catches: A single-keyword text-decoration parser passes underline and overline fixtures separately but drops the combined declaration. |
| unsupported | FAIL | 18.31 | inline-text | text-decoration-style:wavy | inline-text-text-decoration-wavy | A large underlined word requests a red wavy underline instead of the default solid rule. Catches: Painting every decoration as a straight solid line passes simple underline tests but misses style geometry. |
| unsupported | FAIL | 9.16 | inline-text | text-decoration-thickness:6px | inline-text-text-decoration-thickness-length | A large underlined word sets a 6px decoration thickness, visibly thicker than the default underline. Catches: A renderer using a fixed font-size-relative stroke passes default underline tests but ignores author thickness. |
| unsupported | FAIL | 16.13 | inline-text | text-underline-offset:10px | inline-text-text-underline-offset-length | An underlined word requests a 10px underline offset, leaving a large gap below the baseline. Catches: A fixed underline-position renderer passes ordinary underline tests but cannot move the line. |
| unsupported | FAIL | 7.76 | inline-text | vertical-align | inline-text-vertical-align-text-bottom | vertical-align:text-bottom aligns to the parent text bottom inside a tall line box; catches aliasing text-bottom to bottom. |
| unsupported | FAIL | 10.56 | inline-text | vertical-align:<percentage> | inline-text-vertical-align-percent | An inline-block square uses vertical-align:50% and shifts upward by half its own line-height. Catches: A keyword/length-only implementation ignores percentages while still passing keyword and length baseline tests. |
| unsupported | FAIL | 15.51 | lists-counters | ::marker font-size | lists-counters-marker-font-size | A list styles only ::marker with a 36px font-size while item text remains 20px. Catches: A marker renderer that only recolors markers passes marker color tests but misses marker font metrics. |
| unsupported | FAIL | 3.05 | lists-counters | @counter-style cyclic symbols | lists-counters-counter-style-cyclic | A custom @counter-style named checks cycles through two literal symbols for list markers. Catches: Ignoring @counter-style passes built-in marker tests but falls back to disc for author-defined styles. |
| unsupported | FAIL | 3.86 | lists-counters | @counter-style extends decimal pad prefix suffix | lists-counters-counter-style-pad-prefix-suffix | A custom counter style extends decimal with pad:2 '0', prefix '[' and suffix '] '. Catches: A decimal-only formatter passes decimal-leading-zero and roman fixtures but ignores custom style descriptors. |
| unsupported | FAIL | 7.07 | lists-counters | @counter-style negative descriptor | lists-counters-counter-style-negative | A generated counter starts at -2 and uses a custom negative descriptor to wrap negative values. Catches: A counter implementation that only formats positive decimal/roman cases passes current fixtures but mishandles negative custom styles. |
| partial | PASS | 1.19 | lists-counters | counters | counter-content-roman | content:counter(x, upper-roman) rendering counter values in an explicit counter style. |
| unsupported | FAIL | 39.13 | lists-counters | marker-side:match-parent | lists-counters-marker-side-match-parent | A vertical list sets marker-side:match-parent so markers use the parent's side rather than each list item's self side. Catches: A list renderer with fixed outside-left markers passes horizontal list fixtures but places markers on the wrong side in vertical writing flows. |
| partial | PASS | 5.28 | multicol | column-count | multicol-column-count-text | Four short ParitySans paragraphs balanced across a two-column container (column-count: 2); tests text fragmentation and column balancing. |
| partial | PASS | 0.00 | multicol | column-count | multicol-column-count-three | Six fixed-height bordered blocks flowed into a three-column container (column-count: 3); tests column fragmentation into equal-width tracks. |
| unsupported | FAIL | 0.74 | overflow-clipping | overflow | overflow-axis-visible-hidden-coercion | overflow-x:visible plus overflow-y:hidden coerces the x axis to non-visible clipping; catches preserving visible overflow on the x axis. |
| partial | PASS | 0.00 | overflow-clipping | overflow | overflow-hidden-grid-item | A grid cell with overflow:hidden clips its oversized inner block to the cell box while the sibling cell is unaffected. |
| unsupported | FAIL | 2.36 | paged-media | footnote | paged-footnote-counter-reset | counter-reset:footnote changes generated footnote call and marker numbers; catches hard-coded sequential footnote markers starting at 1. |
| unsupported | FAIL | 4.58 | paged-media | footnote | paged-footnote-display-compact | footnote-display:compact places fitting short footnotes inline in the footnote area; catches footnote implementations that always stack notes as blocks. |
| unsupported | FAIL | 100.00 | paged-media | footnote | paged-footnote-max-height | @footnote max-height constrains note placement; catches footnote extractors that ignore @footnote sizing and let notes consume arbitrary page height. |
| unsupported | FAIL | 5.64 | paged-media | footnote | paged-footnote-policy-block | footnote-policy:block moves the whole owning paragraph with its footnote when the note cannot fit; catches auto-policy-only footnote placement. |
| unsupported | FAIL | 4.41 | paged-media | footnote | paged-footnote-policy-inline | footnote-policy:line with footnote-display:inline keeps the owning line with its notes and lays the note bodies inline; catches footnote support that only extracts block notes with auto policy. |
| unsupported | FAIL | 6.77 | paged-media | footnote | paged-footnote-styling-pseudos | @footnote styling plus ::footnote-call and ::footnote-marker custom content affect footnote calls and markers; catches basic footnote extractors that keep default markers and no separator styling. |
| partial | PASS | 0.00 | paged-media | page-break-before | paged-page-break-before-avoid-noop | page-break-before:avoid on content that already fits on one page is a no-op; both blocks remain stacked. Tracks the legacy avoid-value gap. |
| unsupported | FAIL | 0.30 | paged-media | page-counter | paged-page-counter-increment-two | @page counter-increment:page 2 changes counter(page) in margin boxes; catches renderers that substitute the physical one-based page index. |
| unsupported | FAIL | 0.51 | paged-media | page-counter | paged-pages-counter-immutable | counter-reset:pages in the page context must not alter counter(pages), which remains the total page count; catches over-eager page counter implementations. |
| unsupported | FAIL | 17.92 | paged-media | page-margin-box | paged-blank-margin-box-specificity | On a blank page inserted by break-after:left, @page :blank top-center content overrides @page :right; catches engines that ignore blank-page margin boxes or page selector specificity. |
| unsupported | FAIL | 0.74 | paged-media | page-margin-box | paged-named-page-margin-box | A block assigned page:chapter must use the chapter @page top-center margin box instead of the default one; catches engines that apply named-page geometry but collect only unqualified margin boxes. |
| unsupported | FAIL | 23.36 | paged-media | running-element | paged-running-named-page-margin-box | position:running() referenced from a named @page chapter margin box appears only on chapter pages; catches named-page geometry support that discards named margin boxes. |
| unsupported | FAIL | 5.39 | paged-media | running-header | paged-gcpm-string-element-last | string(section,last) and element(head,last) in page margin boxes must use the last heading on the page; catches GCPM implementations that ignore named strings or only support element(name)'s default selection. |
| unsupported | FAIL | 3.59 | paged-media | running-header | paged-string-set-attr-start | string-set with attr() feeds string(section,start) in the page header; catches margin header renderers without named strings or attr() support. |
| partial | PASS | 0.00 | selectors-cascade | attribute-selector | selectors-cascade-attribute-equals | [data-pick="yes"] colors only the box whose attribute equals the value; boxes with a different value or no attribute stay gray. |
| partial | PASS | 0.00 | selectors-cascade | pseudo-class | selectors-cascade-first-last-child | :first-child paints the first box green and :last-child paints the last box blue; the middle box stays gray. |
| partial | PASS | 0.00 | selectors-cascade | pseudo-class | selectors-cascade-not-negation | :not(.skip) colors every .box except the one carrying .skip; the skipped box stays gray. |
| partial | PASS | 0.00 | selectors-cascade | pseudo-class | selectors-cascade-root-element | :root paints the page background green behind a centered white bordered panel; if :root is unmatched the page stays white. |
| unsupported | FAIL | 8.27 | selectors-cascade | selectors-4 | selectors-has-nth-of-supports | @supports selector(), child :has(> .flag), and :nth-child(2 of .flagged) make the intended boxes green; catches sibling-only :has and nth-child without the of-selector clause. |
| unsupported | FAIL | 7.84 | tables | border-collapse | tables-collapse-outer-table-border | The table has a thick red collapsed border while the outer cells have thinner blue borders on the same outer grid edges. Catches wrong implementations where an implementation that paints the table border box independently or samples only cell borders shows the wrong outer color/thickness. |
| unsupported | FAIL | 47.35 | tables | display | tables-inline-table-inline-flow | Text before and after an inline-table should remain on the same line while the inline-table internally lays out two cells side by side. Catches wrong implementations where a renderer that treats inline-table as inline text or block flow will either drop the table grid or force the trailing text to a new line. |
| unsupported | FAIL | 3.07 | tables | html-table-attributes | tables-border-attribute | A table uses border=4 without CSS borders, so browser presentational hints draw visible table/cell borders. Catches wrong implementations where a CSS-only renderer ignores the attribute and paints only the cell backgrounds. |
| partial | PASS | 3.47 | tables | table-layout | tables-layout-auto-overflow | table-layout:auto grows past its declared width when nowrap content needs more (aspirational: ironpress currently clamps to the declared width). |
| unsupported | FAIL | 3.71 | text-advanced | dir=auto | text-advanced-dir-auto | Two boxes use dir=auto; the Hebrew-first row aligns right while the Latin-first row aligns left. Catches: Supporting only explicit ltr/rtl dir values passes fixed-direction fixtures but treats auto as the inherited/default direction. |
| unsupported | FAIL | 5.32 | text-advanced | direction:rtl basic inline paragraph | text-advanced-direction-rtl-mixed-bidi | An RTL block containing Hebrew followed by Latin starts from the right edge and orders the mixed text by the Unicode bidi algorithm. Catches: A left-to-right-only renderer passes Latin fixtures and even text-align:right tests but paints mixed RTL text in logical order. |
| unsupported | FAIL | 4.16 | text-advanced | hyphens | text-advanced-hyphens-manual-soft-hyphen | hyphens:manual honors a soft hyphen break and paints a hyphen at the line end; catches wrappers that only break at spaces or overflow points. |
| unsupported | FAIL | 5.89 | text-advanced | mixed-script Unicode bidi visual order | text-advanced-mixed-script-bidi | A left-to-right paragraph contains a Hebrew segment between Latin words and should reorder the RTL segment visually without reversing Latin text. Catches: A run-order-only renderer passes pure LTR and pure RTL tests but leaves mixed scripts in source order. |
| unsupported | FAIL | 13.23 | text-advanced | overflow-wrap:anywhere min-content contribution | text-advanced-overflow-wrap-anywhere-min-content | Two inline-blocks with the same long word compare anywhere against break-word; anywhere can shrink much narrower. Catches: Mapping anywhere and break-word to the same visual breaker passes simple wrapping tests but misses intrinsic sizing differences. |
| unsupported | FAIL | 4.60 | text-advanced | text-align-last:center | text-advanced-text-align-last-center | A justified paragraph has a short final line that should be centered by text-align-last:center. Catches: A justify-only renderer passes multiline justify tests while leaving the last line left-aligned. |
| unsupported | FAIL | 5.40 | text-advanced | text-combine-upright:digits 2 | text-advanced-text-combine-upright-digits | A vertical date combines two-digit numbers horizontally inside a single vertical character slot. Catches: A vertical-writing implementation can pass plain vertical text while rendering each digit as a separate stacked glyph. |
| unsupported | FAIL | 4.21 | text-advanced | text-orientation:upright | text-advanced-text-orientation-upright | A vertical-rl box with text-orientation:upright stacks upright Latin letters instead of rotating the word sideways. Catches: A hard-coded vertical-rl mixed orientation passes default vertical text tests but cannot honor upright orientation. |
| partial | PASS | 0.82 | text-advanced | text-overflow | text-advanced-text-overflow-clip | text-overflow:clip on an overflow:hidden nowrap fixed-width box hard-clips overflowing text with no ellipsis. |
| partial | PASS | 1.02 | text-advanced | text-overflow | text-advanced-text-overflow-ellipsis | text-overflow:ellipsis on an overflow:hidden nowrap fixed-width box renders a trailing ellipsis where text is clipped. |
| unsupported | FAIL | 35.16 | text-advanced | text-wrap-mode:nowrap | text-advanced-text-wrap-mode-nowrap | A long phrase with text-wrap-mode:nowrap overflows a narrow box even though white-space is otherwise normal. Catches: Only recognizing white-space:nowrap passes shorthand tests while ignoring the longhand added by CSS Text 4. |
| partial | PASS | 2.33 | text-advanced | white-space | text-advanced-white-space-break-spaces | white-space:break-spaces preserves spaces and forced segment breaks while still soft-wrapping; preserved trailing spaces occupy width at the line end and cannot hang (css-text-3 §3). |
| unsupported | FAIL | 7.68 | text-advanced | word-break:keep-all with CJK text | text-advanced-word-break-keep-all-cjk | A narrow CJK phrase with keep-all should avoid ordinary CJK line breaks and overflow/clamp as one unbroken run. Catches: Leaving keep-all as normal passes Latin keep-all smoke tests because spaced Latin text wraps the same either way. |
| unsupported | FAIL | 4.33 | text-advanced | writing-mode | text-advanced-writing-mode-vertical-rl-columns | writing-mode:vertical-rl lays Latin inline text sideways from the top of the right column; catches horizontal fallback rendering. |
| unsupported | FAIL | 5.01 | text-advanced | writing-mode:vertical-lr | text-advanced-writing-mode-vertical-lr | A vertical-lr block should set text vertically with columns progressing left-to-right. Catches: A vertical-rl-only implementation passes its one vertical fixture but falls back to horizontal for vertical-lr. |
| partial | PASS | 0.00 | transforms | transform | transforms-translate | transform: translate(x, y) shifts a positioned box right and down from its in-flow slot without affecting surrounding layout. |
| partial | PASS | 0.00 | transforms | transform | transforms-translate-x | transform: translateX() shifts the box horizontally only, leaving its vertical position unchanged. |
| partial | PASS | 0.00 | transforms | transform | transforms-translate-y | transform: translateY() shifts the box vertically only, leaving its horizontal position unchanged. |
| unsupported | FAIL | 16.18 | typography | initial-letter:2 | typography-initial-letter-two-line | A paragraph uses p::first-letter{initial-letter:2} to create a dropped initial spanning two lines without manual float sizing. Catches: A renderer with only custom ::first-letter font-size/float drop caps passes handcrafted drop-cap fixtures but ignores the initial-letter property. |
| unsupported | FAIL | 20.26 | typography | mixed inline font-size line box metrics | typography-mixed-inline-font-size-linebox | A paragraph with a 42px span in the middle must produce a taller first line than the following normal line. Catches: A block-level line-height-only engine passes uniform line-height tests while clipping or overlapping large inline runs. |
| unsupported | FAIL | 39.69 | typography | text-emphasis filled dot | typography-text-emphasis-filled-dot | Japanese text uses filled red emphasis dots above the characters. Catches: An implementation with only text-decoration lines passes underline/overline tests but renders no emphasis marks. |
| partial | PASS | 0.27 | units-values | length-units | units-length-cm | Box sized in centimetres (5cm x 3cm ~= 189x113px) verifying absolute metric cm unit resolution. |
| partial | PASS | 0.00 | units-values | length-units | units-length-in | Box sized in inches (2.5in x 1.25in = 240x120px) verifying absolute inch unit resolution (1in=96px). |
| partial | PASS | 0.34 | units-values | viewport-units | units-viewport-vw-vh | Aspirational: box sized 30vw x 20vh resolves against the printable page (viewport) box; ironpress vw/vh resolution is only partial. |

## Detail
### backgrounds-borders — 75.38%
- **background-attachment** — 50.00%
  - PASS 0.00% background-attachment=fixed-viewport — `background-attachment-fixed-viewport` — background-attachment:fixed anchors a split gradient to the page viewport; catches element-relative fixed positioning.
  - FAIL 32.50% background-attachment=local-scroll-canvas — `background-attachment-local-scroll-canvas` — background-attachment:local positions a bottom stripe against the scrollable content canvas; catches aliases to scroll/static attachment.
- **background-blend-mode** — 50.00%
  - FAIL 75.87% background-blend-mode=layer-list-matching — `background-blend-mode-list-matching` — Comma-separated background-blend-mode values keep the top red tile normal while multiplying the bottom blue layer into the yellow color; catches one global blend mode.
  - PASS 0.64% background-blend-mode=multiply-image-color — `background-blend-mode-multiply-image` — background-blend-mode:multiply blends a blue image layer with a yellow background color; catches engines that paint the image normally.
- **background-clip** — 40.00%
  - PASS 0.08% background-clip=content-box-color — `background-clip-content-box-color` — background-clip:content-box clips a solid background color to the content box; catches border-box clipping that fills padding.
  - FAIL 50.17% background-clip=layer-list-matching — `background-clip-list-matching` — Comma-separated background-clip values clip only the top layer to content-box over a blue border-box layer; catches single-clip implementations.
  - PASS 0.25% background-clip=padding-box-dashed-gradient — `background-clip-padding-box-dashed-gradient` — background-clip:padding-box clips a gradient before dashed border gaps; catches border-box clipping that leaks red through the border.
  - FAIL 23.06% background-clip=padding-box-elliptical-radius — `background-clip-padding-box-elliptical-radius` — background-clip:padding-box follows elliptical rounded corners; catches scalar radius clipping that leaks the background into wrong corner arcs.
  - FAIL 98.30% background-clip=text — `background-clip-text-gradient` — background-clip:text clips a gradient background to glyph shapes; catches engines that map text to border-box or leave transparent text invisible.
- **background-color** — 100.00%
  - PASS 0.00% background-color=rgba-alpha — `background-color-rgba` — Semi-transparent rgba() box composited over a solid background box.
  - PASS 0.00% background-color=solid — `background-color-solid` — Single box filled with a solid opaque background-color.
- **background-image** — 66.67%
  - FAIL 20.29% background-image=image-set — `background-image-image-set` — background-image:image-set() paints its selected candidate; catches parsers that drop image-set() and leave the box white.
  - PASS 0.00% background-image=none-layer-list-matching — `background-image-none-layer-list` — A none background layer still consumes matching size/repeat/position list entries; catches implementations that drop none before list matching.
  - PASS 0.54% background-image=nonuniform-radius-clipping — `background-image-nonuniform-radius-clip` — A raster background is clipped by four unequal border radii; catches unclipped or uniform-radius image clipping.
- **background-layer-order** — 0.00%
  - FAIL 17.64% background-layer-order=gradient-over-raster — `background-layer-order-raster-gradient` — A top red gradient layer covers a bottom blue raster at the same corner; catches fixed slot painters that put raster over gradient.
- **background-origin** — 50.00%
  - PASS 0.00% background-origin=border-box — `background-origin-border-box` — background-origin:border-box positions a tile from the outer border edge; catches implementations that always use padding-box.
  - FAIL 1.38% background-origin=layer-list-matching — `background-origin-list-matching` — Comma-separated background-origin values position two image layers from different boxes; catches implementations with one origin for all layers.
- **background-position** — 100.00%
  - PASS 0.00% background-position=four-value-edge-offsets — `background-position-edge-offsets` — background-position: right 20px bottom 15px anchors a tile from named edges; catches one/two-value parsers that fall back to top-left.
  - PASS 0.13% background-position=x-y-longhands — `background-position-xy-longhands` — background-position-x and background-position-y independently place a tile at fixed offsets; catches engines that ignore the longhands.
- **background-repeat** — 50.00%
  - PASS 0.00% background-repeat=repeat-no-repeat-two-keyword — `background-repeat-repeat-x-no-repeat-y` — background-repeat:repeat no-repeat repeats a tile only horizontally; catches parsers that collapse the two-keyword value to full repeat.
  - FAIL 7.92% background-repeat=space-round — `background-repeat-space-round` — background-repeat: space round distributes columns and rounds row tile height; catches parsers that treat unsupported repeat keywords as ordinary repeat.
- **background-shorthand** — 75.00%
  - FAIL 5.21% background-shorthand=multiple-layers-final-color — `background-shorthand-multiple-layers-color` — The background shorthand accepts multiple image layers plus a final background color; catches one-layer parsers that drop a tile or the color.
  - PASS 0.11% background-shorthand=origin-clip-visual-box-pair — `background-shorthand-origin-clip-pair` — The background shorthand with border-box content-box positions from border-box and clips to content-box; catches reversed or collapsed visual-box parsing.
  - PASS 0.00% background-shorthand=reset-longhands — `background-shorthand-reset-longhands` — A later background shorthand resets earlier image, repeat, size, origin, and clip longhands; catches stale red image or stale content-box clipping.
  - PASS 0.17% background-shorthand=single-visual-box — `background-shorthand-visual-box-single` — The background shorthand with one visual-box sets both origin and clip to content-box; catches shorthands that set only one of them.
- **background-size** — 100.00%
  - PASS 0.00% background-size=auto-length-intrinsic-ratio — `background-size-auto-length` — background-size: auto 60px preserves the image intrinsic ratio; catches implementations that treat mixed auto/length as plain auto.
- **body-background-canvas-propagation** — 100.00%
  - PASS 0.00% body-background-canvas-propagation=transparent-root — `body-background-canvas-propagation` — The body background propagates to the page canvas when the html background is transparent; catches body-only painting that leaves page margins white.
- **border** — 100.00%
  - PASS 0.06% border=per-side-width-color — `border-per-side-colors` — Box with four different per-side border widths and colors.
  - PASS 0.00% border=solid-width — `border-solid-width` — Box with a uniform 8px solid border around a light fill.
  - PASS 0.00% border=width-keywords — `border-width-keywords` — Per-side border-width keywords thin/medium/thick.
- **border-color** — 66.67%
  - PASS 0.00% border-color=currentcolor — `border-color-currentcolor` — border-color currentColor resolves to the element color.
  - FAIL 1.87% border-color=four-value-shorthand — `border-color-four-value-shorthand` — border-color four-value shorthand assigns a distinct color to each side; catches single-color shorthand parsing.
  - PASS 0.24% border-color=transparent-side — `border-color-transparent` — border-color transparent on one side, solid on the rest.
- **border-image** — 0.00%
  - FAIL 16.01% border-image=gradient-slice — `border-image-gradient-slice` — border-image paints a linear-gradient source into a transparent physical border; catches engines that ignore border-image or fall back to a plain/transparent border.
- **border-radius** — 100.00%
  - PASS 0.27% border-radius=50pct-circle — `border-radius-circle` — Square box turned into a filled circle via border-radius 50%.
  - PASS 0.14% border-radius=overlap-clamping — `border-radius-clamped` — border-radius larger than half the box is clamped/normalized to fit.
  - PASS 0.03% border-radius=corner-longhands — `border-radius-corner-longhands` — Per-corner border-radius longhands (top-left + bottom-right rounded, others square).
  - PASS 0.19% border-radius=50pct-nonsquare — `border-radius-ellipse-nonsquare` — border-radius 50% on a non-square box (elliptical corners).
  - PASS 0.06% border-radius=elliptical-slash — `border-radius-elliptical` — Elliptical border-radius via the Rx / Ry slash syntax.
  - PASS 0.05% border-radius=four-value-shorthand — `border-radius-four-values` — border-radius 1-4 value shorthand assigning a distinct radius to each corner.
  - PASS 0.03% border-radius=per-corner — `border-radius-per-corner` — Filled box with two rounded corners and two square corners.
  - PASS 0.06% border-radius=percentage — `border-radius-percentage` — border-radius given as a non-50% percentage of the box.
  - PASS 0.04% border-radius=two-value-shorthand — `border-radius-two-values` — border-radius two-value shorthand (TL/BR vs TR/BL diagonal radii).
  - PASS 0.05% border-radius=uniform — `border-radius-uniform` — Filled box with a uniform 24px border-radius and a solid border.
  - PASS 0.03% border-radius=x-linear-gradient — `border-radius-x-linear-gradient` — Combo: border-radius rounding must clip a linear-gradient background fill (same-category combination).
- **border-style** — 66.67%
  - FAIL 30.55% border-style=groove-ridge-inset-outset — `border-style-3d-bevels` — groove, ridge, inset, and outset render with 3D bevel polarity; catches unsupported keywords collapsed to flat solid borders.
  - PASS 0.05% border-style=dashed — `border-style-dashed` — Box with a 6px dashed border.
  - PASS 0.03% border-style=dotted — `border-style-dotted` — Box with a 6px dotted border.
  - PASS 0.00% border-style=double — `border-style-double` — Box with a 10px double border (two solid rules separated by a gap).
  - FAIL 8.21% border-style=four-value-shorthand — `border-style-four-value-shorthand` — border-style four-value shorthand assigns solid, dashed, dotted, and double styles to different sides; catches uniform-style parsing.
  - PASS 0.00% border-style=none-no-paint — `border-style-none` — border-style none paints no border while reserving the declared width via longhands.
- **border-width** — 100.00%
  - PASS 1.20% border-width=four-value-shorthand — `border-width-four-value-shorthand` — border-width four-value shorthand assigns visibly different widths to every side; catches uniform-width shorthand parsing.
- **box-shadow** — 80.00%
  - PASS 0.00% box-shadow=hard-offset — `border-box-shadow-offset` — Box with a hard-edged (zero-blur) offset box-shadow for deterministic diffing.
  - PASS 0.00% box-shadow=x-border — `border-x-box-shadow` — Combo: a hard offset box-shadow cast by a box that also has a solid border (same-category combination).
  - PASS 0.24% box-shadow=elliptical-percentage-radius — `box-shadow-elliptical-radius` — A pill-shaped border-radius:50% casts a hard oval shadow; catches scalar rounded-rectangle shadow math on non-square boxes.
  - FAIL 20.97% box-shadow=inset-elliptical-radius — `box-shadow-inset-elliptical-radius` — An inset spread shadow follows an elliptical border-radius inner curve; catches rectangular or scalar-radius clipping.
  - PASS 0.35% box-shadow=spread-nonuniform-radius — `box-shadow-spread-nonuniform-radius` — A spread-only outer box-shadow preserves four unequal corner radii; catches scalar-radius shadow inflation.
- **conic-gradient** — 100.00%
  - PASS 0.07% conic-gradient=center — `background-conic-gradient` — Centered multi-stop conic-gradient background.
- **linear-gradient** — 100.00%
  - PASS 0.00% linear-gradient=to-right — `background-linear-gradient` — Horizontal two-stop linear-gradient background.
  - PASS 0.00% linear-gradient=45deg-diagonal — `background-linear-gradient-diagonal` — Diagonal 45deg two-stop linear-gradient background on a square box.
- **outline** — 100.00%
  - PASS 0.00% outline=negative-offset — `outline-offset-negative` — outline-offset negative pulls the outline inward over the box.
  - PASS 0.00% outline=solid-offset — `outline-solid` — Box with a solid outline drawn outside its border via outline-offset.
- **radial-gradient** — 100.00%
  - PASS 0.00% radial-gradient=circle-center — `background-radial-gradient` — Centered circular two-stop radial-gradient background on a square box.
  - PASS 0.00% radial-gradient=ellipse-farthest-corner — `background-radial-gradient-ellipse` — Elliptical radial-gradient sized to the farthest corner.
- **root-body-canvas-background-precedence** — 0.00%
  - FAIL 63.61% root-body-canvas-background-precedence=root-suppresses-body-propagation — `html-background-suppresses-body-canvas` — An html background paints the canvas and prevents body background propagation; catches engines that fill the whole page with the body color.

### backgrounds-gradients — 97.62%
- **background-clip** — 100.00%
  - PASS 0.28% background-clip=padding-box — `background-clip-padding-box` — background-clip: padding-box should stop the background-color under a translucent border; background-clip is not implemented, so this is a known-gap fixture.
- **background-origin** — 100.00%
  - PASS 0.17% background-origin=content-box — `background-origin-content-box` — background-origin: content-box anchors the raster inside the padding box of a bordered, padded element.
  - PASS 0.00% background-origin=content-box-gradient-tile — `background-origin-content-box-gradient` — background-origin:content-box anchors a fixed-size gradient tile at the content edge; catches implementations that apply origin only to raster images.
- **background-position** — 100.00%
  - PASS 0.04% background-position=bottom-right-keyword — `background-position-keyword` — A no-repeat raster placed at the 'bottom right' keyword position inside a sized box.
- **background-size** — 100.00%
  - PASS 0.00% background-size=contain — `background-size-contain` — background-size: contain scales the raster to fit within the box, left-aligned, leaving base color visible.
  - PASS 0.00% background-size=cover — `background-size-cover` — background-size: cover scales the raster to fill the box, cropping overflow, centered.
  - PASS 0.00% background-size=explicit-length — `background-size-length` — background-size with an explicit 120px x 80px length, no-repeat, top-left.
- **conic-gradient** — 100.00%
  - PASS 0.14% conic-gradient=at-position — `conic-gradient-at-position` — Four-quadrant conic-gradient with its center anchored at 30%/30%.
  - PASS 0.31% conic-gradient=four-quadrant-sweep — `conic-gradient-basic` — Four-quadrant conic-gradient sweep, rendered via a sector fan clipped to the box.
  - PASS 0.24% conic-gradient=color-hint — `conic-gradient-color-hint` — A standalone conic-gradient color hint shifts the angular interpolation midpoint late in the turn; catches hint rejection or ignored hints.
  - PASS 0.50% conic-gradient=edge-offset-position — `conic-gradient-edge-offset-position` — conic-gradient() accepts a four-value edge-offset at-position from right and bottom; catches center fallback or parse failure.
  - PASS 0.51% conic-gradient=from-angle — `conic-gradient-from-angle` — Four-quadrant conic-gradient rotated by a 45deg 'from' start angle.
  - PASS 0.32% conic-gradient=hard-stops-pie — `conic-gradient-hard-stops` — Three-wedge hard-stop conic-gradient pie chart (deterministic 120deg sectors).
  - PASS 0.58% conic-gradient=turn-radian-stop-units — `conic-gradient-turn-rad-stops` — conic-gradient color-stop positions accept turn and rad angle units in the same list; catches degree-only parsers.
- **linear-gradient** — 100.00%
  - PASS 0.00% linear-gradient=alpha-stops — `linear-gradient-alpha-stops` — Transparent-to-opaque red gradient composites over a blue parent; catches gradient painters that drop alpha from color stops.
  - PASS 0.00% linear-gradient=angle-90deg — `linear-gradient-angle-90deg` — Two-stop linear-gradient using an explicit 90deg angle (paints left-to-right).
  - PASS 0.00% linear-gradient=calc-color-stop-positions — `linear-gradient-calc-stops` — linear-gradient color stops accept calc() length-percentage positions around the midpoint; catches literal-percentage-only parsers.
  - PASS 0.00% linear-gradient=interpolation-hint — `linear-gradient-color-hint` — A standalone 20% color interpolation hint moves the red/blue midpoint left; catches gradient parsers that ignore hints or drop the gradient.
  - PASS 0.23% linear-gradient=hard-color-stops — `linear-gradient-hard-stops` — Hard color-stop linear-gradient (two stops at 50%) producing a deterministic sharp 50/50 split.
  - PASS 0.00% linear-gradient=three-stops-percent — `linear-gradient-multi-stop` — Three-stop linear-gradient with explicit 0%/50%/100% color-stop positions, to right.
  - PASS 0.00% linear-gradient=oklab-interpolation-method — `linear-gradient-oklab-interpolation` — linear-gradient() with `in oklab` interpolates in Oklab rather than rejecting the gradient or using default sRGB interpolation.
  - PASS 0.00% linear-gradient=to-bottom-right — `linear-gradient-to-corner` — Two-stop linear-gradient using the 'to bottom right' corner keyword on a square box.
  - PASS 0.00% linear-gradient=to-bottom-side — `linear-gradient-to-side` — Two-stop linear-gradient using the 'to bottom' side keyword on a tall box.
- **multiple-backgrounds** — 50.00%
  - PASS 0.05% multiple-backgrounds=image-over-gradient — `multiple-backgrounds-layered` — Two comma-separated background layers (a raster over a linear-gradient) with per-layer position/size; multiple backgrounds are not implemented, so this is an aspirational known-gap fixture.
  - FAIL 80.96% multiple-backgrounds=two-gradient-layers — `multiple-gradient-layers` — Two gradient background layers use independent size, position, repeat, and paint order; catches one-slot gradient storage or shared geometry.
- **radial-gradient** — 100.00%
  - PASS 0.00% radial-gradient=circle-at-25-25 — `radial-gradient-at-position` — Circular radial-gradient anchored at the 25%/25% position (off-center).
  - PASS 0.00% radial-gradient=circle-at-center — `radial-gradient-circle-center` — Centered circular two-stop radial-gradient (the shape ironpress natively supports).
  - PASS 0.71% radial-gradient=closest-corner-offcenter — `radial-gradient-closest-corner-offcenter` — An off-center radial-gradient(circle closest-corner) sizes the hard transition by nearest-corner distance; catches aliases to side or far-corner sizing.
  - PASS 0.00% radial-gradient=circle-closest-side — `radial-gradient-closest-side` — Circular radial-gradient sized to the closest side of a non-square box.
  - PASS 0.00% radial-gradient=color-hint — `radial-gradient-color-hint` — A standalone radial-gradient color hint shifts the red/blue interpolation midpoint toward the outer edge; catches hint rejection or ignored hints.
  - PASS 0.42% radial-gradient=edge-offset-position — `radial-gradient-edge-offset-position` — radial-gradient() accepts a four-value edge-offset at-position from right and bottom; catches center fallback or parse failure.
  - PASS 0.00% radial-gradient=ellipse-at-top-left — `radial-gradient-ellipse-corner` — Elliptical radial-gradient positioned at the top-left corner; ironpress only paints centered circles so shape/position are not honored.
  - PASS 0.00% radial-gradient=ellipse-explicit-radii — `radial-gradient-ellipse-explicit` — Elliptical radial-gradient with explicit 120px x 60px radii centered in the box.
  - PASS 0.84% radial-gradient=farthest-corner-offcenter — `radial-gradient-farthest-corner-offcenter` — An off-center radial-gradient(circle farthest-corner) sizes the hard transition by farthest-corner distance; catches farthest-side aliases.
  - PASS 0.00% radial-gradient=circle-farthest-side — `radial-gradient-farthest-side` — Circular radial-gradient sized to the farthest side of a non-square box.
  - PASS 0.00% radial-gradient=explicit-pixel-size — `radial-gradient-sized-px` — Radial-gradient with an explicit 60px circle radius over a solid base; ironpress ignores explicit size/extent so the radius will not match Chrome.
  - PASS 1.18% radial-gradient=two-position-percent-stops — `radial-gradient-two-position-percent-stops` — radial-gradient two-position percentage stops create crisp yellow, red, and blue rings; catches smooth interpolation or dropped range stops.
- **repeating-conic-gradient** — 100.00%
  - PASS 1.05% repeating-conic-gradient=wedges — `repeating-conic-gradient` — Repeating-conic-gradient producing alternating 30deg wedges around the center.
- **repeating-linear-gradient** — 100.00%
  - PASS 0.80% repeating-linear-gradient=stripes — `repeating-linear-gradient` — Repeating-linear-gradient producing evenly spaced vertical stripes (10% period).
  - PASS 2.13% repeating-linear-gradient=length-stops — `repeating-linear-gradient-px-stops` — repeating-linear-gradient with 12px hard color-stop ranges creates fixed-width stripes; catches percentage-only stop handling.
- **repeating-radial-gradient** — 100.00%
  - PASS 1.28% repeating-radial-gradient=rings — `repeating-radial-gradient` — Repeating-radial-gradient producing concentric rings (10% period circle).
  - PASS 3.15% repeating-radial-gradient=length-range-stops — `repeating-radial-gradient-px-ranges` — repeating-radial-gradient with 10px two-position stop ranges creates crisp rings; catches percentage-only or non-repeating fallback.

### block-box-model — 98.25%
- **border-width** — 100.00%
  - PASS 0.00% border-width=per-side — `block-border-width-thick` — Box with asymmetric per-side solid border widths (6/14/22/30px) verifying each edge thickness independently.
- **box-sizing** — 100.00%
  - PASS 0.00% box-sizing=border-box — `block-box-sizing-border-box` — box-sizing:border-box keeps the total rendered width at 200px despite 24px padding and 8px border.
  - PASS 0.00% box-sizing=border-box-auto-width — `block-box-sizing-border-box-auto-width` — box-sizing:border-box with width:auto fills the CB; padding+border drawn inside the 300px box (css-sizing-3).
  - PASS 0.00% box-sizing=content-box — `block-box-sizing-content-box` — box-sizing:content-box expands rendered width to 264px (200 + padding + border) for the same declared width.
  - PASS 0.00% box-sizing=padding-exceeds-width — `block-box-sizing-padding-exceeds` — border-box where padding exceeds declared width: content floors to 0, border box grows to padding sum (css-sizing-3 5.1).
- **display** — 87.50%
  - PASS 0.00% display=block — `block-display-block-stacking` — Inline spans forced to display:block stack vertically full-width, each on its own line.
  - PASS 0.00% display=contents-box-suppression — `block-display-contents` — display:contents suppresses the wrapper box, paint, and padding while keeping children in flow; catches treating contents as block.
  - PASS 0.00% display=inline-block — `block-display-inline-block-row` — Three display:inline-block chips laid out in a row (whitespace collapsed via font-size:0) verifying inline flow of block boxes.
  - PASS 0.20% display=inline-grid-multi-keyword — `block-display-inline-grid` — display:inline grid creates inline-level grid containers whose children lay out in columns; catches parsers that support only block-level grid or old single-keyword syntax.
  - FAIL 11.90% display=list-item-marker — `block-display-list-item` — display:list-item on a div generates an inside marker box; catches treating list-item as a plain block with no marker.
  - PASS 0.00% display=flow-root-float-containment — `block-flow-root-float-containment` — display:flow-root establishes a BFC whose height contains a floated child before the following block; catches normal-block zero-height float collapse.
  - PASS 0.39% display=inline-block-baseline — `block-inline-block-baseline` — inline-block chips align to the text baseline; vertical-align:top lifts one to the line top (CSS2 10.8).
  - PASS 0.00% display=inline-block-wrap — `block-inline-block-wrap` — inline-block chips wrap to the next line when the row overflows the container width (css-display-3).
- **fragmentation** — 100.00%
  - PASS 0.00% fragmentation=exact-fill-no-extra-page — `block-exact-fill-no-extra-page` — Content that fills the page content box EXACTLY (two 100px blocks = the full 200px page) must NOT spill a spurious empty second page. The multi-page harness asserts the page COUNT (1); an off-by-one pagination emitting a trailing blank page would fail.
  - PASS 0.00% fragmentation=box-decoration-break-clone — `box-decoration-break-clone` — box-decoration-break:clone on a tall bordered, padded box that spans a page break. Unlike slice, EACH page fragment is independently wrapped with the FULL border + padding + background, so both pages show a box closed on all four sides. ironpress implements the css-break-3 §6.2 clone model; the multi-page harness asserts the page COUNT (2) and diffs every page.
  - PASS 0.14% fragmentation=box-decoration-break-clone-radius — `box-decoration-break-clone-radius` — box-decoration-break:clone with border-radius across a page break: each fragment is independently wrapped with the full border/padding/background, so BOTH fragments are fully closed boxes with all four rounded corners. Contrasts the slice-radius case. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% fragmentation=box-decoration-break-slice — `box-decoration-break-slice` — Explicit box-decoration-break:slice (the default) on a tall bordered, padded box that spans a page break. Page 1 carries the TOP border and is OPEN at the page bottom; page 2 carries the BOTTOM border and is OPEN at the top. Pins the slice decoration model independently of the default-driven container-splits case.
  - PASS 0.18% fragmentation=box-decoration-break-slice-radius — `box-decoration-break-slice-radius` — box-decoration-break:slice with border-radius across a page break. Per CSS Fragmentation 3 §5.4 the cut edge of each fragment must be SQUARE: only the box's real corners are rounded (top corners on page 1, bottom corners on page 2). ironpress now drops the radius on each fragment's cut edge and strokes the present border sides along the rounded (non-cut) corners as an open path, so page 1 rounds only its top corners and page 2 only its bottom — matching Chrome. The clone-radius companion (all corners rounded per fragment) still passes. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% fragmentation=container-split-across-pages — `container-splits-across-pages` — A bordered, padded, background-filled container with nine child blocks is far taller than the 240px page and FRAGMENTS across three pages (CSS Fragmentation 3 §3, class-A break between children). Under the default box-decoration-break:slice the box is sliced at each page edge: page 1 keeps the TOP border (open at the bottom), the middle page has NO top/bottom border (only the left/right edges), and the last page keeps the BOTTOM border (open at the top). The multi-page harness asserts the page COUNT (3) and diffs every page, so any clipping or mis-placed border fails — the first fixture that validates container fragmentation (splitting a box between its children), not just a single text block.
  - PASS 0.00% fragmentation=nested-container-recursive-split — `nested-container-deep-split` — A bordered container nested inside another bordered container (the outer box has a SINGLE child, so there is no between-children break point at the outer level) holds ten 50px rows and is far taller than the 240px page. The fragmentation engine must RECURSE into the inner box and split it between its rows: rows 0-3 on page 1, 4-7 on page 2, 8-9 on page 3, with both boxes sliced (box-decoration-break:slice) at each page edge. Catches the data-loss bug where a single-child container was placed whole and clipped to one page. The multi-page harness asserts the page COUNT (3) and diffs every page.
  - PASS 3.38% fragmentation=paragraph-split-across-pages — `paragraph-splits-across-pages` — A single paragraph far taller than the 200px page is SPLIT across page boundaries (CSS Fragmentation 3 §3): 24 lines at 16px/1.5 (18pt each, 8 per 150pt page) span THREE pages. The multi-page harness asserts the page COUNT (3) and diffs every page against Chrome, so any clipping or off-by-one split fails — the first fixture that validates layout-level fragmentation (line-box splitting), not just a forced break. Text-bearing on every page, so it carries the text/AA threshold class (6/18%) shared by the typography/inline-text corpus: each page matches Chrome exactly once the global ~0.32px text-baseline AA offset (see probe-text-baseline) is set aside.
- **height** — 100.00%
  - PASS 0.00% height=auto-from-children — `block-height-auto-from-children` — height:auto grows to contain in-flow children (60+90=150px) (CSS2 10.6.3).
  - PASS 0.00% height=percent-auto-parent — `block-height-percent-auto-parent` — Percentage height with an auto-height parent behaves as auto (falls back to content height) (CSS2 10.5).
  - PASS 0.00% height=percent-definite-parent — `block-height-percent-definite-parent` — Percentage height resolves to 50% of the parent's definite 200px content height = 100px (CSS2 10.5).
- **margin** — 100.00%
  - PASS 0.00% margin=auto-centering — `block-margin-auto-centering` — Fixed-width block with margin-left/right:auto horizontally centered inside a wider track.
  - PASS 0.00% margin=auto-no-width — `block-margin-auto-no-width` — margin:0 auto with width:auto does not center: auto margins compute to 0 and width fills the CB (CSS2 10.3.3).
  - PASS 0.00% margin=auto-single-side — `block-margin-auto-single-side` — margin-left:auto on a fixed-width block absorbs all free space, pushing the box to the right edge (CSS2 10.3.3).
  - PASS 0.00% margin=collapse-adjacent — `block-margin-collapse-adjacent` — Two stacked blocks whose adjacent vertical margins (40px and 24px) collapse to the larger 40px gap.
  - PASS 0.00% margin=collapse-all-negative — `block-margin-collapse-all-negative` — All-negative adjacent margins collapse to the most-negative (-50px), overlapping the boxes (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-empty — `block-margin-collapse-empty` — Empty block's own top+bottom margins collapse together and with siblings to a single 40px gap (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-negative-mixed — `block-margin-collapse-negative-mixed` — Mixed-sign adjacent margins collapse to max_positive + min_negative = 30px (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-parent-first-child — `block-margin-collapse-parent-first-child` — Parent + first in-flow child top margins collapse through (CSS2 8.3.1); child margin-top protrudes above the parent.
  - PASS 0.00% margin=collapse-parent-last-child — `block-margin-collapse-parent-last-child` — Parent (auto height) + last in-flow child bottom margins collapse through (CSS2 8.3.1); following sibling sits at the larger margin.
  - PASS 0.00% margin=collapse-prevented-bfc-sibling — `block-margin-collapse-prevented-bfc-sibling` — A BFC wrapper does not collapse its child's margin with margins outside it (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-prevented-flex — `block-margin-collapse-prevented-flex` — Flex items do not collapse margins; the gap is the sum (70px), not the collapsed max (CSS2 8.3.1 + flexbox 6).
  - PASS 0.00% margin=collapse-prevented-height — `block-margin-collapse-prevented-height` — Parent explicit height prevents parent/last-child bottom margin collapse-through (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-prevented-bfc — `block-margin-collapse-prevented-overflow` — overflow:hidden establishes a BFC, preventing parent/child margin collapse (CSS2 8.3.1).
  - PASS 0.00% margin=collapse-prevented-padding — `block-margin-collapse-prevented-padding` — Parent padding-top prevents parent/first-child margin collapse; child margin offsets it inside the parent (CSS2 8.3.1).
  - PASS 0.00% margin=descendant-protrudes — `block-margin-descendant-protrudes` — Descendant top margin protrudes above the parent when not blocked, vs contained when padding blocks the collapse (CSS2 8.3.1).
  - PASS 0.00% margin=negative-offset — `block-margin-negative-offset` — Negative explicit margin-left/-top pull the box left/up, overlapping the parent edge (CSS2 8.3).
  - PASS 0.00% margin=explicit-offsets — `block-margin-offsets` — Block with asymmetric explicit margin-top:40px and margin-left:90px offsetting it within a track.
- **max-height** — 100.00%
  - PASS 0.00% max-height=clamp — `block-max-height-clamp` — max-height caps the box to 120px and clips overflowing content via overflow:hidden (css-sizing-3).
- **max-width** — 100.00%
  - PASS 0.00% max-width=clamp — `block-max-width-clamp` — Auto-width block clamped down to max-width:200px instead of filling the wider track.
  - PASS 0.15% max-width=min-content — `block-max-width-min-content` — max-width:min-content constrains a 200px text box to the longest word and wraps the phrase; catches ignoring intrinsic max-width keywords.
  - PASS 0.00% max-width=percent — `block-max-width-percent` — Percentage max-width clamps auto width to 50% of the 400px CB content width = 200px (css-sizing-3).
- **min-height** — 100.00%
  - PASS 0.00% min-height=floor — `block-min-height-floor` — min-height raises a short box to the 160px floor; background extends below the child (css-sizing-3).
- **min-width** — 100.00%
  - PASS 0.00% min-width=min-over-max — `block-min-max-width-conflict` — min-width > max-width: min wins via max(min,min(value,max)) -> 240px (css-sizing-3 5.2).
  - PASS 0.00% min-width=floor — `block-min-width-floor` — Declared width:80px raised to the min-width:220px floor.
  - PASS 0.15% min-width=max-content — `block-min-width-max-content` — min-width:max-content expands a narrow box to its unbreakable text width; catches ignoring intrinsic min-width keywords.
- **nested-block-flow** — 100.00%
  - PASS 0.00% nested-block-flow=containment — `block-nested-containment` — Three nested blocks, each inset by its parent's padding, forming concentric frames to verify nested block containment.
- **padding** — 100.00%
  - PASS 0.00% padding=uniform — `block-padding-all-sides` — content-box container with 30px padding on all sides framing an inner box; verifies padding insets content equally.
  - PASS 0.00% padding=asymmetric — `block-padding-asymmetric` — Per-side asymmetric padding (10/50/30/70) insets content distinctly on each edge (css-box-3).
  - PASS 0.00% padding=percent — `block-padding-percent` — Percentage padding resolves against containing-block WIDTH on every side incl. vertical (CSS2 8.4).
- **width** — 100.00%
  - PASS 0.00% width=auto — `block-width-auto-fills-parent` — Block child with width:auto expands to fill the parent's content box (parent width minus padding).
  - PASS 0.00% width=fit-content — `block-width-fit-content` — width:fit-content shrinks to wrap content rather than filling the parent (css-sizing-3 intrinsic sizing).
  - PASS 0.00% width=percent — `block-width-percent` — Child with width:50% resolves to half (200px) of the 400px parent content box.
- **width-height** — 100.00%
  - PASS 0.00% width-height=explicit-px — `block-width-height-explicit` — Single block with explicit width:240px height:120px and a 4px border; baseline box dimensions.

### clip-mask — 92.31%
- **-webkit-mask-image** — 100.00%
  - PASS 0.00% -webkit-mask-image=prefixed-alias — `mask-webkit-alias` — -webkit-mask-image (prefixed alias) fades a solid fill top-to-bottom; the alias must behave as mask-image. CSS mask is unsupported.
- **-webkit-mask-position** — 0.00%
  - FAIL 4.35% -webkit-mask-position=webkit-mask-position-offset — `r2-webkit-mask-position-offset` — -webkit-mask-position alias must behave like mask-position: Only the prefixed mask-position/size/repeat properties are authored, matching common Chromium content. Catches wrong implementation: Implementation aliases -webkit-mask-image but ignores prefixed geometry longhands.
- **clip** — 100.00%
  - PASS 0.00% clip=deprecated-clip-rect — `r2-clip-property-rect-absolute` — Deprecated clip:rect() still clips absolutely positioned elements: An absolutely positioned blue rectangle has clip:rect(15px, 120px, 75px, 35px). Catches wrong implementation: Renderer ignores the deprecated but required clip property.
- **clip-path has no effect on layout** — 100.00%
  - PASS 0.00% clip-path has no effect on layout=no-layout-shrink — `r2-clip-path-no-layout-shrink` — clip-path must not change layout geometry: A clipped first box keeps its original block size, so the following green bar starts below the unclipped 100px layout height. Catches wrong implementation: Layout engine shrinks the box to the clip bounds and moves the following bar upward.
- **clip-path: circle()** — 100.00%
  - PASS 0.20% clip-path: circle()=centered — `clip-path-circle` — clip-path: circle() clips a solid square to a centered circular disc. CSS clip-path on boxes is unsupported.
  - PASS 0.22% clip-path: circle()=circle-default-closest-side — `r2-clip-path-circle-default-closest-side` — circle() default radius must resolve closest-side: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: circle() with left/top position keywords** — 100.00%
  - PASS 0.20% clip-path: circle() with left/top position keywords=circle-position-keywords — `r2-clip-path-circle-position-keywords` — circle() position keywords must resolve: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: circle(farthest-side at ...)** — 100.00%
  - PASS 0.07% clip-path: circle(farthest-side at ...)=circle-farthest-side — `r2-clip-path-circle-farthest-side` — circle() keyword radii must parse: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: content-box** — 100.00%
  - PASS 0.00% clip-path: content-box=content-box-geometry — `r2-clip-path-content-box-geometry` — standalone clip-path geometry boxes must clip to that box: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: ellipse()** — 100.00%
  - PASS 0.24% clip-path: ellipse()=distinct-radii — `clip-path-ellipse` — clip-path: ellipse() clips a solid rectangle to a centered ellipse with distinct x/y radii. CSS clip-path on boxes is unsupported.
  - PASS 0.22% clip-path: ellipse()=ellipse-default-radii — `r2-clip-path-ellipse-default-radii` — ellipse() default radii must resolve: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: ellipse() with right/bottom position keywords** — 100.00%
  - PASS 0.22% clip-path: ellipse() with right/bottom position keywords=ellipse-position-keywords — `r2-clip-path-ellipse-position-keywords` — ellipse() position keywords must resolve: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: ellipse(closest-side farthest-side at ...)** — 100.00%
  - PASS 0.25% clip-path: ellipse(closest-side farthest-side at ...)=ellipse-extent-keywords — `r2-clip-path-ellipse-extent-keywords` — ellipse() extent keywords must parse: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: inset()** — 100.00%
  - PASS 0.00% clip-path: inset()=rectangular — `clip-path-inset` — clip-path: inset() clips a solid box down to an inner rectangle. CSS clip-path on boxes is unsupported (only inside SVG defs).
  - PASS 0.00% clip-path: inset()=content-box-geometry — `clip-path-inset-content-box` — clip-path: inset(0) content-box clips a bordered, padded box to its content-box reference; catches geometry-box keywords ignored as bare border-box inset().
  - PASS 0.07% clip-path: inset()=rounded — `clip-path-inset-round` — clip-path: inset() with a round radius clips a solid box to a rounded-corner rectangle. CSS clip-path on boxes is unsupported.
- **clip-path: inset() round elliptical radii** — 100.00%
  - PASS 0.02% clip-path: inset() round elliptical radii=inset-round-elliptical-radii — `r2-clip-path-inset-round-elliptical-radii` — clip-path inset round must support elliptical and per-corner radii: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: inset(<percentage>{4})** — 100.00%
  - PASS 0.00% clip-path: inset(<percentage>{4})=inset-percent-four-values — `r2-clip-path-inset-percent-four-values` — clip-path inset percentages with four-value shorthand need coverage: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: path()** — 100.00%
  - PASS 0.00% clip-path: path()=path-triangle — `r2-clip-path-path-triangle` — clip-path path() basic shape must render: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: polygon()** — 100.00%
  - PASS 0.00% clip-path: polygon()=diamond — `clip-path-polygon` — clip-path: polygon() clips a solid square into a diamond via four percentage vertices. CSS clip-path on boxes is unsupported.
  - PASS 0.00% clip-path: polygon()=evenodd-fill-rule — `clip-path-polygon-evenodd` — clip-path: polygon(evenodd, ...) cuts an inner rectangular hole; catches polygon clipping that ignores the fill-rule or always uses nonzero fill.
- **clip-path: rect()** — 100.00%
  - PASS 0.02% clip-path: rect()=rect-rounded — `r2-clip-path-rect-rounded` — clip-path rect() basic shape must render: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-path: url(#clip)** — 100.00%
  - PASS 0.22% clip-path: url(#clip)=url-inline-svg-circle — `r2-clip-path-url-inline-svg-circle` — clip-path url(#clipPath) must reference inline SVG clip sources: A CSS box references an inline SVG clipPath containing a circle. Catches wrong implementation: Parser handles only CSS basic shapes and ignores url(#clip).
- **clip-path: xywh()** — 100.00%
  - PASS 0.02% clip-path: xywh()=xywh-rounded-rect — `r2-clip-path-xywh-rounded-rect` — clip-path xywh() basic shape must render: A single colored box isolates the clip geometry without text or external assets. Catches wrong implementation: Parser or renderer only handles the simplest existing basic-shape subset.
- **clip-rule with clip-path** — 100.00%
  - PASS 0.00% clip-rule with clip-path=url-clip-rule-evenodd — `r2-clip-path-url-clip-rule-evenodd` — clip-rule evenodd must cut holes in referenced SVG clipPath: An inline clipPath contains two nested rectangles with clip-rule:evenodd, producing a frame-shaped visible region. Catches wrong implementation: Renderer ignores clip-rule or cannot render URL clip paths, filling the inner hole.
- **cumulative clipping with overflow and clip-path** — 100.00%
  - PASS 0.09% cumulative clipping with overflow and clip-path=overflow-clip-intersection — `r2-overflow-plus-clip-path-intersection` — Ancestor overflow and child clip-path must intersect: A clipped child overflows an overflow:hidden parent; visible paint is the intersection of parent rectangle and child circle. Catches wrong implementation: Renderer applies only one clip or fails to nest clipping paths.
- **mask shorthand full grammar** — 100.00%
  - PASS 0.01% mask shorthand full grammar=shorthand-position-size-repeat-alpha — `r2-mask-shorthand-position-size-repeat-alpha` — mask shorthand must expand position/size/repeat/mode/composite: The mask shorthand sets a linear mask at 20px 10px / 80px 80px no-repeat alpha. Catches wrong implementation: Implementation treats mask as only mask-image and drops geometry/mode components.
- **mask-border-source/slice/width/repeat** — 100.00%
  - PASS 0.17% mask-border-source/slice/width/repeat=mask-border-gradient-ring — `r2-mask-border-gradient-ring` — mask-border must slice and apply a border-box mask: A mask-border gradient should reveal only a border ring around a solid box. Catches wrong implementation: Renderer has no mask-border fields and paints the entire box.
- **mask-clip** — 100.00%
  - PASS 0.33% mask-clip=mask-clip-content-box — `r2-mask-clip-content-box` — mask-clip must limit the mask painting area: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-composite** — 100.00%
  - PASS 0.74% mask-composite=exclude-two-layers — `mask-composite-exclude-layers` — Two mask-image layers with mask-composite:exclude punch a circular hole through a full-box mask; catches single-layer masks or ignored compositing.
- **mask-composite: add** — 100.00%
  - PASS 4.38% mask-composite: add=composite-add — `r2-mask-composite-add` — mask-composite:add must combine multiple mask layers: Two mask layers, a circle and a rectangle, are combined with add to create union of two masks. Catches wrong implementation: Renderer keeps only the first mask layer or ignores mask-composite.
- **mask-composite: intersect** — 100.00%
  - PASS 3.01% mask-composite: intersect=composite-intersect — `r2-mask-composite-intersect` — mask-composite:intersect must combine multiple mask layers: Two mask layers, a circle and a rectangle, are combined with intersect to create only circle-rectangle overlap. Catches wrong implementation: Renderer keeps only the first mask layer or ignores mask-composite.
- **mask-composite: subtract** — 0.00%
  - FAIL 2.06% mask-composite: subtract=composite-subtract — `r2-mask-composite-subtract` — mask-composite:subtract must combine multiple mask layers: Two mask layers, a circle and a rectangle, are combined with subtract to create visible circle minus rectangle. Catches wrong implementation: Renderer keeps only the first mask layer or ignores mask-composite.
- **mask-image: conic-gradient()** — 100.00%
  - PASS 0.00% mask-image: conic-gradient()=alpha-sweep — `mask-image-conic-gradient` — mask-image: conic-gradient() masks a solid fill to an opaque half-turn wedge fading to transparent. CSS mask is unsupported.
- **mask-image: linear-gradient()** — 100.00%
  - PASS 0.02% mask-image: linear-gradient()=diagonal-angle — `mask-image-linear-diagonal` — mask-image: linear-gradient(135deg) fades a solid fill diagonally from opaque (top-left) to transparent (bottom-right) via the alpha channel. CSS mask is unsupported (PDF SMask used internally for blur alpha only).
  - PASS 0.00% mask-image: linear-gradient()=alpha-fade — `mask-image-linear-gradient` — mask-image: linear-gradient() fades a solid fill from opaque to transparent left-to-right. CSS mask is unsupported (PDF SMask used internally for blur alpha only).
- **mask-image: none** — 100.00%
  - PASS 0.00% mask-image: none=initial-value — `mask-image-none` — mask-image: none leaves a solid box fully opaque (initial value). CSS mask is unsupported.
- **mask-image: radial-gradient()** — 100.00%
  - PASS 0.00% mask-image: radial-gradient()=alpha-disc — `mask-image-radial-gradient` — mask-image: radial-gradient() leaves a solid disc fading to transparent at the edges. CSS mask is unsupported.
- **mask-image: repeating-conic-gradient()** — 100.00%
  - PASS 0.51% mask-image: repeating-conic-gradient()=repeating-conic-spokes — `r2-mask-image-repeating-conic-spokes` — repeating-conic-gradient masks must repeat angular bands: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-image: repeating-linear-gradient()** — 100.00%
  - PASS 0.00% mask-image: repeating-linear-gradient()=striped-alpha — `mask-image-repeating-linear` — mask-image: repeating-linear-gradient() tiles opaque/transparent bands across a solid fill (repeating CSS gradient mask source). CSS mask is unsupported.
- **mask-image: repeating-radial-gradient()** — 100.00%
  - PASS 0.62% mask-image: repeating-radial-gradient()=repeating-radial-rings — `r2-mask-image-repeating-radial-rings` — repeating-radial-gradient masks need direct coverage: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-image: url(#mask) default mask-type luminance** — 100.00%
  - PASS 0.35% mask-image: url(#mask) default mask-type luminance=inline-svg-mask-luminance — `r2-mask-inline-svg-default-luminance` — Inline SVG <mask> default luminance must hide black regions: A CSS box references an inline SVG <mask> with a black left half and white right half; default luminance hides black and reveals white. Catches wrong implementation: Implementation treats the mask as alpha-only or ignores inline fragment masks.
- **mask-image: url(#mask) with mask-type: alpha** — 100.00%
  - PASS 0.53% mask-image: url(#mask) with mask-type: alpha=inline-svg-mask-alpha — `r2-mask-inline-svg-alpha-type` — Inline SVG <mask> references must honor mask-type:alpha: A CSS box references an inline SVG <mask> whose black regions have alpha 1 and mask-type:alpha, while a transparent middle stripe masks out content. Catches wrong implementation: Implementation only loads SVG image URLs, treats black as zero luminance, or ignores transparent mask alpha.
- **mask-image: url()** — 100.00%
  - PASS 0.48% mask-image: url()=svg-luminance — `mask-image-url-svg` — mask-image: url() references a data:-URI SVG whose white circle defines the visible region. CSS mask is unsupported.
- **mask-image: url(data:image/svg+xml,...) match-source luminance** — 100.00%
  - PASS 0.18% mask-image: url(data:image/svg+xml,...) match-source luminance=svg-image-match-source-luminance — `r2-mask-svg-image-match-source-luminance` — SVG image masks in match-source mode use luminance: An SVG mask image has a red half and a white half; match-source should use luminance so red is only partially visible and white is fully visible. Catches wrong implementation: Implementation treats SVG image masks as pure alpha, making both halves equally visible.
- **mask-mode: alpha with SVG image mask** — 100.00%
  - PASS 0.18% mask-mode: alpha with SVG image mask=svg-image-alpha-mode — `r2-mask-svg-image-alpha-mode` — mask-mode:alpha on SVG images must ignore luminance: The same red/white SVG mask is used with mask-mode:alpha; both opaque halves should be fully visible. Catches wrong implementation: Renderer always uses luminance for SVG masks, making the red half dim.
- **mask-mode: luminance** — 100.00%
  - PASS 0.00% mask-mode: luminance=gradient-source — `mask-mode-luminance` — mask-mode: luminance uses a white->black gradient's luminance as coverage so a solid fill fades from visible (white) to masked (black). CSS mask is unsupported.
- **mask-mode: match-source with CSS gradient** — 100.00%
  - PASS 0.15% mask-mode: match-source with CSS gradient=match-source-gradient-alpha — `r2-mask-mode-match-source-gradient-alpha` — mask-mode:match-source on CSS gradients uses alpha, not luminance: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-origin** — 0.00%
  - FAIL 5.68% mask-origin=mask-origin-padding-box — `r2-mask-origin-padding-box` — mask-origin must use the padding box as positioning area: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-position** — 0.00%
  - FAIL 3.86% mask-position=mask-position-offset — `r2-mask-position-offset-layer` — mask-position must offset mask images: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-repeat** — 100.00%
  - PASS 1.02% mask-repeat=mask-repeat-stripes — `r2-mask-repeat-tiled-stripes` — mask-repeat must tile the mask layer: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.
- **mask-size** — 100.00%
  - PASS 0.00% mask-size=mask-size-layer — `r2-mask-size-independent-layer` — mask-size must size the mask layer independently: A single solid box uses a CSS mask longhand or gradient source to isolate mask geometry/coverage behavior. Catches wrong implementation: Renderer supports only a single element-sized alpha/luminance mask source and ignores the longhand.

### color-opacity — 100.00%
- **CSS named color** — 100.00%
  - PASS 0.00% CSS named color=named-papayawhip — `r2-color-named-papayawhip` — All CSS named colors must be available, not just the small legacy subset: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **background-color** — 100.00%
  - PASS 0.00% background-color=background-currentcolor — `r2-background-color-currentcolor` — currentColor must resolve in background-color: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **color(display-p3 ...)** — 100.00%
  - PASS 0.00% color(display-p3 ...)=color-display-p3 — `r2-color-function-display-p3` — display-p3 colors must gamut-map to output sRGB: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **color(srgb ...)** — 100.00%
  - PASS 0.00% color(srgb ...)=color-srgb — `r2-color-function-srgb` — color(srgb ...) must parse as a predefined RGB color: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **color(srgb-linear ...)** — 100.00%
  - PASS 0.00% color(srgb-linear ...)=color-srgb-linear — `r2-color-function-srgb-linear` — color(srgb-linear ...) must convert linear-light components: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **color(xyz-d65 ...)** — 100.00%
  - PASS 0.00% color(xyz-d65 ...)=color-xyz-d65 — `r2-color-function-xyz-d65` — XYZ predefined colors must convert to output sRGB: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **color-format** — 100.00%
  - PASS 0.00% color-format=alpha-percentage — `color-alpha-percentage` — Percentage alpha rgb(... / 50%) composited over a solid box; css-color-4 §15.
  - PASS 0.00% color-format=hex-3-digit — `color-hex-rgb` — Box filled with a 3-digit shorthand hex color (#0a6).
  - PASS 0.00% color-format=hex-4-digit — `color-hex-rgba` — Semi-transparent 4-digit hex (#f508) composited over a solid box; css-color-4 §5.2.
  - PASS 0.00% color-format=hex-6-digit — `color-hex-rrggbb` — Box filled with a 6-digit hex color (#c2185b).
  - PASS 0.00% color-format=hex-8-digit — `color-hex-rrggbbaa` — Semi-transparent 8-digit hex (#ff572280) composited over a solid box; css-color-4 §5.2.
  - PASS 0.00% color-format=hsl-function — `color-hsl` — Box filled with an hsl() functional color (hsl(280 60% 45%)); css-color-4 §7.
  - PASS 0.00% color-format=hsl-hue-angle — `color-hsl-hue-angle` — hsl() hue with angle unit (0.5turn = 180deg); css-color-4 §7.
  - PASS 0.00% color-format=hsl-modern-slash — `color-hsl-modern-slash` — Modern hsl(h s l / a) slash-alpha composited over a solid box; css-color-4 §7.
  - PASS 0.00% color-format=hsl-powerless-hue — `color-hsl-powerless-hue` — hsl() powerless hue at 0% saturation -> gray; css-color-4 §7.
  - PASS 0.00% color-format=hsla-alpha — `color-hsla-alpha` — Semi-transparent hsla() box composited over a solid background; css-color-4 §7.
  - PASS 0.00% color-format=hwb-function — `color-hwb` — hwb(194 0% 0%) fully-saturated functional color; css-color-4 §8.
  - PASS 0.00% color-format=hwb-gray — `color-hwb-gray` — hwb(120 50% 50%) where whiteness+blackness=1 -> gray; css-color-4 §8.
  - PASS 0.00% color-format=named-keyword — `color-named` — Box filled with a named CSS color keyword (navy).
  - PASS 0.00% color-format=named-case-insensitive — `color-named-case-insensitive` — Named color is ASCII case-insensitive (NavY -> navy); css-color-4 §6.1.
  - PASS 0.00% color-format=oklch — `color-oklch` — CSS Color 4 oklch() background color converts to output sRGB; catches legacy-only parsers that drop the declaration to the fallback color.
  - PASS 0.00% color-format=rebeccapurple — `color-rebeccapurple` — rebeccapurple named keyword (#663399); css-color-4 §6.1.
  - PASS 0.00% color-format=rgb-clamp — `color-rgb-clamp` — Out-of-range rgb(300 -20 999) clamps to [0,255]; css-color-4 §11.
  - PASS 0.00% color-format=rgb-function — `color-rgb-function` — Box filled with an rgb() functional color.
  - PASS 0.00% color-format=rgb-modern-slash — `color-rgb-modern-slash` — Modern space syntax rgb(r g b / a) composited over a solid box; css-color-4 §11.
  - PASS 0.00% color-format=rgb-none — `color-rgb-none` — Modern rgb() with the none keyword (resolves to 0); css-color-4 §4.3.
  - PASS 0.00% color-format=rgb-percentage — `color-rgb-percentage` — Box filled with rgb() percentage components rgb(80% 20% 10%); css-color-4 §11.
  - PASS 0.00% color-format=rgba-alpha — `color-rgba-alpha` — Semi-transparent rgba() box composited (source-over) over a solid background box; css-color-4 §12.
  - PASS 0.00% color-format=text-glyph-fill — `color-text-glyph` — The color property fills text glyphs in ParitySans with a solid color.
  - PASS 0.00% color-format=opacity-clamp — `opacity-clamp` — opacity:1.5 clamps to 1.0 (fully opaque); computed opacity is in [0,1].
  - PASS 0.00% color-format=opacity-nested — `opacity-nested-group` — opacity on a group flattens then composites the whole at 0.5 (not per child).
  - PASS 0.00% color-format=opacity-zero — `opacity-zero` — opacity:0 fully transparent but layout space reserved; the next box flows below.
  - PASS 0.00% color-format=visibility-visible-after-hidden — `visibility-visible-after-hidden` — visibility:visible child paints inside a visibility:hidden parent; CSS2 §11.2.
- **color-keyword** — 100.00%
  - PASS 0.00% color-keyword=currentcolor — `color-currentcolor` — currentColor resolves the color property into a thick solid border; css-color-4 §6.2.
  - PASS 0.00% color-keyword=transparent — `color-transparent-keyword` — transparent (= rgb(0 0 0 / 0)) background lets the under box show through a bordered overlay; css-color-4 §6.1.
- **color-mix()** — 100.00%
  - PASS 0.00% color-mix()=color-mix — `r2-color-mix-red-blue` — color-mix() static colors must resolve before painting: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **display** — 100.00%
  - PASS 0.00% display=none-removal — `display-none-removal` — display:none removes the first block entirely so the second block rises to the top of the page.
- **hsl() with none component** — 100.00%
  - PASS 0.00% hsl() with none component=hsl-none — `r2-color-hsl-none-component` — hsl() missing components beyond rgb() must parse: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **hwb() with alpha** — 100.00%
  - PASS 0.00% hwb() with alpha=hwb-alpha — `r2-color-hwb-alpha-over-blue` — hwb() slash alpha needs coverage: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **lab()** — 100.00%
  - PASS 0.00% lab()=lab — `r2-color-lab` — lab() colors must convert to sRGB: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **lch()** — 100.00%
  - PASS 0.00% lch()=lch — `r2-color-lch` — lch() colors must convert to sRGB: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **oklab()** — 100.00%
  - PASS 0.00% oklab()=oklab — `r2-color-oklab` — oklab() colors must convert to sRGB: A fixed-size color swatch isolates parsing/conversion/compositing of the color syntax. Catches wrong implementation: Parser accepts only the existing subset or drops the declaration, producing default/transparent paint.
- **opacity** — 100.00%
  - PASS 0.00% opacity=half — `opacity-half` — Box with opacity 0.5 composited over a solid background box.
  - PASS 0.00% opacity=percentage-value — `opacity-percentage` — opacity:50% composites a red box at half alpha over a blue stage; catches opacity parsers that accept only numeric values.
  - PASS 1.32% opacity=text-group — `opacity-text-glyph-group` — Overlapping glyphs in one opacity:.5 element flatten before compositing; catches per-glyph alpha drawing where overlaps become too bright.
- **opacity group flattening with alpha child overlap** — 100.00%
  - PASS 0.00% opacity group flattening with alpha child overlap=opacity-group-flatten-alpha-overlap — `r2-opacity-group-flatten-alpha-overlap` — opacity on a group with alpha children must flatten before compositing: A parent opacity:.5 contains a semitransparent red child over an opaque blue child; the group is flattened before being composited onto white. Catches wrong implementation: Renderer multiplies each child alpha independently against the page, causing a different overlap color.
- **opacity stacking context with z-index descendants** — 100.00%
  - PASS 0.00% opacity stacking context with z-index descendants=opacity-stacking-context — `r2-opacity-stacking-context-z-index` — opacity-created stacking contexts must contain positioned descendants: An opacity:.99 parent has a high z-index child and is followed by a sibling; the child cannot escape the parent's stacking context to cover the sibling. Catches wrong implementation: Renderer applies opacity per draw and sorts the descendant globally by z-index.
- **visibility** — 100.00%
  - PASS 0.00% visibility=collapse-table-row — `visibility-collapse-row` — visibility:collapse on a table row removes the row and collapses its height.
  - PASS 0.00% visibility=hidden-reserves-space — `visibility-hidden` — visibility:hidden hides the first block but reserves its space, so the second block stays offset below.

### effects — 83.05%
- **background-blend-mode** — 100.00%
  - PASS 0.06% background-blend-mode=multiply — `background-blend-mode-multiply` — A linear-gradient background blended against a solid background-color via background-blend-mode multiply (aspirational; not implemented).
  - PASS 0.61% background-blend-mode=overlay — `background-blend-mode-overlay` — background-blend-mode: overlay blends black/white gradient halves with a purple background color; catches normal or multiply-only background blending.
  - PASS 0.16% background-blend-mode=color — `r2-background-blend-mode-color` — Non-separable background-blend-mode:color is required: A red/blue gradient background blends with a gray/yellow base using color. Catches wrong implementation: Parser falls back to normal because non-separable blend keywords are not recognized.
  - PASS 0.16% background-blend-mode=color-burn — `r2-background-blend-mode-color-burn` — background-blend-mode:color-burn needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:color-burn over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.16% background-blend-mode=color-dodge — `r2-background-blend-mode-color-dodge` — background-blend-mode:color-dodge needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:color-dodge over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.32% background-blend-mode=darken — `r2-background-blend-mode-darken` — background-blend-mode:darken needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:darken over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.32% background-blend-mode=difference — `r2-background-blend-mode-difference` — background-blend-mode:difference needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:difference over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.32% background-blend-mode=exclusion — `r2-background-blend-mode-exclusion` — background-blend-mode:exclusion needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:exclusion over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.16% background-blend-mode=hard-light — `r2-background-blend-mode-hard-light` — background-blend-mode:hard-light needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:hard-light over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.16% background-blend-mode=hue — `r2-background-blend-mode-hue` — Non-separable background-blend-mode:hue is required: A red/blue gradient background blends with a gray/yellow base using hue. Catches wrong implementation: Parser falls back to normal because non-separable blend keywords are not recognized.
  - PASS 0.32% background-blend-mode=lighten — `r2-background-blend-mode-lighten` — background-blend-mode:lighten needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:lighten over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.00% background-blend-mode=luminosity — `r2-background-blend-mode-luminosity` — Non-separable background-blend-mode:luminosity is required: A red/blue gradient background blends with a gray/yellow base using luminosity. Catches wrong implementation: Parser falls back to normal because non-separable blend keywords are not recognized.
  - PASS 0.00% background-blend-mode=saturation — `r2-background-blend-mode-saturation` — Non-separable background-blend-mode:saturation is required: A red/blue gradient background blends with a gray/yellow base using saturation. Catches wrong implementation: Parser falls back to normal because non-separable blend keywords are not recognized.
  - PASS 0.32% background-blend-mode=screen — `r2-background-blend-mode-screen` — background-blend-mode:screen needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:screen over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
  - PASS 0.16% background-blend-mode=soft-light — `r2-background-blend-mode-soft-light` — background-blend-mode:soft-light needs direct coverage: A two-layer solid linear-gradient background uses background-blend-mode:soft-light over a yellow background color. Catches wrong implementation: Renderer supports only multiply or treats all other background blend values as normal.
- **background-blend-mode comma-separated list** — 0.00%
  - FAIL 50.11% background-blend-mode comma-separated list=list-matching — `r2-background-blend-mode-list-matching` — background-blend-mode lists must match background layers: Two background image layers use different blend modes: top red stripe multiplies, lower blue stripe screens with the background color. Catches wrong implementation: Computed style keeps one blend mode and applies it to every layer or only the first layer.
- **box-shadow** — 100.00%
  - PASS 0.00% box-shadow=blur — `box-shadow-blur` — Offset box-shadow with a soft blur radius on a non-white stage.
  - PASS 0.00% box-shadow=border-radius — `box-shadow-border-radius` — Box-shadow on a border-radius element must adopt the same rounded corners.
  - PASS 0.09% box-shadow=colored — `box-shadow-color` — Hard offset box-shadow in a saturated color distinct from the box fill.
  - PASS 0.00% box-shadow=currentcolor — `box-shadow-currentcolor` — Box-shadow with the color omitted, so it paints in the element's currentColor.
  - PASS 0.00% box-shadow=inset — `box-shadow-inset` — Inset hard box-shadow drawing an inner band on two sides of the box.
  - PASS 0.07% box-shadow=inset-spread — `box-shadow-inset-spread` — Inset box-shadow with spread: a solid inner ring clipped to the padding box.
  - PASS 0.00% box-shadow=multiple — `box-shadow-multiple` — Two stacked hard box-shadows in different directions and colors.
  - PASS 0.00% box-shadow=negative-offset — `box-shadow-negative-offset` — Box-shadow with negative offsets cast to the upper-left of the element.
  - PASS 0.00% box-shadow=negative-spread — `box-shadow-negative-spread` — Hard-edged offset box-shadow with negative spread (-8px) shrinking the shadow smaller than the box.
  - PASS 0.00% box-shadow=hard-offset — `box-shadow-offset` — Box with a hard-edged (zero-blur) offset box-shadow cast onto a non-white stage.
  - PASS 0.00% box-shadow=spread — `box-shadow-spread` — Zero-offset zero-blur positive-spread box-shadow forming a hard symmetric halo.
  - PASS 0.00% box-shadow=spread-outline — `box-shadow-spread-outline` — Zero-offset zero-blur box-shadow with positive spread: a solid even ring of equal width on all four sides.
- **box-shadow inset blur with border-radius** — 0.00%
  - FAIL 13.92% box-shadow inset blur with border-radius=inset-blur-radius — `r2-box-shadow-inset-blur-radius` — inset box-shadow blur must fade inward inside rounded corners: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details.
- **box-shadow list with inset and outset layers** — 100.00%
  - PASS 0.49% box-shadow list with inset and outset layers=inset-outset-list — `r2-box-shadow-inset-outset-list` — mixed inset and outset shadows must paint on opposite sides of the background: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details.
- **box-shadow multiple shadow paint order** — 100.00%
  - PASS 0.00% box-shadow multiple shadow paint order=multiple-order-overlap — `r2-box-shadow-multiple-order-overlap` — multiple box-shadows must paint first shadow on top: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details.
- **box-shadow positive spread with blur and border-radius** — 0.00%
  - FAIL 5.23% box-shadow positive spread with blur and border-radius=spread-blur-radius — `r2-box-shadow-spread-blur-radius` — box-shadow spread plus blur and radius must grow the rounded shadow shape: A deterministic solid-color box isolates the shadow edge/order behavior. Catches wrong implementation: Simplified shadow renderer ignores blur/spread/order/inset clipping details.
- **isolation** — 0.00%
  - FAIL 40.48% isolation=isolate-descendant-blend — `r2-isolation-isolate-descendant-blend` — isolation:isolate must constrain descendant blending: Two identical groups contain a red multiply child over blue page stripes; the isolated group blends only within its own white group backdrop. Catches wrong implementation: Renderer ignores isolation so both red children multiply against the external blue backdrop.
- **mix-blend-mode** — 93.33%
  - FAIL 100.00% mix-blend-mode=luminosity — `mix-blend-mode-luminosity` — mix-blend-mode: luminosity blends a yellow source with a blue backdrop; catches unsupported non-separable blend modes treated as normal.
  - PASS 0.44% mix-blend-mode=multiply — `mix-blend-mode-multiply` — Two overlapping solid boxes whose overlap should darken via mix-blend-mode multiply (aspirational; not implemented).
  - PASS 0.29% mix-blend-mode=screen — `mix-blend-mode-screen` — Two overlapping solid boxes on a dark stage whose overlap should lighten via mix-blend-mode screen (aspirational; not implemented).
  - PASS 0.35% mix-blend-mode=color — `r2-mix-blend-mode-color` — Non-separable mix-blend-mode:color is required: A red square overlaps a blue/gray backdrop using non-separable blend mode color. Catches wrong implementation: Parser treats non-separable blend keywords as normal.
  - PASS 0.35% mix-blend-mode=color-burn — `r2-mix-blend-mode-color-burn` — mix-blend-mode:color-burn needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:color-burn. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.19% mix-blend-mode=color-dodge — `r2-mix-blend-mode-color-dodge` — mix-blend-mode:color-dodge needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:color-dodge. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.19% mix-blend-mode=darken — `r2-mix-blend-mode-darken` — mix-blend-mode:darken needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:darken. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.10% mix-blend-mode=difference — `r2-mix-blend-mode-difference` — mix-blend-mode:difference needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:difference. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.10% mix-blend-mode=exclusion — `r2-mix-blend-mode-exclusion` — mix-blend-mode:exclusion needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:exclusion. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.19% mix-blend-mode=hard-light — `r2-mix-blend-mode-hard-light` — mix-blend-mode:hard-light needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:hard-light. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.35% mix-blend-mode=hue — `r2-mix-blend-mode-hue` — Non-separable mix-blend-mode:hue is required: A red square overlaps a blue/gray backdrop using non-separable blend mode hue. Catches wrong implementation: Parser treats non-separable blend keywords as normal.
  - PASS 0.35% mix-blend-mode=lighten — `r2-mix-blend-mode-lighten` — mix-blend-mode:lighten needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:lighten. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.35% mix-blend-mode=overlay — `r2-mix-blend-mode-overlay` — mix-blend-mode:overlay needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:overlay. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
  - PASS 0.35% mix-blend-mode=saturation — `r2-mix-blend-mode-saturation` — Non-separable mix-blend-mode:saturation is required: A red square overlaps a blue/gray backdrop using non-separable blend mode saturation. Catches wrong implementation: Parser treats non-separable blend keywords as normal.
  - PASS 0.35% mix-blend-mode=soft-light — `r2-mix-blend-mode-soft-light` — mix-blend-mode:soft-light needs direct parity coverage: A foreground red square overlaps a blue square with mix-blend-mode:soft-light. Catches wrong implementation: Renderer maps only multiply/screen or falls back to normal blend for this mode.
- **mix-blend-mode on inline SVG content** — 100.00%
  - PASS 0.17% mix-blend-mode on inline SVG content=inline-svg-screen — `r2-mix-blend-mode-inline-svg` — mix-blend-mode must apply to inline SVG/replaced content: An inline SVG red circle blends with a blue box using screen. Catches wrong implementation: Blend state is applied to CSS boxes but not SVG/replaced drawing paths.
- **mix-blend-mode on text** — 100.00%
  - PASS 1.87% mix-blend-mode on text=text-difference — `r2-mix-blend-mode-text-difference` — mix-blend-mode must apply to text content: Large white text with mix-blend-mode:difference sits over a blue block and should invert the backdrop under glyphs. Catches wrong implementation: Blend wrapper only covers boxes, not text glyph drawing.
- **mix-blend-mode with overflow** — 100.00%
  - PASS 0.24% mix-blend-mode with overflow=rounded-overflow-clip — `r2-mix-blend-mode-clipped-rounded-overflow` — mix-blend-mode must respect clipping ancestors: A blended red child overflows a rounded clipped blue parent; blending is only visible inside the rounded clip. Catches wrong implementation: Renderer applies blend outside the clip or clips before selecting the proper backdrop.
- **opacity group isolation with mix-blend-mode descendant** — 0.00%
  - FAIL 25.28% opacity group isolation with mix-blend-mode descendant=opacity-isolates-blend — `r2-opacity-isolates-descendant-blend` — Opacity-created stacking contexts must isolate inner blending: A parent with opacity:.99 contains a multiply child over a blue page; the opacity group is isolated, so child blends with the group backdrop, not the page. Catches wrong implementation: Renderer applies alpha per draw without isolating the group, allowing the child to multiply with page content behind the parent.
- **text-shadow** — 100.00%
  - PASS 0.25% text-shadow=blur — `text-shadow-blur` — Short heading with a blurred text-shadow glow (aspirational; not implemented).
  - PASS 0.67% text-shadow=hard-offset — `text-shadow-offset` — Short heading with a hard-edged (zero-blur) offset text-shadow (aspirational; not implemented).
- **text-shadow blur with rgba color** — 0.00%
  - FAIL 38.59% text-shadow blur with rgba color=blur-rgba — `r2-text-shadow-blur-rgba` — text-shadow blur with alpha color must preserve soft transparent edges: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order.
- **text-shadow currentColor default** — 0.00%
  - FAIL 3.19% text-shadow currentColor default=currentcolor-default — `r2-text-shadow-currentcolor-default` — text-shadow omitted color resolves to currentColor: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order.
- **text-shadow multiple shadow paint order** — 0.00%
  - FAIL 2.99% text-shadow multiple shadow paint order=multiple-order — `r2-text-shadow-multiple-order` — multiple text-shadows must paint first listed shadow on top: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order.
- **text-shadow shadows text decorations** — 0.00%
  - FAIL 13.87% text-shadow shadows text decorations=decoration-line — `r2-text-shadow-decoration-line` — text-shadow must shadow text decorations too: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order.
- **text-shadow visual overflow** — 100.00%
  - PASS 0.73% text-shadow visual overflow=visual-overflow-layout — `r2-text-shadow-visual-overflow-layout` — text-shadow visual overflow must not affect layout or be clipped to glyph bounds: A large ParitySans text run isolates text-shadow color, order, blur, overflow, or decoration behavior. Catches wrong implementation: Renderer treats text-shadow as a single hard glyph-only pass or uses the wrong default color/order.

### filters — 76.09%
- **backdrop-filter** — 100.00%
  - PASS 1.77% backdrop-filter=backdrop-blur — `r2-backdrop-filter-blur-stripes` — backdrop-filter blur must sample content behind the element: A translucent panel overlays hard vertical stripes; backdrop-filter: blur(8px) should smear the stripes under the panel only. Catches wrong implementation: Renderer treats backdrop-filter as unsupported or applies normal filter to the panel itself.
- **color-interpolation-filters** — 0.00%
  - FAIL 83.36% color-interpolation-filters=color-interpolation-filters-srgb — `r2-filter-color-interpolation-filters-srgb` — color-interpolation-filters must switch SVG filter color space: Two identical muted boxes use feColorMatrix saturate(0); one filter defaults to linearRGB and one explicitly uses sRGB, producing different grays. Catches wrong implementation: Implementation ignores color-interpolation-filters and produces identical gray boxes.
- **filter compositing order with clip-path** — 100.00%
  - PASS 0.25% filter compositing order with clip-path=filter-before-clip-path — `r2-filter-before-clip-path-order` — filter must be applied before clip-path: A blurred red square is clipped to a circle; the blur is first computed then the circular clip removes all outside-circle paint. Catches wrong implementation: Renderer clips first and then blurs, creating a red feather outside the circular clip.
- **filter compositing order with mask-image** — 0.00%
  - FAIL 64.19% filter compositing order with mask-image=filter-before-mask-image — `r2-filter-before-mask-image-order` — filter must be applied before mask-image: A blurred red rectangle is masked by a hard half-plane gradient; blur is computed before the mask clips the right half. Catches wrong implementation: Renderer masks first and then blurs, bleeding red into the transparent half.
- **filter establishes stacking context** — 100.00%
  - PASS 1.28% filter establishes stacking context=filter-stacking-context — `r2-filter-stacking-context-negative-z` — filter-created stacking contexts must contain negative z-index descendants: A filtered parent contains a negative z-index child and sits on a gray page; the child cannot escape behind outside content. Catches wrong implementation: Renderer applies filter paint but does not create a stacking context, letting the child paint behind the page/sibling order incorrectly.
- **filter value grammar with an unknown function** — 100.00%
  - PASS 0.00% filter value grammar with an unknown function=unknown-function-invalidates-list — `r2-filter-invalid-function-chain` — Invalid filter functions must invalidate the whole declaration: A red block has filter: blur(8px) bogus(1); the whole declaration is invalid so it remains crisp. Catches wrong implementation: Parser ignores bogus(1) and still applies blur(8px).
- **filter: blur()** — 100.00%
  - PASS 0.01% filter: blur()=on-box-background — `filter-blur-box` — filter: blur() applied to a box with a solid background-color fill.
  - PASS 0.00% filter: blur()=on-img-raster — `filter-blur-img` — filter: blur() applied to an <img> raster; blur of image rasters is the implemented path.
  - PASS 0.01% filter: blur()=on-box-vs-img — `filter-on-box-blur` — filter: blur() on a bordered solid box: should soften both fill and border edge, contrasting with the img-raster blur path.
- **filter: blur() on a container group** — 0.00%
  - FAIL 35.94% filter: blur() on a container group=blur-group-descendants — `r2-filter-blur-group-descendants` — blur() must apply to the whole element subtree: A container with a blue background, a yellow child, and text is blurred as one source graphic. Catches wrong implementation: Only the container background is blurred, leaving child paint and glyphs crisp.
- **filter: blur() on text** — 0.00%
  - FAIL 73.02% filter: blur() on text=text-glyph-blur — `r2-filter-blur-text-glyphs` — blur() on text glyphs needs raster/filter coverage: Large text with filter: blur(3px) should render as softened glyph shapes. Catches wrong implementation: Text remains vector-crisp while only surrounding box paint changes or no blur is applied.
- **filter: blur() visual overflow** — 0.00%
  - FAIL 36.19% filter: blur() visual overflow=visual-overflow-no-layout — `r2-filter-blur-visual-overflow-layout` — blur() visual overflow must paint outside the border box without moving layout: A blurred red square sits next to a green square; blur feathers extend left/right but the green square keeps its fixed position. Catches wrong implementation: Renderer clips the blur to the red border box or expands layout and shifts the green square.
- **filter: brightness()** — 100.00%
  - PASS 0.40% filter: brightness()=1.6 — `filter-brightness` — filter: brightness(1.6) lightening a four-quadrant color image.
- **filter: brightness() on a container group** — 0.00%
  - FAIL 1.29% filter: brightness() on a container group=brightness-group-descendants — `r2-filter-brightness-group-descendants` — brightness() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:brightness(180%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered.
- **filter: chained** — 50.00%
  - FAIL 2.34% filter: chained=ordered-contrast-blur — `filter-chain-order-contrast-blur` — contrast(5) blur(8px) and blur(8px) contrast(5) produce different edge softness; catches implementations that reorder filter functions.
  - PASS 0.00% filter: chained=blur-plus-brightness — `filter-chained` — Chained filter functions blur(4px) brightness(1.5); parser matches only a single blur() so the chain is unsupported.
- **filter: contrast()** — 100.00%
  - PASS 0.40% filter: contrast()=2 — `filter-contrast` — filter: contrast(2) increasing contrast of a four-quadrant color image.
- **filter: contrast() on a container group** — 0.00%
  - FAIL 1.35% filter: contrast() on a container group=contrast-group-descendants — `r2-filter-contrast-group-descendants` — contrast() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:contrast(220%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered.
- **filter: drop-shadow()** — 50.00%
  - PASS 0.79% filter: drop-shadow()=alpha-diamond-hard — `filter-drop-shadow` — filter: drop-shadow() casting a hard offset shadow that traces a non-rectangular (diamond) image alpha, not its bounding box.
  - FAIL 2.55% filter: drop-shadow()=default-currentcolor — `filter-drop-shadow-currentcolor` — drop-shadow() without a color uses currentColor for the shadow; catches parsers that default the omitted color to black.
- **filter: drop-shadow() followed by brightness()** — 100.00%
  - PASS 0.83% filter: drop-shadow() followed by brightness()=drop-shadow-before-brightness — `r2-filter-drop-shadow-chain-position` — drop-shadow() must obey authored chain position: A dark drop shadow is followed by brightness(2); the shadow itself should be brightened because later filter functions consume the prior output. Catches wrong implementation: Renderer always applies color functions to the source before computing drop-shadow, leaving the shadow dark.
- **filter: drop-shadow() on rounded CSS box alpha** — 100.00%
  - PASS 1.20% filter: drop-shadow() on rounded CSS box alpha=rounded-css-alpha — `r2-filter-drop-shadow-rounded-css-alpha` — drop-shadow() on CSS boxes follows the alpha shape, not a rectangular box-shadow: A circular red element casts a zero-blur drop-shadow; the shadow should be circular because it is based on source alpha. Catches wrong implementation: Implementation treats it like a rectangular box-shadow or only supports drop-shadow on replaced images.
- **filter: drop-shadow(<color> <length>{2,3})** — 100.00%
  - PASS 1.50% filter: drop-shadow(<color> <length>{2,3})=drop-shadow-color-first — `r2-filter-drop-shadow-color-first` — drop-shadow() accepts color before the lengths: The color token appears first in drop-shadow(#00695c 36px 0 0); grammar allows color and lengths in any order. Catches wrong implementation: Parser only accepts color after the lengths and drops the authored teal shadow.
- **filter: grayscale()** — 100.00%
  - PASS 0.20% filter: grayscale()=1 — `filter-grayscale` — filter: grayscale(1) fully desaturating a four-quadrant color image.
  - PASS 1.53% filter: grayscale()=group-descendants — `filter-grayscale-group-descendants` — filter: grayscale(1) applies to a parent source graphic including colored text and a child box; catches implementations that recolor only the parent background or ignore descendants.
- **filter: hue-rotate()** — 100.00%
  - PASS 0.20% filter: hue-rotate()=120deg — `filter-hue-rotate` — filter: hue-rotate(120deg) rotating the hue of a four-quadrant color image.
- **filter: hue-rotate() on a container group** — 100.00%
  - PASS 0.69% filter: hue-rotate() on a container group=hue-rotate-group-descendants — `r2-filter-hue-rotate-group-descendants` — hue-rotate() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:hue-rotate(120deg); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered.
- **filter: invert()** — 100.00%
  - PASS 0.40% filter: invert()=1 — `filter-invert` — filter: invert(1) inverting the colors of a four-quadrant color image.
- **filter: invert() on a container group** — 100.00%
  - PASS 0.97% filter: invert() on a container group=invert-group-descendants — `r2-filter-invert-group-descendants` — invert() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:invert(100%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered.
- **filter: opacity()** — 100.00%
  - PASS 0.61% filter: opacity()=0.5-over-grey — `filter-opacity-fn` — filter: opacity(0.5) compositing a red box over a grey backdrop; distinct from the opacity property.
- **filter: saturate()** — 100.00%
  - PASS 0.40% filter: saturate()=3 — `filter-saturate` — filter: saturate(3) boosting saturation of a four-quadrant color image.
- **filter: saturate() on a container group** — 0.00%
  - FAIL 1.35% filter: saturate() on a container group=saturate-group-descendants — `r2-filter-saturate-group-descendants` — saturate() must filter descendant text and child paint: A container with a solid background, child chip, and glyphs uses filter:saturate(300%); all descendants are part of the filtered source graphic. Catches wrong implementation: Implementation applies the color operation only to the box fill/image path and leaves descendants unfiltered.
- **filter: sepia()** — 100.00%
  - PASS 0.59% filter: sepia()=1 — `filter-sepia` — filter: sepia(1) applying a sepia tone to a four-quadrant color image.
- **filter: url(#id) with feBlend multiply** — 100.00%
  - PASS 0.77% filter: url(#id) with feBlend multiply=feblend-multiply — `r2-filter-url-feblend-multiply` — SVG feBlend must composite primitive inputs: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feColorMatrix luminanceToAlpha** — 100.00%
  - PASS 0.00% filter: url(#id) with feColorMatrix luminanceToAlpha=fecolormatrix-luminance-to-alpha — `r2-filter-url-fecolormatrix-luminance-alpha` — SVG feColorMatrix luminanceToAlpha must affect alpha: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feColorMatrix type=matrix channel swap** — 100.00%
  - PASS 0.31% filter: url(#id) with feColorMatrix type=matrix channel swap=fecolormatrix-channel-swap — `r2-filter-url-fecolormatrix-channel-swap` — SVG feColorMatrix arbitrary matrices must be honored: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feComponentTransfer** — 100.00%
  - PASS 0.31% filter: url(#id) with feComponentTransfer=fecomponenttransfer — `r2-filter-url-fecomponenttransfer` — SVG feComponentTransfer must apply channel transfer functions: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feComposite operator=in** — 100.00%
  - PASS 0.21% filter: url(#id) with feComposite operator=in=fecomposite-in — `r2-filter-url-fecomposite-in` — SVG feComposite must support Porter-Duff operators: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feDropShadow** — 100.00%
  - PASS 0.54% filter: url(#id) with feDropShadow=fedropshadow — `r2-filter-url-fedropshadow` — SVG feDropShadow primitive must render inside filter url(): An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feMerge** — 100.00%
  - PASS 0.26% filter: url(#id) with feMerge=femerge — `r2-filter-url-femerge` — SVG feMerge must paint multiple primitive outputs: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feMorphology** — 100.00%
  - PASS 0.61% filter: url(#id) with feMorphology=femorphology-dilate — `r2-filter-url-femorphology-dilate` — SVG feMorphology must dilate/erode the source alpha: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feOffset** — 100.00%
  - PASS 0.13% filter: url(#id) with feOffset=feoffset — `r2-filter-url-feoffset` — SVG feOffset must move filtered output: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#id) with feTurbulence and feDisplacementMap** — 0.00%
  - FAIL 5.60% filter: url(#id) with feTurbulence and feDisplacementMap=feturbulence-displacement — `r2-filter-url-feturbulence-displacement` — Procedural SVG filter primitives must affect static output: An inline SVG filter referenced from CSS contains a non-feColorMatrix primitive or unsupported matrix form. Catches wrong implementation: CSS filter:url() resolver extracts only a narrow feColorMatrix subset and skips the primitive.
- **filter: url(#missing) blur()** — 100.00%
  - PASS 0.27% filter: url(#missing) blur()=missing-url-invalidates-list — `r2-filter-missing-url-invalidates-chain` — Unresolved filter url() must ignore the entire filter chain: A filter list starts with an unresolved url(#missing) followed by blur(7px); the whole chain is ignored. Catches wrong implementation: Implementation treats the missing url as a no-op and still applies the later blur.
- **filter: url()** — 100.00%
  - PASS 0.23% filter: url()=svg-fecolormatrix — `filter-url-svg` — filter: url(#id) referencing an inline SVG feColorMatrix saturate filter (aspirational).
  - PASS 0.14% filter: url()=svg-fegaussianblur — `filter-url-svg-gaussian-blur` — filter:url(#id) references an inline SVG feGaussianBlur primitive; catches URL-filter implementations limited to feColorMatrix or no-op filters.

### flexbox — 96.00%
- **absolute-positioning** — 100.00%
  - PASS 0.00% absolute-positioning=order-painting — `flexbox-abspos-order-paint` — An absolutely positioned flex child paints as order:0 relative to ordered flex items; catches renderers that append all absolute children above flex content.
- **abspos-flex-child** — 100.00%
  - PASS 0.00% abspos-flex-child=static-position-alignment — `flexbox-abspos-static-position` — An absolutely positioned flex child with auto insets uses the flex static-position rectangle from justify-content and align-self; catches anchoring at the padding-box origin.
- **align-content** — 100.00%
  - PASS 0.00% align-content=center — `flexbox-align-content-center` — align-content:center centers the wrapped flex lines on the cross axis.
  - PASS 0.00% align-content=flex-end — `flexbox-align-content-flex-end` — align-content:flex-end packs the wrapped flex lines to the cross-end edge.
  - PASS 0.00% align-content=flex-start — `flexbox-align-content-flex-start` — align-content:flex-start packs the wrapped flex lines to the cross-start edge.
  - PASS 0.00% align-content=safe-center — `flexbox-align-content-safe-center` — align-content:safe center centers wrapped flex lines when they fit; catches parsers that reject safe center and fall back to stretch.
  - PASS 0.00% align-content=single-line-noop — `flexbox-align-content-single-line-noop` — align-content:flex-end has no effect on a nowrap single-line flex container; catches implementations that move the only line to cross-end.
  - PASS 0.00% align-content=space-around — `flexbox-align-content-space-around` — align-content:space-around gives each wrapped line equal surrounding space (half at edges).
  - PASS 0.00% align-content=space-between — `flexbox-align-content-space-between` — align-content:space-between pins the first and last lines to the edges with equal gaps between.
  - PASS 0.64% align-content=space-evenly — `flexbox-align-content-space-evenly` — align-content:space-evenly distributes equal space between lines and at both cross edges.
  - PASS 0.00% align-content=stretch — `flexbox-align-content-stretch` — align-content:stretch grows the wrapped flex lines to fill the extra cross-axis space.
  - PASS 0.00% align-content=wrap-reverse-edge — `flexbox-align-content-wrap-reverse-edge` — flex-wrap:wrap-reverse swaps the cross-start/cross-end edges, so align-content:flex-start packs the flex lines toward the bottom with reversed line order (css-flexbox-1 §5.3, §8.4).
  - PASS 0.04% align-content=column-wrap-space-between — `flexbox-column-wrap-align-content` — align-content:space-between distributes wrapped column flex lines to the left and right edges; catches row-only align-content code that packs column-wrap lines together.
- **align-items** — 91.67%
  - PASS 0.87% align-items=baseline — `flexbox-align-items-baseline` — align-items:baseline aligns the first text baselines of differently-sized items.
  - PASS 0.00% align-items=center — `flexbox-align-items-center` — align-items:center centers differently-tall boxes on the cross axis of a fixed-height row container.
  - PASS 0.46% align-items=first-baseline — `flexbox-align-items-first-baseline` — align-items:first baseline parses as baseline alignment for differently sized text flex items; catches fallback to stretch or cross-start.
  - PASS 0.00% align-items=flex-end — `flexbox-align-items-flex-end` — align-items:flex-end anchors differently-tall items to the cross-end (bottom) edge.
  - PASS 0.00% align-items=flex-start — `flexbox-align-items-flex-start` — align-items:flex-start anchors differently-tall items to the cross-start (top) edge.
  - PASS 0.03% align-items=last-baseline — `flexbox-align-items-last-baseline` — align-items:last baseline aligns a one-line item's baseline with the last baseline of a two-line flex item; catches unsupported last-baseline parsing.
  - PASS 0.00% align-items=min-height — `flexbox-align-items-min-height` — A non-stretch (align-items:center) item honors its min-height and overflows the shorter container line symmetrically (css-flexbox-1 §9.4 step 11).
  - PASS 0.00% align-items=safe-center — `flexbox-align-items-safe-center` — align-items:safe center is equivalent to center when the item fits; catches parsers that reject the safe overflow-alignment prefix.
  - PASS 0.00% align-items=stretch — `flexbox-align-items-stretch` — align-items:stretch expands auto-height boxes to fill the cross-axis height of the container.
  - PASS 0.00% align-items=stretch-max-height — `flexbox-align-items-stretch-max-height` — align-items:stretch clamps the used cross size to the item's max-height (css-flexbox-1 §9.4 step 11): the capped item stays top-anchored at its max-height while its siblings stretch to the full container height.
  - FAIL 10.84% align-items=baseline-empty-item — `flexbox-baseline-empty-synthesis` — align-items:baseline synthesizes a baseline for an empty fixed-size item rather than falling back to cross-start; catches baseline code that only handles text baselines.
  - PASS 0.00% align-items=column-center — `flexbox-column-align-items-center` — align-items:center in flex-direction:column centers fixed-width items on the horizontal cross axis; catches row-only cross-axis placement or unwanted stretch.
- **align-self** — 100.00%
  - PASS 1.08% align-self=baseline — `flexbox-align-self-baseline` — align-self:baseline opts a single item into baseline alignment within the line.
  - PASS 0.00% align-self=center — `flexbox-align-self-center` — align-self overrides container align-items per item (flex-start container, one centered, one flex-end).
  - PASS 0.00% align-self=flex-end — `flexbox-align-self-flex-end` — align-self overrides container align-items per item: one box flex-end, one centered, one flex-start.
  - PASS 0.00% align-self=safe-center — `flexbox-align-self-safe-center` — align-self:safe center overrides container align-items:flex-start when the item fits; catches parsers that leave align-self:auto.
- **aspect-ratio** — 100.00%
  - PASS 0.00% aspect-ratio=cross-size-after-grow — `flexbox-aspect-ratio-grow-cross-size` — Zero-basis growing flex items with aspect-ratio derive auto cross size from their final grown main size; catches implementations that compute aspect-ratio height before flex-grow.
  - PASS 0.00% aspect-ratio=height-to-basis — `flexbox-aspect-ratio-height-transfer` — A definite height plus aspect-ratio transfers into an auto main-size flex base in a row; catches code that only derives height from width.
- **auto-margins** — 100.00%
  - PASS 0.00% auto-margins=negative-main-free-space — `flexbox-auto-margin-negative-free-space` — Main-axis auto margins are treated as zero when flex items overflow with negative free space; catches auto margins that still absorb space.
  - PASS 0.03% auto-margins=one-sided-cross-axis — `flexbox-cross-auto-margin-one-sided` — A one-sided cross-axis auto margin absorbs free space and pushes only that flex item to cross-end; catches implementations that only handle symmetric cross-axis auto margins.
- **automatic-minimum-size** — 75.00%
  - PASS 0.59% automatic-minimum-size=column-min-height-auto — `flexbox-column-min-height-auto` — In flex-direction:column, min-height:auto gives a shrinkable item a content-based main-axis minimum; catches row-only auto-min logic that compresses content.
  - FAIL 8.22% automatic-minimum-size=min-width-zero — `flexbox-min-width-zero-auto-min` — min-width:0 plus overflow:hidden disables the flex item's automatic min-content floor so equal flex items share the row; catches implementations that always reserve min-content width.
  - PASS 0.69% automatic-minimum-size=overflow-hidden — `flexbox-overflow-hidden-auto-min` — overflow:hidden alone makes a shrinking row flex item's automatic minimum size zero; catches implementations that always keep the min-content floor and push the following item out.
  - PASS 2.45% automatic-minimum-size=overflow-wrap-anywhere — `flexbox-overflow-wrap-anywhere-auto-min` — overflow-wrap:anywhere lowers a long-word flex item's automatic minimum size so the row can keep the following item inside; catches unbreakable min-content floors.
- **display** — 60.00%
  - PASS 0.00% display=block-flex — `flexbox-display-block-flex` — display:block flex uses modern two-keyword display syntax to create a block-level flex container; catches parsers that only recognize display:flex and stack children.
  - PASS 0.00% display=contents-flex-items — `flexbox-display-contents-items` — A display:contents child of a flex container is box-suppressed so its element children become flex items; catches wrappers that incorrectly become one flex item.
  - PASS 0.00% display=flex — `flexbox-display-flex` — display:flex lays three fixed-size boxes in a row along the main axis with default packing.
  - FAIL 53.32% display=inline-flex — `flexbox-display-inline-flex` — display:inline-flex creates an inline-level flex container that shares the line with surrounding text; catches parsers that only recognize block-level display:flex.
  - FAIL 48.82% display=inline-flex-two-keyword — `flexbox-display-inline-flex-two-keyword` — display:inline flex creates an inline-level flex container in a text line; catches parsers that ignore the two-keyword form and lose child sizing.
- **flex** — 100.00%
  - PASS 0.00% flex=keywords — `flexbox-flex-shorthand-keywords` — flex shorthand keywords none/auto/1 expand to 0 0 auto / 1 1 auto / 1 1 0 respectively.
- **flex-basis** — 100.00%
  - PASS 0.41% flex-basis=content — `flexbox-basis-content` — flex-basis:content sizes the item to its content (text) instead of the explicit width:40px.
  - PASS 0.00% flex-basis=percentage-column — `flexbox-column-percentage-flex-basis` — Percentage flex-basis values in a definite-height column flex container resolve against the vertical main size; catches column code that treats percentages as auto.
  - PASS 0.00% flex-basis=px — `flexbox-flex-basis` — flex-basis sets explicit main-size for non-growing non-shrinking boxes (60/160/100px).
  - PASS 0.00% flex-basis=auto — `flexbox-flex-basis-auto` — flex-basis:auto resolves to each item's declared width with no grow/shrink.
  - PASS 0.59% flex-basis=calc-percent — `flexbox-flex-basis-calc-percent` — flex-basis:calc(25% - 10px) resolves against the definite row main size; catches implementations that ignore calc() and use auto or equal-share sizing.
  - PASS 0.84% flex-basis=fit-content — `flexbox-flex-basis-fit-content` — flex-basis:fit-content shrink-to-fit sizes the flex base rather than using a declared width fallback; catches parsers that ignore fit-content.
  - PASS 0.56% flex-basis=max-content — `flexbox-flex-basis-max-content` — flex-basis:max-content must use the item's unwrapped max-content contribution instead of its small width; catches flex-basis parsers that reject intrinsic sizing keywords.
  - PASS 1.29% flex-basis=min-content — `flexbox-flex-basis-min-content` — flex-basis:min-content uses the min-content contribution as the flex base size; catches parsers that fall back to auto or max-content sizing.
  - PASS 0.00% flex-basis=percentage — `flexbox-flex-basis-percentage` — flex-basis percentages resolve against the container inner main size (25/50/25%).
  - PASS 0.23% flex-basis=zero — `flexbox-flex-basis-zero` — flex-basis:0 with equal grow makes every item the same size regardless of content.
- **flex-direction** — 100.00%
  - PASS 0.00% flex-direction=row-rtl-main-start — `flexbox-direction-rtl-row` — flex-direction:row follows direction:rtl so source order starts at the right main-start edge; catches physical left-to-right row layout that ignores logical axis mapping.
  - PASS 0.00% flex-direction=column — `flexbox-flex-direction-column` — flex-direction:column stacks items top-to-bottom along the vertical main axis.
  - PASS 0.00% flex-direction=column-reverse — `flexbox-flex-direction-column-reverse` — flex-direction:column-reverse stacks items bottom-to-top in reverse source order.
  - PASS 0.00% flex-direction=row-reverse — `flexbox-flex-direction-row-reverse` — flex-direction:row-reverse reverses the main axis so items pack from the right edge in reverse source order.
  - PASS 0.00% flex-direction=row-reverse-rtl — `flexbox-row-reverse-rtl` — direction:rtl plus flex-direction:row-reverse reverses the RTL inline axis so visual order runs left-to-right; catches physical row-reverse placement.
- **flex-flow** — 100.00%
  - PASS 0.00% flex-flow=row-wrap — `flexbox-flex-flow` — flex-flow shorthand sets flex-direction and flex-wrap together (row wrap).
- **flex-grow** — 100.00%
  - PASS 0.17% flex-grow=column — `flexbox-column-flex-grow` — In a definite-height column flex container, grow factors distribute vertical free space; catches row-only flex-grow implementations.
  - PASS 0.00% flex-grow=ratio — `flexbox-flex-grow` — flex-grow distributes free space in a 1:2:1 ratio over zero-basis boxes.
  - PASS 0.22% flex-grow=basis-auto — `flexbox-flex-grow-basis-auto` — flex-grow with flex-basis:auto adds free space on top of each item's content-sized base, keeping the larger base wider.
  - PASS 0.00% flex-grow=fractional-sum — `flexbox-grow-fractional` — When the flex-grow factors sum to less than 1, only that fraction of the positive free space is distributed, leaving the remainder unfilled (css-flexbox-1 §9.7 step 4.b).
- **flex-item** — 100.00%
  - PASS 0.00% flex-item=float-clear-ignored — `flexbox-float-clear-ignored` — float and clear on a flex item do not affect its source-order flex placement; catches layout that lets float or clear move the item.
  - PASS 0.00% flex-item=blockification — `flexbox-inline-item-blockification` — Inline direct children are blockified as flex items and honor width and height; catches inline-formatting implementations that render tiny inline spans.
  - PASS 0.00% flex-item=vertical-align-ignored — `flexbox-vertical-align-ignored` — vertical-align on a flex item is ignored and must not override align-items:flex-start; catches inline vertical-align being applied to flex items.
- **flex-item-positioning** — 100.00%
  - PASS 0.00% flex-item-positioning=item-margin — `flexbox-item-margin` — Flex item margin: honored during placement/paint.
  - PASS 0.00% flex-item-positioning=item-relative-offset — `flexbox-item-relative-offset` — Flex item relative-offset: honored during placement/paint.
- **flex-items** — 100.00%
  - PASS 1.18% flex-items=anonymous-text — `flexbox-anonymous-text-items` — Direct text around an element child becomes anonymous flex items and remains visible in the flex row; catches implementations that iterate only element children and drop text nodes.
- **flex-shorthand** — 100.00%
  - PASS 0.12% flex-shorthand=basis-only — `flexbox-flex-shorthand-basis-only` — The one-token flex:80px shorthand is a flex-basis form with grow and shrink enabled; catches parsers that only accept numeric grow values.
  - PASS 0.00% flex-shorthand=grow-basis — `flexbox-flex-shorthand-grow-basis` — The two-token flex:1 80px shorthand means grow factor plus basis, not grow plus shrink; catches parsers that give both items zero basis.
- **flex-shrink** — 100.00%
  - PASS 0.00% flex-shrink=column — `flexbox-column-flex-shrink` — In a definite-height column flex container, negative free space is removed using scaled shrink factors on the vertical main axis; catches row-only or unscaled shrink.
  - PASS 0.17% flex-shrink=ratio — `flexbox-flex-shrink` — flex-shrink removes overflow in a 1:2 ratio when two oversized boxes exceed the container width.
  - PASS 0.00% flex-shrink=zero — `flexbox-flex-shrink-zero` — flex-shrink:0 makes one item refuse to shrink, forcing the others to absorb the overflow.
  - PASS 0.00% flex-shrink=fractional-sum — `flexbox-shrink-fractional` — When the flex-shrink factors sum to less than 1, only that fraction of the negative free space is absorbed, so the items overflow the container (css-flexbox-1 §9.7 step 4.b).
- **flex-wrap** — 100.00%
  - PASS 0.00% flex-wrap=column-wrap — `flexbox-column-wrap` — flex-direction:column with flex-wrap:wrap starts additional columns when items exceed the container height.
  - PASS 0.00% flex-wrap=wrap — `flexbox-flex-wrap` — flex-wrap:wrap breaks four boxes onto two lines when they overflow the container width.
  - PASS 0.32% flex-wrap=nowrap — `flexbox-flex-wrap-nowrap` — flex-wrap:nowrap keeps a single line; shrinkable items compress to fit the container.
  - PASS 0.00% flex-wrap=wrap-reverse — `flexbox-flex-wrap-wrap-reverse` — flex-wrap:wrap-reverse stacks wrapped lines from cross-end toward cross-start (second line above first).
- **fragmentation** — 66.67%
  - PASS 0.00% fragmentation=break-after-item — `flexbox-break-after-item` — break-after:page on a column flex item creates a forced page break after that item; catches flex layout that ignores child break-after.
  - PASS 0.00% fragmentation=break-before-item — `flexbox-break-before-item` — break-before:page on a column flex item creates a forced page break before that item; catches flex layout that ignores child break-before.
  - FAIL 0.11% fragmentation=wrapped-lines-across-pages — `flexbox-fragmentation-wrap-pages` — A wrapped row flex container whose lines exceed one page fragments between flex lines; catches paginators that paint the flex layout as one overflowing atomic row.
- **gap** — 100.00%
  - PASS 0.00% gap=column-row-gap-axis — `flexbox-column-row-gap-main-axis` — row-gap, not column-gap, separates adjacent items along the vertical main axis in flex-direction:column; catches physical column-gap-as-main-gap mapping.
  - PASS 0.00% gap=fixed — `flexbox-gap` — gap:24px inserts a fixed 24px gap between adjacent flex items on the main axis.
  - PASS 0.00% gap=calc — `flexbox-gap-calc` — gap:calc(10px + 10px) computes before flex item placement; catches gap code that ignores calc() values and packs boxes together.
  - PASS 0.40% gap=percentage — `flexbox-gap-percentage` — A percentage gap resolves against the flex container's own content-box width (CSS Box Alignment §8.3), not the parent/ICB width.
  - PASS 0.00% gap=two-value — `flexbox-gap-two-value` — gap:<row> <column> applies the row value between wrapped lines and the column value between items in a line.
  - PASS 0.00% gap=grid-gap-alias — `flexbox-grid-gap-alias` — legacy grid-gap aliases row-gap and column-gap and creates gaps in flex layout; catches engines that only apply grid-gap to CSS Grid.
  - PASS 0.00% gap=longhands — `flexbox-row-column-gap` — row-gap and column-gap longhands set the two axes independently.
  - PASS 0.00% gap=percentage-row-gap — `flexbox-row-gap-percentage-height` — A percentage row-gap in a definite-height wrapped row flex container resolves against the container content-box height; catches width-based or zero percentage gap resolution.
- **intrinsic-sizing** — 100.00%
  - PASS 0.00% intrinsic-sizing=max-content-width — `flexbox-container-width-max-content` — width:max-content shrink-wraps a flex container to its flex line rather than filling the containing block; catches block-width-only flex container sizing.
  - PASS 0.00% intrinsic-sizing=min-content-width — `flexbox-container-width-min-content` — width:min-content gives a flex container an intrinsic width rather than a page-wide block width; catches flex layouts that ignore intrinsic width keywords.
- **justify-content** — 100.00%
  - PASS 0.00% justify-content=column-center — `flexbox-column-justify-center` — justify-content:center in flex-direction:column centers the item group on the vertical main axis; catches horizontal-only justify-content.
  - PASS 0.00% justify-content=center-overflow — `flexbox-justify-center-overflow` — With negative free space (flex-shrink:0 items wider than the container) justify-content:center keeps items centered, overflowing equally past both main-axis edges instead of collapsing to flex-start (css-align-3 §9 Overflow Alignment).
  - PASS 0.00% justify-content=center — `flexbox-justify-content-center` — justify-content:center groups items centered on the main axis with equal leftover space on both sides.
  - PASS 0.00% justify-content=flex-end — `flexbox-justify-content-flex-end` — justify-content:flex-end packs items to the main-end edge.
  - PASS 0.00% justify-content=flex-start — `flexbox-justify-content-flex-start` — justify-content:flex-start packs items to the main-start edge (explicit default packing).
  - PASS 0.43% justify-content=space-around — `flexbox-justify-content-space-around` — justify-content:space-around distributes items with equal space around each, so edge gaps are half the inner gaps.
  - PASS 0.00% justify-content=space-between — `flexbox-justify-content-space-between` — justify-content:space-between pins first and last boxes to the edges with equal gaps between.
  - PASS 0.00% justify-content=space-evenly — `flexbox-justify-content-space-evenly` — justify-content:space-evenly distributes equal space between items and at both edges.
  - PASS 0.00% justify-content=safe-center — `flexbox-justify-safe-center` — The css-align-3 safe/unsafe overflow-alignment prefix is parsed: when content fits, justify-content:safe center is identical to center.
  - PASS 0.00% justify-content=safe-center-overflow — `flexbox-justify-safe-center-overflow` — justify-content:safe center falls back to start alignment when non-shrinking items overflow; catches implementations that strip safe and always perform unsafe centering.
- **margin** — 100.00%
  - PASS 0.00% margin=auto-center — `flexbox-margin-auto-center` — An item with margin:auto centers on both axes: the main-axis auto margins split the free space to center it horizontally and the cross-axis auto margins center it vertically, suppressing the default stretch (css-flexbox-1 §8.1).
  - PASS 0.00% margin=auto-main-end — `flexbox-margin-auto-main-end` — An item with margin-left:auto absorbs the positive main free space and is pushed to the main-end edge, overriding the default flex-start packing (css-flexbox-1 §8.1).
  - PASS 0.00% margin=auto-split — `flexbox-margin-auto-split` — With two items, margin-left:auto on the second pins the first to the main-start and pushes the second to the main-end, splitting the line (nav-bar idiom, css-flexbox-1 §8.1).
- **max-width** — 100.00%
  - PASS 0.00% max-width=clamp — `flexbox-max-width-clamp` — max-width caps a growing flex item and redistributes the leftover free space to the other item.
- **min-height** — 100.00%
  - PASS 0.00% min-height=column-floor — `flexbox-min-height-column` — min-height floors how far a column-direction item may shrink on the main (vertical) axis.
- **min-width** — 100.00%
  - PASS 1.51% min-width=auto-content-min — `flexbox-min-content-no-overflow` — Flex items with min-width:auto floor at their content-based minimum size: two shrinking text items stop at min-content and sit side by side without overlapping (css-flexbox-1 §4.5).
  - PASS 0.00% min-width=shrink-floor — `flexbox-min-width-constraint` — min-width floors how far a flex item may shrink, forcing the other item to absorb the rest of the overflow.
- **nested-flex** — 100.00%
  - PASS 0.00% nested-flex=column-background — `flexbox-nested-column-bg` — A nested column flex item with its own background stretches to the row cross size and paints both stacked children (previously only the column background drew and the children vanished).
  - PASS 0.00% nested-flex=column-basis-vs-width — `flexbox-nested-column-stretch` — Column-direction items set a main-axis flex-basis but no width, so align-items:stretch fills the cross-axis width instead of the basis leaking into the item width.
  - PASS 0.00% nested-flex=row-of-columns — `flexbox-nested-flex` — A row flex container holds two growing column flex containers, each space-between stacking two boxes.
  - PASS 0.00% nested-flex=row-in-row — `flexbox-nested-row-in-row` — A row flex item that is itself a flex row paints its own background and both inner boxes (previously the whole nested row was dropped, leaving a blank container).
- **order** — 100.00%
  - PASS 0.00% order=reorder — `flexbox-order` — order reorders source boxes (3,1,2) into visual order blue,green,red on the main axis.
  - PASS 0.00% order=integer-grammar — `flexbox-order-decimal-invalid` — order:1.5 is invalid because order accepts integers, so source order is preserved; catches parsers that truncate decimal order values.
  - PASS 0.00% order=negative — `flexbox-order-negative` — A negative order value moves an item before the order:0 items; equal orders keep source order.
  - PASS 0.00% order=painting — `flexbox-order-paint-overlap` — Overlapping flex items paint in order-modified document order when z-index is equal; catches source-order painting that leaves the lower-order item on top.
- **painting** — 100.00%
  - PASS 0.00% painting=static-flex-item-z-index — `flexbox-z-index-static` — Static flex items paint in z-index order; overlapping items expose incorrect source-order painting.
- **percent-height** — 100.00%
  - PASS 0.00% percent-height=stretch-definite — `flexbox-percent-height-stretch` — align-items:stretch gives a height-less flex item a definite cross size, so its height:50% child resolves against the stretched 180px line (90px).
- **place-content** — 100.00%
  - PASS 0.00% place-content=justify-content — `flexbox-place-content-justify` — place-content:center space-between sets flex justify-content:space-between in a row container; catches implementations that ignore place-content.
- **place-items** — 100.00%
  - PASS 0.00% place-items=align-items — `flexbox-place-items-center` — place-items:center sets align-items:center for a flex row container; catches grid-only place-items implementations that leave items at cross-start.
- **place-self** — 100.00%
  - PASS 0.00% place-self=align-self — `flexbox-place-self-end` — place-self:end sets flex align-self to the cross-end side; catches grid-only place-self implementations that leave the item at flex-start.
- **writing-mode** — 100.00%
  - PASS 0.00% writing-mode=vertical-lr-row — `flexbox-writing-mode-vertical-lr-row` — writing-mode:vertical-lr with flex-direction:row follows the vertical inline axis; catches parsers that treat vertical-lr as horizontal-tb.
  - PASS 0.00% writing-mode=vertical-rl-column — `flexbox-writing-mode-vertical-rl-column` — In writing-mode:vertical-rl, flex-direction:column follows the horizontal block axis right-to-left; catches physical top-to-bottom column layout.
  - PASS 0.00% writing-mode=vertical-rl-row — `flexbox-writing-mode-vertical-rl-row` — In writing-mode:vertical-rl, flex-direction:row follows the vertical inline axis and stacks items top-to-bottom; catches physical horizontal row layout.

### fonts-advanced — 80.77%
- **@font-face font-style descriptor** — 100.00%
  - PASS 2.17% @font-face font-style descriptor=font-face-style-descriptor — `fonts-advanced-font-face-style-descriptor` — A shared @font-face family maps normal to ParitySans and italic to ParitySerif; font-style:italic selects the italic descriptor source. Catches: Ignoring font-style descriptors passes a single-face @font-face test but cannot match style-specific faces.
- **@font-face font-weight descriptor** — 100.00%
  - PASS 2.23% @font-face font-weight descriptor=font-face-weight-descriptor — `fonts-advanced-font-face-weight-descriptor` — Two @font-face rules share a family name but map normal to ParitySerif and bold to ParitySans; font-weight:bold must select the bold descriptor face. Catches: A parser that ignores descriptors registers one family/source and passes simple custom-font fixtures but chooses the wrong face for bold text.
- **@font-face size-adjust descriptor** — 100.00%
  - PASS 2.23% @font-face size-adjust descriptor=font-face-size-adjust — `fonts-advanced-font-face-size-adjust` — An @font-face alias applies size-adjust:150% to ParitySans, making it visibly larger than a normal ParitySans row at the same CSS font-size. Catches: An @font-face parser that extracts only family/src passes basic custom-source tests but ignores metric override descriptors.
- **@font-face src:local()** — 100.00%
  - PASS 2.77% @font-face src:local()=font-face-local-src — `fonts-advanced-font-face-local-src` — An @font-face rule aliases local ParitySerif and then uses that alias for a line of text. Catches: A url-only @font-face extractor passes relative URL fixtures but ignores local() sources.
- **@font-face unicode-range** — 100.00%
  - PASS 2.17% @font-face unicode-range=font-face-unicode-range — `fonts-advanced-font-face-unicode-range` — Two @font-face rules share a family; uppercase A comes from ParitySerif and uppercase B from ParitySans via unicode-range. Catches: A font registry that ignores unicode-range passes ordinary fallback tests but renders all glyphs from one face.
- **font shorthand** — 0.00%
  - FAIL 21.01% font shorthand=font-shorthand — `fonts-advanced-font-shorthand` — A single font shorthand declaration sets italic bold 28px/36px ParitySerif in one declaration. Catches: Parsing only longhands passes separate font-size/weight/style/family tests but drops the shorthand entirely.
- **font-face** — 100.00%
  - PASS 1.70% font-face=custom-src — `fonts-advanced-font-face-custom-src` — An @font-face rule registering a second custom family ('ParityCustom') from the bundled ParitySerif TTF via src: url(), exercising the @font-face declaration and family-matching mechanism.
- **font-feature-settings** — 100.00%
  - PASS 1.39% font-feature-settings=ligatures-off — `fonts-advanced-font-feature-settings-ligatures` — Text with font-feature-settings: "liga" 0 to disable standard ligatures (aspirational; no CSS font-feature control, default shaping always applied).
- **font-kerning:none** — 100.00%
  - PASS 0.00% font-kerning:none=font-kerning-none — `fonts-advanced-font-kerning-none` — The AVAV pair is rendered with and without kerning so the no-kerning row is visibly wider/looser. Catches: Always using the shaper defaults passes normal text tests but cannot disable kern feature application.
- **font-size** — 100.00%
  - PASS 2.16% font-size=ch — `fonts-advanced-font-size-ch` — font-size: 5ch sized relative to the advance of the '0' glyph in a monospace face (aspirational; the ch unit is not confirmed in length parsing).
  - PASS 0.00% font-size=em — `fonts-advanced-font-size-em` — font-size: 2em resolved against the parent region's 16px font-size for a 32px effective size.
  - PASS 3.40% font-size=ex — `fonts-advanced-font-size-ex` — font-size: 4ex sized relative to the font's x-height (aspirational; the ex unit is not confirmed in length parsing).
  - PASS 1.37% font-size=percent — `fonts-advanced-font-size-percent` — font-size: 150% resolved against the parent region's 20px font-size for a 30px effective size.
  - PASS 0.00% font-size=rem — `fonts-advanced-font-size-rem` — font-size: 2rem resolved against the root 16px font-size (32px), independent of the local 10px font-size.
- **font-size-adjust:0.8** — 0.00%
  - FAIL 11.29% font-size-adjust:0.8=font-size-adjust — `fonts-advanced-font-size-adjust` — Two same-size lines compare default font sizing with font-size-adjust:0.8, which changes used font size to match the requested aspect value. Catches: A font-size-only implementation passes px/em/rem fixtures but ignores x-height adjustment.
- **font-stretch** — 100.00%
  - PASS 1.99% font-stretch=condensed — `fonts-advanced-font-stretch-condensed` — Text with font-stretch: condensed selecting a narrower face/width (aspirational; font-stretch is not parsed or matched).
- **font-style:oblique 20deg** — 100.00%
  - PASS 2.35% font-style:oblique 20deg=font-style-oblique-angle — `fonts-advanced-font-style-oblique-angle` — A word uses font-style:oblique 20deg and should visibly slant while a normal reference remains upright. Catches: Recognizing only the bare oblique keyword passes simple oblique tests but drops angle syntax.
- **font-synthesis:none** — 0.00%
  - FAIL 2.69% font-synthesis:none=font-synthesis-none — `fonts-advanced-font-synthesis-none` — A local @font-face exposes only a regular face; font-weight:bold with font-synthesis:none must remain regular instead of fake-bolding. Catches: A renderer that always synthesizes missing bold/italic passes bold fallback tests but violates explicit synthesis suppression.
- **font-variant** — 100.00%
  - PASS 1.99% font-variant=small-caps — `fonts-advanced-font-variant-small-caps` — Mixed-case text rendered with font-variant: small-caps so lowercase letters become small uppercase forms (aspirational; no font-variant support).
- **font-variant-caps** — 0.00%
  - FAIL 1.81% font-variant-caps=small-caps-synthesis — `fonts-advanced-small-caps-synthesis` — font-variant-caps:small-caps synthesizes smaller uppercase forms for lowercase letters; catches implementations that ignore font variant caps.
- **font-variant-ligatures:no-common-ligatures** — 100.00%
  - PASS 1.98% font-variant-ligatures:no-common-ligatures=font-variant-ligatures-none — `fonts-advanced-font-variant-ligatures-none` — A serif line disables common ligatures through the high-level font-variant-ligatures property. Catches: Supporting only low-level font-feature-settings passes a liga-off fixture but ignores the high-level variant property.
- **font-weight:600** — 100.00%
  - PASS 2.04% font-weight:600=font-weight-600 — `fonts-advanced-font-weight-600` — With regular and bold faces available, font-weight:600 should choose/synthesize a visibly heavier weight than 400. Catches: Collapsing only 700+ to bold passes bold/normal tests but renders 600 as regular.
- **font-weight:bolder/lighter** — 0.00%
  - FAIL 2.85% font-weight:bolder/lighter=font-weight-relative — `fonts-advanced-font-weight-relative` — Nested spans use bolder and lighter relative to a 600-weight parent, producing visibly different weights. Catches: A normal/bold-only parser treats bolder/lighter as normal and passes absolute weight fixtures.
- **text-transform** — 100.00%
  - PASS 1.97% text-transform=capitalize — `fonts-advanced-text-transform-capitalize` — Lowercase words rendered with text-transform: capitalize so the first letter of each word is uppercased.
  - PASS 1.67% text-transform=lowercase — `fonts-advanced-text-transform-lowercase` — Uppercase source text rendered with text-transform: lowercase so every glyph is lowercased before shaping.
  - PASS 1.71% text-transform=uppercase — `fonts-advanced-text-transform-uppercase` — Lowercase source text rendered with text-transform: uppercase so every glyph is uppercased before shaping.

### generated-content — 67.57%
- **::before{display:list-item}** — 0.00%
  - FAIL 7.66% ::before{display:list-item}=before-list-item-marker — `generated-content-before-list-item-marker` — A block's ::before pseudo is display:list-item with list-style-type:square, producing a marker before the generated label. Catches: Supporting inline/block generated pseudos passes existing ::before fixtures but drops marker generation for list-item pseudos.
- **::first-letter numeric first-letter unit** — 0.00%
  - FAIL 2.38% ::first-letter numeric first-letter unit=first-letter-leading-digit — `generated-content-first-letter-leading-digit` — A paragraph starts with a digit and ::first-letter should style that digit before the rest of the text. Catches: An implementation limited to alphabetic letters passes prose drop-cap fixtures but ignores numeric initial-letter units.
- **::first-letter punctuation unit** — 0.00%
  - FAIL 2.59% ::first-letter punctuation unit=first-letter-leading-quote — `generated-content-first-letter-leading-quote` — A paragraph begins with an opening quote; ::first-letter background and color should apply to the quote plus the first letter as one unit. Catches: Styling only the first alphanumeric glyph passes simple first-letter tests but leaves the quote unstyled.
- **::first-line letter-spacing** — 0.00%
  - FAIL 16.20% ::first-line letter-spacing=first-line-letter-spacing — `generated-content-first-line-letter-spacing` — Only the first formatted line gets 5px letter spacing, changing where that line wraps relative to later lines. Catches: Restyling after line breaking passes color-only first-line fixtures but misses geometry-affecting first-line properties.
- **::first-line line-height** — 100.00%
  - PASS 4.83% ::first-line line-height=first-line-line-height — `generated-content-first-line-line-height` — The first line gets a 54px line-height while later lines keep 24px, creating extra vertical space after line one. Catches: Applying first-line style after wrapping without recomputing line metrics passes color/background tests but keeps uniform line heights.
- **::first-line text-transform** — 100.00%
  - PASS 0.00% ::first-line text-transform=first-line-text-transform — `generated-content-first-line-text-transform` — A wrapped paragraph uppercases only the first formatted line through ::first-line{text-transform:uppercase}. Catches: A first-line implementation that only copies color/background/font-weight passes existing first-line tests while leaving text case unchanged.
- **content-attr** — 100.00%
  - PASS 2.98% content-attr=before-attr — `generated-content-attr` — content: attr(data-prefix) pulls the value of an HTML data attribute into ::before generated text.
  - PASS 1.80% content-attr=attr-missing-empty — `generated-content-attr-missing` — attr() of a missing attribute resolves to the empty string, leaving only the literal brackets.
- **content-counter** — 100.00%
  - PASS 1.59% content-counter=counter-reset-increment — `generated-content-counter` — counter-reset plus counter-increment with content: counter(step) numbers three stacked items via ::before.
  - PASS 1.38% content-counter=counter-style-upper-roman — `generated-content-counter-roman` — content: counter(step, upper-roman) renders item numbers as I, II, III, IV.
- **content-counters** — 100.00%
  - PASS 2.86% content-counters=counters-nested — `generated-content-counters-nested` — content: counters(name, ".") numbers nested list items as 1, 1.1, 1.2, 2 via ::before.
- **content-list** — 100.00%
  - PASS 3.32% content-list=string-counter-attr-mix — `generated-content-content-list-mix` — A single content declaration concatenates a string, counter(), and attr() into one ::before label.
- **content-suppression** — 100.00%
  - PASS 1.67% content-suppression=content-none — `generated-content-content-none` — content: none on ::before suppresses the pseudo-element so only the original element text renders.
  - PASS 1.67% content-suppression=content-normal — `generated-content-content-normal` — content: normal on ::before computes to none and suppresses the pseudo-element.
- **content-url** — 100.00%
  - PASS 1.05% content-url=before-image — `generated-content-content-url-image` — content: url(data:png) on ::before places a small decoded raster image before the element text.
  - PASS 1.05% content-url=before-image-sized — `generated-content-content-url-sized` — content: url(data:png) with explicit width/height scales the ::before replaced image.
- **content:leader('.')** — 0.00%
  - FAIL 8.35% content:leader('.')=leader-function — `generated-content-leader-function` — A table-of-contents row inserts a dotted leader between a title and a page number. Catches: A generated-content parser without leader() passes string and counter fixtures but leaves a blank gap before page numbers.
- **first-letter** — 100.00%
  - PASS 5.78% first-letter=color-background — `generated-content-first-letter-color` — ::first-letter recolors the initial letter white on an orange background at the same size.
  - PASS 4.77% first-letter=drop-cap — `generated-content-first-letter-dropcap` — ::first-letter floats and enlarges the initial letter of a paragraph into a drop cap with text wrapping beside it.
  - PASS 6.00% first-letter=text-transform-uppercase — `generated-content-first-letter-transform` — ::first-letter text-transform:uppercase uppercases just the initial letter.
- **first-line** — 66.67%
  - PASS 7.23% first-line=color-weight — `generated-content-first-line` — ::first-line restyles only the first wrapped line of a paragraph to red bold while later lines stay normal.
  - PASS 7.41% first-line=background-color — `generated-content-first-line-background` — ::first-line paints a highlight background behind the first wrapped line only.
  - FAIL 6.88% first-line=font-size-geometry — `generated-content-first-line-font-size` — ::first-line font-size and line-height change the first formatted line geometry; catches implementations that only restyle first-line paint.
- **no-open-quote/no-close-quote depth effects** — 0.00%
  - FAIL 3.87% no-open-quote/no-close-quote depth effects=no-open-quote-depth — `generated-content-no-open-quote-depth` — A hidden opener increments quote depth before a visible nested quote, so the visible quote uses the second-level pair. Catches: Treating no-open-quote as a no-op passes no-glyph tests but fails depth-dependent nested quote rendering.
- **open-quote/close-quote quote depth across nested elements** — 0.00%
  - FAIL 28.08% open-quote/close-quote quote depth across nested elements=nested-q-quote-depth — `generated-content-nested-q-quote-depth` — Nested q elements use generated open-quote/close-quote with two quote pairs; the inner quote must use the second pair. Catches: Resolving quote depth independently inside each content declaration passes single open/close quote fixtures but never reaches nested quote pairs.
- **position:running() and element()** — 0.00%
  - FAIL 5.65% position:running() and element()=running-element-margin — `generated-content-running-element-margin` — A small heading is captured with position:running(head) and reused by @page top-left via element(head). Catches: Page margin boxes that only support strings/page counters pass simple margin content tests but cannot render running elements.
- **pseudo-element** — 100.00%
  - PASS 1.79% pseudo-element=after-on-replaced-img — `generated-content-after-replaced` — ::after on a replaced <img> generates no box; only the image renders.
  - PASS 2.57% pseudo-element=after-content-string — `generated-content-after-string` — ::after appends a literal string after the element text inside a filled, bordered box.
  - PASS 4.14% pseudo-element=before-display-block — `generated-content-before-block` — ::before with display:block renders a full-width colored header band above the body text.
  - PASS 2.81% pseudo-element=before-empty-decorative-box — `generated-content-before-decorative-box` — content: "" with display:inline-block renders an empty ::before as a filled, bordered marker box before the text.
  - PASS 2.78% pseudo-element=before-content-string — `generated-content-before-string` — ::before inserts a literal red string in front of the element text inside a filled, bordered box.
- **quotes** — 100.00%
  - PASS 3.90% quotes=nested-two-levels — `generated-content-nested-quotes` — Two quote pairs nest: open-quote/open-quote/close-quote/close-quote use the first then second declared pair.
  - PASS 2.67% quotes=no-open-no-close-quote — `generated-content-no-quote-keywords` — no-open-quote emits no glyph while close-quote still renders the declared closing quote.
  - PASS 2.41% quotes=open-close-quote — `generated-content-open-close-quote` — content: open-quote / close-quote on ::before and ::after wraps text in quotation marks from the quotes property.
- **string-set and string()** — 0.00%
  - FAIL 3.06% string-set and string()=string-set-running-header — `generated-content-string-set-running-header` — A heading stores its text in a named running string and the page top-center margin prints string(chapter). Catches: @page literal/counter support passes simple page footer tests but cannot collect named strings from document content.
- **target-counter(attr(href), page)** — 0.00%
  - FAIL 23.39% target-counter(attr(href), page)=target-counter-page — `generated-content-target-counter-page` — A link to a later section appends that target's page number through target-counter(attr(href), page). Catches: Counter() support in normal flow passes local counter tests but cannot resolve target page cross references.
- **target-text(attr(href))** — 0.00%
  - FAIL 6.63% target-text(attr(href))=target-text — `generated-content-target-text` — A cross-reference link appends the heading text of its target using target-text(attr(href)). Catches: An attr()/counter-only content implementation passes generated string tests but cannot resolve target element text.

### grid — 98.88%
- **absolute-positioning** — 100.00%
  - PASS 0.00% absolute-positioning=excluded-from-grid-flow — `grid-absolute-grid-child-does-not-consume-an-auto-placement-cell` — An absolutely positioned child should overlay the grid container and not take the first grid cell from in-flow items; catches element-only grid placement can accidentally include abspos children as grid items and still pass non-positioned fixtures.
  - PASS 0.00% absolute-positioning=grid-area-containing-block — `grid-absolute-grid-child-positions-against-its-specified-grid-area` — An abspos child with grid-column:2 / 3 and grid-row:2 / 3 should use that grid area as its containing block; catches laying every absolute child against the grid padding box passes out-of-flow tests that do not set grid placement.
- **align-content** — 100.00%
  - PASS 0.00% align-content=end — `grid-align-content-positions-grid-rows-in-a-tall-container` — align-content:end should pack fixed-height rows at the block-end of a taller grid container; catches only stretching auto rows for normal alignment misses explicit align-content values.
- **align-items** — 100.00%
  - PASS 0.00% align-items=start — `grid-align-items-start` — align-items: start aligns short cells to the block (top) start of each track.
  - PASS 0.59% align-items=baseline — `grid-items-baseline-align-across-a-row` — align-items:baseline should align text baselines of differently sized grid items rather than stretching or top-aligning them; catches mapping baseline to stretch passes start/end/center/stretch alignment fixtures.
- **direction** — 100.00%
  - PASS 0.00% direction=rtl-grid-line-numbering — `grid-rtl-direction-reverses-grid-column-line-start` — With direction:rtl, grid-column:1 / 2 should address the rightmost column line interval; catches physical left-to-right line numbering passes all LTR placement fixtures.
- **display** — 66.67%
  - PASS 0.00% display=contents-grid-items — `grid-display-contents-flattens-children-into-grid` — A display:contents wrapper should not consume a grid cell; its two children should become separate grid items; catches treating the wrapper as a normal grid item passes direct-child grid fixtures but stacks both grandchildren inside one cell.
  - PASS 0.00% display=grid — `grid-display-grid` — 2x2 grid of fixed-px tracks establishing a grid formatting context; four colored cells fill the tracks.
  - FAIL 71.05% display=inline-grid — `grid-inline-grid-participates-in-inline-flow` — An inline-grid between two inline labels should remain on the same text line and occupy only its grid width; catches treating inline-grid as block or ignoring it still passes block-level display:grid fixtures.
- **fragmentation** — 100.00%
  - PASS 0.00% fragmentation=spanned-grid-area — `grid-row-spanning-grid-item-fragments-across-spanned-rows` — A grid item spanning multiple rows should keep its area geometry and fragment content consistently across page breaks; catches approximating spanning items as a single starting-row cell passes simple row-span layout but fails paged output.
  - PASS 0.00% fragmentation=grid-rows — `grid-rows-fragment-between-pages` — A grid with fixed-height rows taller than the page should break between rows rather than clipping or overlapping rows on one page; catches single-page grid fixtures cannot detect whether grid row boxes participate in pagination.
  - PASS 1.04% fragmentation=grid-item — `grid-tall-grid-item-fragments-inside-its-grid-area` — A tall block child inside one grid item should fragment across pages without escaping the grid item's containing area geometry; catches paginating between grid rows only passes row-break fixtures while clipping tall cell contents.
- **gap** — 100.00%
  - PASS 0.00% gap=row-column-gap — `grid-gap` — 2x2 grid with distinct row-gap (20px) and column-gap (40px) separating the cells.
  - PASS 0.00% gap=legacy-grid-gap-aliases — `grid-legacy-grid-row-and-column-gap-aliases-apply-separately` — grid-row-gap and grid-column-gap should map to row-gap and column-gap with independent values; catches supporting only gap or grid-gap passes the current generic gap fixture.
  - PASS 0.00% gap=percentage — `grid-percentage-grid-gaps-resolve-from-container-size` — A 10% column gap in a 200px grid should create a 20px gutter between two 90px columns; catches parsing percentage gaps but never resolving them in grid layout passes length gap fixtures.
- **generated-content** — 100.00%
  - PASS 0.00% generated-content=pseudo-grid-items — `grid-container-pseudo-elements-are-grid-items` — A grid container's ::before generated box should participate as the first grid item before DOM children; catches ignoring generated grid items passes DOM-child-only auto-placement fixtures.
- **grid** — 100.00%
  - PASS 0.00% grid=auto-flow-shorthand — `grid-shorthand-defines-auto-flow-rows` — The grid shorthand should set explicit columns and grid-auto-flow/auto rows in one declaration; catches supporting only grid-template longhands and shorthand area form misses common grid:auto-flow syntax.
  - PASS 0.00% grid=shorthand-reset — `grid-shorthand-resets-omitted-implicit-grid-properties` — A later grid shorthand should reset a previous grid-auto-flow:column to row flow when omitted; catches a cascade implementation that treats grid as unrelated leaves stale auto-flow values and still passes isolated shorthand tests.
- **grid-area** — 100.00%
  - PASS 0.00% grid-area=line-form — `grid-area-line-form` — grid-area four-value line form (row-start / col-start / row-end / col-end) places a 2x2 block and a single trailing cell.
  - PASS 0.00% grid-area=two-value — `grid-area-omitted-end-sides-default-correctly` — grid-area: 2 / 2 should place an item at row 2 column 2 with auto end sides resolving to a one-cell span; catches only testing the four-value grid-area form misses omitted-value shorthand behavior.
  - PASS 0.15% grid-area=implicit-named-area — `grid-explicit-start-end-lines-create-an-implicit-named-area` — A grid-area name should resolve through explicit matching foo-start/foo-end column and row line names even without grid-template-areas; catches engines that only resolve grid-area names from grid-template-areas pass area-string fixtures.
- **grid-auto-columns** — 100.00%
  - PASS 0.00% grid-auto-columns=implicit-columns — `grid-auto-columns-implicit` — An item placed in column 3 creates implicit columns sized by grid-auto-columns; catches engines that collapse implicit columns to zero width.
  - PASS 0.00% grid-auto-columns=pattern — `grid-auto-columns-repeats-a-track-size-pattern` — Implicit columns created by explicit placement should cycle through grid-auto-columns values; catches having no grid-auto-columns model or only a single value passes simple explicit-column fixtures.
- **grid-auto-flow** — 100.00%
  - PASS 0.00% grid-auto-flow=column — `grid-auto-flow-column` — grid-auto-flow: column places items down each column before wrapping to the next.
  - PASS 0.00% grid-auto-flow=row-dense — `grid-auto-flow-dense` — grid-auto-flow:row dense backfills a one-column item into an earlier top-row hole; catches sparse-only auto-placement.
  - PASS 0.00% grid-auto-flow=column-dense — `grid-column-dense-auto-placement-backfills-holes` — In column flow, dense packing should backfill a later one-row item into an earlier hole in the first column; catches testing only row dense or sparse column flow misses column-major dense search.
- **grid-auto-placement** — 100.00%
  - PASS 0.00% grid-auto-placement=definite-column — `grid-auto-placement-handles-definite-column-only` — Items pinned to column 3 but with auto rows should stack down column 3 without occupying earlier columns; catches a row-major-only auto placer can pass default placement while mishandling one-axis definite column placement.
  - PASS 0.00% grid-auto-placement=definite-row — `grid-auto-placement-handles-definite-row-only` — Items pinned to row 2 but with auto columns should fill row 2 columns in order without consuming row 1; catches a simplistic auto-placement implementation can pass fully-auto and fully-definite fixtures while ignoring one-axis definite placement.
- **grid-auto-rows** — 100.00%
  - PASS 0.00% grid-auto-rows=pattern — `grid-auto-rows-repeats-a-track-size-pattern` — Implicit rows should cycle through the grid-auto-rows list: 30px, 70px, 30px, ; catches storing only one grid-auto-rows length passes single-size implicit-row fixtures.
  - PASS 0.00% grid-auto-rows=implicit-tracks — `grid-implicit-tracks` — Six items in a two-column grid generate implicit rows sized by grid-auto-rows: 60px.
- **grid-column** — 100.00%
  - PASS 0.00% grid-column=line-numbers — `grid-column-line-numbers` — Line-number column placement: grid-column: 2 / 4 places a two-track item at columns 2-3; a second item at 4 / 5 sits in the last track.
  - PASS 0.00% grid-column=span — `grid-column-span` — First cell spans two columns (grid-column: span 2) within a 3x2 grid.
  - PASS 0.00% grid-column=start-span — `grid-column-span-to-line` — Definite start line with a span: grid-column: 2 / span 2 places a two-track item starting at column line 2.
  - PASS 0.00% grid-column=named-line — `grid-named-line-placement` — Named column lines [c1]..[c4] referenced by grid-column place items between the declared lines.
  - PASS 0.12% grid-column=negative-lines — `grid-negative-grid-line-numbers-count-from-the-end` — grid-column: -3 / -1 should span the last two columns of a four-column explicit grid; catches positive-only line fixtures cannot catch engines that parse negative lines as auto or zero.
  - PASS 0.00% grid-column=repeated-named-lines — `grid-repeated-named-lines-resolve-nth-occurrence` — grid-column: 2 col / 3 col should select later occurrences of the repeated line name; catches resolving every named line to its first occurrence passes basic named-line fixtures.
  - PASS 0.00% grid-column=span-integer-named-line — `grid-span-integer-named-line-syntax-is-honored` — grid-column: span 2 target / target 3 should count named lines as well as a numeric span; catches a parser accepting only span N or span name passes simpler span fixtures but treats the combined syntax as span 1 or auto.
  - PASS 0.00% grid-column=span-named-line — `grid-span-named-line-searches-to-matching-line` — grid-column: left / span right should span from the left named line to the next right named line, covering two tracks; catches approximating every named span as span 1 passes numeric span fixtures.
- **grid-items** — 100.00%
  - PASS 0.00% grid-items=auto-margins — `grid-auto-margins-align-grid-items-before-self-alignment` — A grid item with margin-left:auto should absorb free inline space and move to the right edge of its grid area; catches ignoring grid item margins and relying only on justify-self passes explicit justify-self tests.
  - PASS 0.00% grid-items=anonymous-text — `grid-direct-text-creates-anonymous-grid-items` — Direct text between element children in a grid container should occupy its own anonymous grid item and shift later cells; catches a grid implementation that filters out text nodes passes element-only grid fixtures.
  - PASS 0.14% grid-items=blockification — `grid-inline-child-grid-item-is-blockified` — An inline span grid child with width and height should be blockified as a grid item and honor its box size/alignment inside the cell; catches an implementation that leaves inline grid items shrink-wrapped to text can pass fixtures using only div children.
  - PASS 0.00% grid-items=margins — `grid-item-margins-inset-the-item-box` — A grid item with margins should paint inset from its grid area and leave white space around the item background; catches ignoring grid item margins passes current padding/border/alignment fixtures.
  - PASS 0.00% grid-items=percentage-margins — `grid-item-percentage-margins-use-grid-area-inline-size` — A 10% left/right margin on a 200px grid area should create 20px side insets, independent of page width; catches resolving percentages against the page or ignoring margins can pass fixed-margin tests.
- **grid-placement** — 100.00%
  - PASS 0.00% grid-placement=longhands — `grid-placement-longhands` — grid-column-start/end and grid-row-start/end longhands place a two-column two-row item; catches shorthand-only placement support.
- **grid-row** — 100.00%
  - PASS 0.00% grid-row=line-numbers — `grid-row-line-numbers` — Line-number row placement: grid-row: 1 / 3 spans two rows; grid-row: 3 / 4 places the second item in the third row.
  - PASS 0.00% grid-row=span — `grid-row-span` — First cell spans two rows (grid-row: span 2) within a 2x2 grid.
- **grid-template** — 100.00%
  - PASS 0.00% grid-template=areas-and-tracks — `grid-template-shorthand` — The grid-template shorthand defines area strings plus row and column tracks; catches engines that implement only the longhands.
- **grid-template-areas** — 100.00%
  - PASS 0.00% grid-template-areas=implicit-lines — `grid-area-names-generate-start-and-end-lines` — grid-template-areas should create hero-start and hero-end lines usable by grid-column/grid-row; catches implementing named areas without generated lines passes grid-area:<name> fixtures.
  - PASS 0.00% grid-template-areas=span-rows — `grid-area-span-rows` — grid-template-areas with a 'nav' area spanning two rows beside stacked main/foot regions.
  - PASS 0.00% grid-template-areas=non-rectangular-invalid — `grid-invalid-template-areas` — A non-rectangular named area invalidates grid-template-areas, so named placement falls back to auto; catches bounding-box rendering of invalid areas.
  - PASS 0.00% grid-template-areas=basic — `grid-template-areas-basic` — grid-template-areas names a header spanning both columns over a side/main row; grid-area: <name> places each item into its region.
  - PASS 0.00% grid-template-areas=implicit-tracks — `grid-template-areas-create-implicit-tracks-when-track-lists-are-omitted` — grid-template-areas alone should define a two-column/two-row explicit grid whose unsized tracks use auto implicit sizing; catches deriving placement from areas but not deriving track widths passes fixtures that also set template columns.
  - PASS 0.00% grid-template-areas=dot-empty — `grid-template-areas-dot` — grid-template-areas with dot (.) null cells: area 'a' spans the first two cells of row 1, area 'b' the last two of row 2, leaving diagonal empty cells.
  - PASS 0.00% grid-template-areas=unequal-row-invalid — `grid-unequal-grid-template-areas-row-lengths-invalidate-declaration` — Rows with different numbers of cell tokens make grid-template-areas invalid, so grid-area names should not resolve; catches padding shorter rows to a rectangle passes valid-area and non-rectangular invalid tests but accepts a distinct invalid syntax.
- **grid-template-columns** — 100.00%
  - PASS 0.00% grid-template-columns=calc — `grid-calc-track-sizes-are-parsed-in-grid-templates` — calc(50px + 30px) should resolve to an 80px first column before the 1fr track; catches a parser that drops calc() tracks passes px/fr fixtures but changes the number and width of columns.
  - PASS 0.00% grid-template-columns=fit-content — `grid-fit-content-track` — fit-content(120px) clamps the first track while a 1fr track takes the remainder; catches implementations that approximate fit-content as auto.
  - PASS 0.73% grid-template-columns=min-content-max-content — `grid-intrinsic-track-keywords` — min-content and max-content columns size the same breakable text differently; catches engines that map both intrinsic keywords to auto.
  - PASS 0.96% grid-template-columns=minmax-auto-min — `grid-minmax-auto-minimum-honors-content` — A minmax(auto,1fr) column with a long unbreakable word should not shrink below the word's min-content contribution; catches coercing auto minimum to zero passes simple flexible minmax tests.
  - PASS 0.00% grid-template-columns=minmax-fixed-max — `grid-minmax-fixed-max-caps-the-track` — A minmax(60px, 100px) track next to 1fr in a 300px grid should cap at 100px, not flex wider; catches treating every minmax as a 1fr flexible track passes minmax(min,1fr) tests.
  - PASS 0.48% grid-template-columns=minmax-intrinsic — `grid-minmax-intrinsic-endpoints-are-distinct` — A minmax(min-content,max-content) track should size from intrinsic text constraints, not as a generic auto track; catches mapping intrinsic keywords to auto passes round-1 min-content/max-content tests only when not nested in minmax.
  - PASS 0.00% grid-template-columns=named-lines — `grid-named-lines-basic` — Named column lines [start] [mid] [end] referenced by grid-column to place overlapping cells.
  - PASS 0.00% grid-template-columns=repeat-auto-fill — `grid-repeat-auto-fill-count` — repeat(auto-fill, 80px) resolves the repetition count from a 500px grid and keeps five cells on one row; catches hard-coded three-repeat expansion.
  - PASS 0.34% grid-template-columns=repeat-auto-fit-collapse — `grid-repeat-auto-fit-collapse` — repeat(auto-fit, minmax(80px, 1fr)) collapses the unused repeated track and stretches four occupied columns; catches auto-fill or fixed-repeat behavior.
  - PASS 0.00% grid-template-columns=repeat-multi-track — `grid-repeat-expands-multi-track-patterns` — repeat(2, 40px 80px) should expand to four tracks alternating narrow and wide; catches a repeat parser that only handles one repeated track passes repeat(3,80px) fixtures.
  - PASS 0.46% grid-template-columns=auto-spanning-items — `grid-spanning-item-contributes-to-auto-columns` — A wide item spanning two auto columns should contribute to both tracks instead of leaving them at zero or only measuring single-span items; catches measuring only items with col_span == 1 passes simple auto-column fixtures.
  - PASS 0.00% grid-template-columns=auto — `grid-template-columns-auto` — Three columns sized 100px auto 100px so the auto track absorbs the leftover width.
  - PASS 0.00% grid-template-columns=fr-mix — `grid-template-columns-fr-mix` — Three columns sized 1fr 2fr 1fr so the middle track is twice the width of the side tracks.
  - PASS 0.00% grid-template-columns=minmax — `grid-template-columns-minmax` — Two flexible columns using minmax(80px,1fr) and minmax(120px,1fr) distributing remaining space.
  - PASS 0.00% grid-template-columns=percent — `grid-template-columns-percent` — Three columns sized 25% 50% 25% of a fixed-width grid container.
  - PASS 0.00% grid-template-columns=repeat — `grid-template-columns-repeat` — Four equal 80px columns produced by repeat(4, 80px).
- **grid-template-rows** — 100.00%
  - PASS 0.92% grid-template-rows=auto-intrinsic — `grid-auto-rows-use-wrapped-content-height` — An auto row containing wrapping text should grow to the full multi-line content height before the next row starts; catches measuring only one line of text passes empty or single-line auto row fixtures.
  - PASS 0.00% grid-template-rows=fr — `grid-fr-rows-divide-definite-grid-height` — Two 1fr rows in a 160px-tall grid with a 10px gap should each be 75px high; catches a row resolver that treats fr rows as auto/content still passes fixed-row fixtures.
  - PASS 0.00% grid-template-rows=percentage — `grid-percentage-rows-resolve-against-grid-height` — Percentage row tracks in a definite-height grid should resolve from the grid content-box height; catches a row resolver that only handles fixed lengths passes column percentage tests.
  - PASS 0.00% grid-template-rows=px — `grid-template-rows` — Single column with three explicit-px rows of 50px, 110px and 70px height.
- **justify-content** — 100.00%
  - PASS 0.00% justify-content=center — `grid-justify-content-centers-the-grid-tracks` — A grid narrower than its container should be centered along the inline axis by justify-content:center; catches ignoring grid content distribution passes item-level justify-items fixtures.
  - PASS 0.00% justify-content=space-between — `grid-justify-content-distributes-free-space-between-tracks` — space-between should put the first track at the left edge and the second at the right edge of a wider grid container; catches implementing only start/center or item alignment passes simpler alignment fixtures.
  - PASS 0.00% justify-content=safe-center-overflow — `grid-safe-alignment-falls-back-to-start-on-grid-overflow` — justify-content:safe center should behave as start when the grid tracks overflow the container; catches stripping safe and always centering passes fit-content safe-center tests but clips both sides on overflow.
- **justify-items** — 100.00%
  - PASS 0.00% justify-items=end — `grid-justify-items-end` — justify-items: end aligns narrow cells to the inline (right) end of each track.
- **order** — 100.00%
  - PASS 0.00% order=grid-auto-placement — `grid-order-auto-placement` — Grid auto-placement follows order-modified document order, yielding yellow-green-red instead of source order; catches engines that ignore order on grid items.
  - PASS 0.00% order=dense-grid-auto-placement — `grid-order-modified-placement-also-applies-to-dense-packing` — Dense auto-placement must process items in order-modified document order before deciding which holes to backfill; catches a simple order test can fail, but an implementation might sort painting only and still pack dense holes in source order.
- **place-content** — 100.00%
  - PASS 0.00% place-content=end-center — `grid-place-content-sets-block-and-inline-grid-distribution` — place-content:end center should set align-content:end and justify-content:center for the grid tracks; catches engines with place-items but no place-content pass item-alignment shorthand fixtures.
- **place-items** — 100.00%
  - PASS 0.00% place-items=center — `grid-place-items-center` — place-items: center centers smaller cells both horizontally and vertically inside larger tracks.
- **place-self** — 100.00%
  - PASS 0.00% place-self=end-center — `grid-place-self-overrides-both-item-alignment-axes` — A single item with place-self:end center should override container stretch and paint at block-end/inline-center within its cell; catches container-only alignment fixtures do not prove item-level place-self parsing and application.
- **subgrid** — 100.00%
  - PASS 0.00% subgrid=rows — `grid-subgrid-adopts-parent-row-tracks` — A nested grid spanning two parent rows should use the parent row sizes for its own children when rows are subgridded; catches round 1 covered subgrid columns only; an implementation could special-case columns and still fail rows.
  - PASS 0.00% subgrid=line-name-augmentation — `grid-subgrid-augments-and-exposes-line-names` — Line names declared after subgrid should be usable by grandchildren placed inside the subgrid; catches basic subgrid track adoption does not prove line-name augmentation or name-repeat behavior.
  - PASS 0.00% subgrid=columns — `grid-subgrid-columns` — grid-template-columns:subgrid makes a nested grid adopt parent column tracks; catches independent one-column nested-grid layout.
  - PASS 0.00% subgrid=inherited-gutters — `grid-subgrid-inherits-parent-gutters-on-subgridded-axis` — Children of a column subgrid should align to parent columns including the parent's column-gap; catches treating a subgrid as an independent grid with no inherited gap can still pass a no-gap subgrid fixture.
- **writing-mode** — 100.00%
  - PASS 0.00% writing-mode=vertical-rl-grid — `grid-vertical-writing-mode-changes-grid-physical-axes` — In vertical-rl writing mode, the inline axis is vertical, so grid rows and columns map to different physical directions than horizontal-tb; catches a horizontal-only grid algorithm passes all default writing-mode fixtures.
- **z-index** — 100.00%
  - PASS 0.00% z-index=static-grid-items — `grid-overlap-z-index` — Overlapping static grid items honor z-index, so an earlier higher-z-index inset item paints above a later lower-z-index item; catches source-order-only stacking.

### images-replaced — 97.06%
- **aspect-ratio** — 100.00%
  - PASS 0.00% aspect-ratio=fixed-width-derived-height — `img-aspect-ratio-box` — A box with width:200px and aspect-ratio:2/1, expected to derive a 100px height.
- **fragmentation** — 100.00%
  - PASS 0.00% fragmentation=monolithic-image-push — `img-monolithic-page-push` — A block image too tall for the remaining space after the filler is pushed WHOLE to page 2 (spec-correct class-A break before a monolithic box), not split or clipped. The multi-page harness asserts both pages.
  - PASS 0.27% fragmentation=monolithic-image-slice — `img-taller-than-page-sliced` — A block image taller than a full page content box is SLICED across 3 pages (css-break-3 §4.1: monolithic content taller than the fragmentainer slices at its edge). Each page embeds only its own vertical band of the source raster, not a full copy. The multi-page harness asserts all three pages.
- **image-orientation** — 100.00%
  - PASS 0.53% image-orientation=rotate-90deg-before-fit — `img-image-orientation-rotate` — image-orientation:90deg rotates the raster before object-fit:contain sizing; catches ignoring the orientation property.
- **image-rendering** — 100.00%
  - PASS 0.00% image-rendering=pixelated — `img-image-rendering-pixelated` — image-rendering:pixelated scales a 2x2 raster to a hard-edged 120px image; catches smoothing the scaled pixels.
- **img** — 100.00%
  - PASS 0.00% img=border-on-image — `img-border` — A replaced image with a 6px solid border framing it under border-box sizing.
  - PASS 0.00% img=data-uri-png-sized — `img-data-uri-intrinsic` — A data: URI PNG placed in an img with explicit 120x120 size, scaled up from its 4x4 intrinsic pixels.
  - PASS 0.00% img=height-auto-width-aspect — `img-height-scaled` — A 2:1 intrinsic image with height:90px and width:auto derives width:180px from the aspect ratio.
  - PASS 0.00% img=intrinsic-no-sizing — `img-intrinsic-natural` — An img with no width/height attribute and no CSS sizing renders at its intrinsic 8x4 px natural dimensions (default sizing algorithm).
  - PASS 0.00% img=max-width-aspect-recompute — `img-max-width-clamp` — max-width:120px clamps a 200px-wide 2:1 image and recomputes its height from the aspect ratio to 60px.
  - PASS 0.00% img=percentage-width — `img-percent-width` — width:50% on a 2:1 image resolves against the 200px containing block to 100px wide, deriving a 50px height.
  - PASS 0.00% img=explicit-width-height-stretch — `img-width-height-stretch` — A 1:1 intrinsic PNG given explicit width:200px and height:100px, stretching it non-uniformly.
  - PASS 0.00% img=width-auto-height-aspect — `img-width-scaled` — A 2:1 intrinsic PNG with width:180px and height:auto, preserving aspect ratio at 180x90.
  - PASS 0.05% img=svg-data-uri-source — `svg-as-img` — An SVG document referenced as the src of an img via a data: URI, drawn at 160x120.
  - PASS 0.00% img=svg-no-viewbox-inner-rect — `svg-img-inner-rect` — A no-viewBox SVG referenced as an img src is scaled into its display box; an inner rect at (40,40,200,140) scales with the viewport and stays centered with a blue margin rather than stretching to the image right/bottom edge.
  - PASS 0.00% img=svg-preserve-aspect-ratio-slice — `svg-img-preserve-aspect-ratio-slice` — An SVG image with preserveAspectRatio xMidYMid slice crops predictably in a square img slot; catches ignoring slice and fitting the whole SVG viewport.
- **inline-svg** — 100.00%
  - PASS 0.10% inline-svg=clip-path-circle — `svg-inline-clip` — Inline SVG rect clipped to a circle via a clipPath def, leaving a red disc.
  - PASS 0.10% inline-svg=gradient-clip-path — `svg-inline-gradient-clip-path` — An inline SVG gradient-filled rect clipped by a clipPath paints only inside the circle; catches supporting gradients or clip paths separately but not together.
  - PASS 0.00% inline-svg=linear-gradient-fill — `svg-inline-linear-gradient` — Inline SVG rect filled with a horizontal linearGradient from blue to red.
  - PASS 0.03% inline-svg=rect-circle-path-viewbox — `svg-inline-shapes` — Inline SVG with a rect, circle and triangle path under a 0 0 100 100 viewBox scaled to 200x200.
  - PASS 0.00% inline-svg=text-element — `svg-inline-text` — Inline SVG text element rendered in ParitySans at 36px over a light fill.
- **object-fit** — 100.00%
  - PASS 0.07% object-fit=contain — `img-object-fit-contain` — object-fit:contain letterboxes a 2:1 image inside a 160x160 box, leaving grey bands top and bottom.
  - PASS 0.20% object-fit=cover — `img-object-fit-cover` — object-fit:cover fills a 160x160 box with a 2:1 image, cropping its left and right edges.
  - PASS 0.00% object-fit=fill — `img-object-fit-fill` — object-fit:fill stretches a 2:1 image to completely fill a 160x160 box, distorting its aspect.
  - PASS 0.13% object-fit=none — `img-object-fit-none` — object-fit:none draws the image at its intrinsic pixel size centred within the 160x160 box.
  - PASS 0.13% object-fit=scale-down-none-wins — `img-object-fit-scale-down-none` — object-fit:scale-down on an 8x4 image smaller than the box picks none, drawing it at intrinsic size centred.
  - PASS 0.07% object-fit=scale-down-contain-wins — `img-object-fit-scale-down-shrink` — object-fit:scale-down on a 240x120 image larger than a 160x160 box behaves like contain, shrinking it to 160x80 centred.
- **object-position** — 85.71%
  - PASS 0.20% object-position=cover-default-50-50 — `img-object-fit-cover-default-center` — object-fit:cover with the default object-position 50% 50% splits the overflow evenly, showing the central strip of the image.
  - PASS 0.27% object-position=cover-left-top-crop — `img-object-fit-cover-position` — object-fit:cover with object-position:left top anchors the left edge of an overflowing 2:1 image, cropping its right half.
  - PASS 0.07% object-position=bottom-with-contain — `img-object-position` — object-position:bottom anchors a contained 2:1 image to the bottom edge of a 160x160 box.
  - FAIL 0.74% object-position=far-edge-length-cover — `img-object-position-far-edge-length` — object-position:right 20px bottom 10px offsets a cover-fitted image from far edges; catches parsing right 20px as plain right alignment.
  - PASS 0.35% object-position=far-edge-percent-cover — `img-object-position-far-edge-percent` — object-position:right 25% bottom 25% offsets a cover-fitted image from far edges; catches treating far-edge percentages as plain 25% 25% or default center.
  - PASS 0.13% object-position=length-values — `img-object-position-length` — object-position:10px 20px offsets a contained 2:1 image by absolute lengths from the box top-left.
  - PASS 0.07% object-position=percentage-values — `img-object-position-percent` — object-position:25% 75% positions a contained 2:1 image within the free space of a 160x160 box.

### inline-text — 87.76%
- **inline-block** — 100.00%
  - PASS 2.79% inline-block=baseline-alignment — `inline-text-inline-block-baseline` — Inline-block with content aligns its last-line baseline to the surrounding text baseline.
  - PASS 1.60% inline-block=baseline-multiline — `inline-text-inline-block-baseline-multiline` — Multi-line inline-block aligns its LAST line's baseline to the surrounding text baseline (CSS2 §10.8.1).
  - PASS 0.33% inline-block=shrink-to-fit — `inline-text-inline-block-shrink-to-fit` — Inline-blocks with no explicit width use shrink-to-fit sizing, hugging their content (CSS2 §10.3.9).
  - PASS 0.60% inline-block=multiple — `inline-text-multiple-inline-blocks` — Several inline-blocks flow left-to-right on one line, separated by inter-element whitespace.
- **letter-spacing** — 100.00%
  - PASS 0.00% letter-spacing=positive — `inline-text-letter-spacing` — letter-spacing:8px on monospace text widens advance between every glyph.
  - PASS 0.00% letter-spacing=negative — `inline-text-letter-spacing-negative` — Negative letter-spacing tightens the gap between glyphs (css-text-3 §8.2).
- **line-height** — 100.00%
  - PASS 4.61% line-height=length — `inline-text-line-height-length` — line-height:<length> sets a fixed line-box height (CSS2 §10.8).
  - PASS 3.36% line-height=numeric — `inline-text-line-height-numeric` — Three filled paragraphs with line-height:2; verifies inter-line spacing and line-box height.
  - PASS 3.43% line-height=unitless — `inline-text-line-height-unitless` — A unitless line-height:1 multiplies the font size for the used line-box height (CSS2 §10.8.1).
- **overflow-wrap** — 100.00%
  - PASS 2.86% overflow-wrap=break-word — `inline-text-overflow-wrap-break-word` — overflow-wrap:break-word breaks an over-long word so it fits the box (css-text-3 §5.4).
- **text-align** — 100.00%
  - PASS 0.00% text-align=center — `inline-text-text-align-center` — text-align:center horizontally centers a short line within the box content area.
  - PASS 6.87% text-align=justify — `inline-text-text-align-justify` — text-align:justify stretches inter-word spacing on wrapped non-last lines to both edges.
  - PASS 5.88% text-align=justify-multiline — `inline-text-text-align-justify-multiline` — text-align:justify stretches inter-word spacing so non-last lines reach both edges (css-text-3 §7.3).
  - PASS 0.00% text-align=right — `inline-text-text-align-right` — text-align:right pushes a short line to the right edge of the box content area.
- **text-decoration** — 100.00%
  - PASS 0.15% text-decoration=line-through — `inline-text-text-decoration-line-through` — text-decoration-line:line-through strikes a line through the text (css-text-decor-3 §2.1).
  - PASS 2.36% text-decoration=overline — `inline-text-text-decoration-overline` — text-decoration-line:overline draws a line above the text (css-text-decor-3 §2.1).
  - PASS 1.44% text-decoration=underline — `inline-text-text-decoration-underline` — text-decoration:underline with a colored 2px line under an inline span.
- **text-decoration-color** — 100.00%
  - PASS 1.07% text-decoration-color=distinct — `inline-text-text-decoration-color` — text-decoration-color sets a line colour distinct from the text colour (css-text-decor-3 §2.2). Aspirational: not yet supported.
- **text-decoration-line:underline overline** — 0.00%
  - FAIL 14.53% text-decoration-line:underline overline=text-decoration-multiple-lines — `inline-text-text-decoration-multiple-lines` — A span requests underline and overline at the same time. Catches: A single-keyword text-decoration parser passes underline and overline fixtures separately but drops the combined declaration.
- **text-decoration-style:wavy** — 0.00%
  - FAIL 18.31% text-decoration-style:wavy=text-decoration-wavy — `inline-text-text-decoration-wavy` — A large underlined word requests a red wavy underline instead of the default solid rule. Catches: Painting every decoration as a straight solid line passes simple underline tests but misses style geometry.
- **text-decoration-thickness:6px** — 0.00%
  - FAIL 9.16% text-decoration-thickness:6px=text-decoration-thickness-length — `inline-text-text-decoration-thickness-length` — A large underlined word sets a 6px decoration thickness, visibly thicker than the default underline. Catches: A renderer using a fixed font-size-relative stroke passes default underline tests but ignores author thickness.
- **text-indent** — 100.00%
  - PASS 5.92% text-indent=length — `inline-text-text-indent-length` — text-indent:<length> indents only the first formatted line (css-text-3 §8).
  - PASS 6.66% text-indent=percent-containing-block — `inline-text-text-indent-percent` — text-indent:50% indents only the first formatted line by half the containing block width; catches length-only text-indent parsing.
- **text-transform** — 100.00%
  - PASS 2.47% text-transform=capitalize — `inline-text-text-transform-capitalize` — text-transform:capitalize upper-cases the first letter of each word (css-text-3 §2.1).
  - PASS 2.17% text-transform=lowercase — `inline-text-text-transform-lowercase` — text-transform:lowercase maps each glyph to lower case (css-text-3 §2.1).
  - PASS 2.55% text-transform=uppercase — `inline-text-text-transform-uppercase` — text-transform:uppercase maps each glyph to upper case (css-text-3 §2.1).
- **text-underline-offset:10px** — 0.00%
  - FAIL 16.13% text-underline-offset:10px=text-underline-offset-length — `inline-text-text-underline-offset-length` — An underlined word requests a 10px underline offset, leaving a large gap below the baseline. Catches: A fixed underline-position renderer passes ordinary underline tests but cannot move the line.
- **vertical-align** — 88.89%
  - PASS 0.57% vertical-align=baseline — `inline-text-vertical-align-baseline` — Inline-block box vertical-align:baseline sitting on the text baseline between glyphs.
  - PASS 1.07% vertical-align=bottom — `inline-text-vertical-align-bottom` — Inline-block box vertical-align:bottom aligned to the bottom of the line box (CSS2 §10.8.1).
  - PASS 0.06% vertical-align=length — `inline-text-vertical-align-length` — vertical-align:18px shifts an inline-block above the surrounding baseline; catches keyword-only implementations that fall back to baseline.
  - PASS 1.35% vertical-align=middle — `inline-text-vertical-align-middle` — Inline-block box vertical-align:middle centered on the line x-height.
  - PASS 0.84% vertical-align=sub — `inline-text-vertical-align-sub` — Inline-block box vertical-align:sub lowered below the text baseline (CSS2 §10.8.1).
  - PASS 0.51% vertical-align=super — `inline-text-vertical-align-super` — Inline-block box vertical-align:super raised above the text baseline (CSS2 §10.8.1).
  - FAIL 7.76% vertical-align=text-bottom — `inline-text-vertical-align-text-bottom` — vertical-align:text-bottom aligns to the parent text bottom inside a tall line box; catches aliasing text-bottom to bottom.
  - PASS 0.90% vertical-align=text-top — `inline-text-vertical-align-text-top` — Inline-block box vertical-align:text-top aligned to the top of the parent text content.
  - PASS 0.59% vertical-align=top — `inline-text-vertical-align-top` — Inline-block box vertical-align:top aligned to the top of the line box.
- **vertical-align:<percentage>** — 0.00%
  - FAIL 10.56% vertical-align:<percentage>=vertical-align-percent — `inline-text-vertical-align-percent` — An inline-block square uses vertical-align:50% and shifts upward by half its own line-height. Catches: A keyword/length-only implementation ignores percentages while still passing keyword and length baseline tests.
- **white-space** — 100.00%
  - PASS 4.70% white-space=break-spaces — `inline-text-white-space-break-spaces` — white-space:break-spaces preserves spaces/newlines and soft-wraps after preserved spaces (css-text-3 §4.1.1).
  - PASS 5.67% white-space=normal — `inline-text-white-space-normal` — white-space:normal collapses runs of spaces/newlines to a single space and soft-wraps (css-text-3 §4.1.1).
  - PASS 5.44% white-space=nowrap — `inline-text-white-space-nowrap` — white-space:nowrap forces a single line, clipped by overflow:hidden in a narrow box.
  - PASS 0.76% white-space=pre — `inline-text-white-space-pre` — white-space:pre preserves runs of spaces and explicit newlines in monospace text.
  - PASS 3.15% white-space=pre-line — `inline-text-white-space-pre-line` — white-space:pre-line collapses spaces but preserves newlines as forced breaks (css-text-3 §4.1.1).
  - PASS 4.70% white-space=pre-wrap — `inline-text-white-space-pre-wrap` — white-space:pre-wrap preserves spaces while still wrapping at the box edge.
- **white-space:break-spaces trailing preserved spaces** — 100.00%
  - PASS 1.29% white-space:break-spaces trailing preserved spaces=break-spaces-trailing-wrap — `inline-text-break-spaces-trailing-wrap` — A narrow preserved-space line wraps after visible spaces before the next word instead of hanging them outside the line box. Catches: Treating break-spaces like pre-wrap passes basic break-spaces fixtures but lets trailing spaces hang and keeps the next word on the wrong line.
- **word-break** — 100.00%
  - PASS 2.79% word-break=break-all — `inline-text-word-break-break-all` — word-break:break-all allows a break between any two characters, filling each line (css-text-3 §5.1).
- **word-spacing** — 100.00%
  - PASS 0.00% word-spacing=positive — `inline-text-word-spacing` — word-spacing:20px widens the gap at space characters between words on a single line.
  - PASS 0.00% word-spacing=negative — `inline-text-word-spacing-negative` — Negative word-spacing narrows the gap at spaces between words (css-text-3 §8.1).
- **word-spacing:<percentage>** — 100.00%
  - PASS 1.11% word-spacing:<percentage>=word-spacing-percent — `inline-text-word-spacing-percent` — Two identical text rows compare normal spacing with word-spacing:200%, making every word gap visibly wider. Catches: A length-only word-spacing implementation ignores percentages and still passes px positive/negative spacing fixtures.
- **wrapping** — 100.00%
  - PASS 4.56% wrapping=normal — `inline-text-wrapping-normal` — Default soft wrapping of inline text across multiple lines inside a narrow fixed-width box.

### interactions — 100.00%
- **containing-block** — 100.00%
  - PASS 0.00% containing-block=padding-box — `interactions-positioning-absolute-x-box-model-padding` — Interaction: absolute child resolves top/left against the padding box of a padded positioned ancestor.
  - PASS 0.00% containing-block=absolute-in-flex — `interactions-positioning-absolute-x-flexbox-container` — Interaction: an absolutely positioned child of a flex container is out of flow and positions against the flex container's padding box while in-flow flex items are spaced by justify-content.
- **flex-wrap-x-gap** — 100.00%
  - PASS 0.05% flex-wrap-x-gap=row-and-column-gap-on-wrap — `flexbox-wrap-x-gap` — flex-wrap pushes the fifth item to a new row; gap must apply both between items in a row and between the wrapped rows, with no gap on outer edges.
- **flexbox-x-grid** — 100.00%
  - PASS 0.01% flexbox-x-grid=grid-in-flex-item — `flexbox-x-grid-nested` — A grid container (2x2 cells) nested inside a flex item; the flex layout sizes the item and the grid lays out its cells within it.
- **float** — 100.00%
  - PASS 0.00% float=with-margin-collapse — `interactions-positioning-float-x-margin-collapse` — Interaction: a left float beside in-flow blocks whose adjacent vertical margins collapse.
- **float-x-margin-collapse** — 100.00%
  - PASS 0.00% float-x-margin-collapse=float-beside-collapsing-blocks — `interactions-float-x-margin-collapse` — A left-floated box beside two stacked blocks whose adjacent vertical margins collapse to the larger value rather than summing.
- **grid-x-background-gradient** — 100.00%
  - PASS 0.54% grid-x-background-gradient=gradient-fill-in-cells — `grid-x-background-gradient` — Each cell of a 2x2 grid is filled with a horizontal linear gradient; grid sizing places the cells while the gradient paints across each cell box.
- **grid-x-flexbox** — 100.00%
  - PASS 0.00% grid-x-flexbox=flex-in-grid-cell — `grid-x-flexbox-nested` — A flex container (space-between row) nested inside the first grid cell; grid places the cells and the flex distributes three boxes inside one of them.
- **inline-block-x-vertical-align** — 100.00%
  - PASS 1.02% inline-block-x-vertical-align=baseline-vs-middle-beside-text — `inline-block-x-vertical-align` — Two inline-block boxes on a text line, one aligned to the baseline and one to the middle of the line box, sitting next to short text.
- **object-fit** — 100.00%
  - PASS 0.07% object-fit=cover-rounded-overflow-clip — `interactions-object-fit-cover-x-rounded-overflow` — Interaction: object-fit:cover image content is clipped by a rounded overflow:hidden ancestor; catches square image corners escaping the ancestor clip.
- **overflow** — 100.00%
  - PASS 0.00% overflow=transformed-descendant-clip — `interactions-overflow-hidden-x-transform-clip` — Interaction: overflow:hidden clips a child after transform:translate moves it partly outside the container; catches clipping before transforms or leaking transformed visual overflow.
  - PASS 0.05% overflow=hidden-with-border-radius — `interactions-positioning-overflow-hidden-x-border-radius` — Interaction: overflow:hidden combined with border-radius clips the overflowing child to the rounded corners.
- **overflow-hidden-x-border-radius** — 100.00%
  - PASS 0.38% overflow-hidden-x-border-radius=rounded-clip-of-child — `box-model-overflow-hidden-x-border-radius` — A rounded container with overflow:hidden clips a square filled child to its rounded corners, cutting away the child's corners.
- **percentage-height-x-nested-block** — 100.00%
  - PASS 0.00% percentage-height-x-nested-block=height-chain-resolution — `block-percentage-height-x-nested-containers` — A definite outer height propagates so a chain of percentage-height nested blocks each resolve against their parent (240 -> 120 -> 60).
- **position** — 100.00%
  - PASS 0.00% position=percent-insets-transformed-containing-block — `interactions-absolute-percent-insets-x-transformed-cb` — Interaction: absolute percentage insets resolve inside a transformed containing block, not the page; catches wrong percentage bases or missing transform containing-block behavior.
  - PASS 0.00% position=absolute-rotated — `interactions-positioning-absolute-x-transforms-rotate` — Interaction: an absolutely positioned box that is also rotated; position resolves first, then transform rotates about its center.
  - PASS 0.00% position=fixed-translated — `interactions-positioning-fixed-x-transforms-translate` — Interaction: a fixed-position box that is then translated by a transform; position resolves against the page box, then translate shifts it.
  - PASS 0.00% position=relative-on-inline-block — `interactions-positioning-relative-x-inline-block` — Interaction: position:relative offset applied to a middle inline-block; its in-flow inline slot is preserved.
- **positioning-x-flexbox** — 100.00%
  - PASS 0.01% positioning-x-flexbox=absolute-child-in-flex — `positioning-absolute-x-flexbox` — An absolutely-positioned child pinned to a corner of a relatively-positioned flex container, taken out of flow while the flex items lay out normally.
- **positioning-x-grid** — 100.00%
  - PASS 0.58% positioning-x-grid=absolute-child-in-grid — `positioning-absolute-x-grid` — An absolutely-positioned child offset from a corner of a relatively-positioned grid container, removed from grid placement while the cells lay out.
- **supports** — 100.00%
  - PASS 0.00% supports=flow-root-query-and-layout — `interactions-supports-flow-root-containment` — Interaction: @supports(display:flow-root) must both query true and lay out flow-root float containment before the following red block; catches query-only support without layout.
- **tables-x-flexbox** — 100.00%
  - PASS 1.25% tables-x-flexbox=table-in-flex-item — `tables-x-flexbox-nested` — A bordered 2x2 table laid out inside a flex item; the flex layout sizes the item around the table's intrinsic grid.
- **tables-x-typography** — 100.00%
  - PASS 1.25% tables-x-typography=bold-header-cells — `tables-x-typography-bold` — A bordered table whose thead cells use bold typography; table cell layout coexists with the heavier glyph weight in the header row.
- **transform** — 100.00%
  - PASS 0.00% transform=transform-box-border-box-translate-percent — `interactions-transform-box-border-box-translate-percent` — Interaction: transform-box:border-box makes translateX(100%) resolve against the padded, bordered border box; catches content-box or parent-width percentage bases.
- **transforms-x-flexbox** — 100.00%
  - PASS 0.03% transforms-x-flexbox=rotate-on-flex-item — `transforms-rotate-x-flexbox-item` — A rotate transform on the middle flex item is purely visual and must not change flex layout; the third item keeps its untransformed slot.
- **z-index** — 100.00%
  - PASS 0.00% z-index=with-gradient — `interactions-positioning-z-index-x-backgrounds-gradient` — Interaction: z-index stacking where the upper positioned box carries a linear-gradient background over a lower solid box.
- **z-index-x-transforms** — 100.00%
  - PASS 0.00% z-index-x-transforms=stacking-with-transformed-box — `positioning-zindex-x-transforms-rotate` — Three overlapping positioned boxes where a rotated, transformed high-z box must paint on top of the lower-z plain positioned boxes.

### lists-counters — 85.71%
- **::marker font-size** — 0.00%
  - FAIL 15.51% ::marker font-size=marker-font-size — `lists-counters-marker-font-size` — A list styles only ::marker with a 36px font-size while item text remains 20px. Catches: A marker renderer that only recolors markers passes marker color tests but misses marker font metrics.
- **::marker{content:...}** — 100.00%
  - PASS 2.46% ::marker{content:...}=marker-content-counter — `lists-counters-marker-content-counter` — An ordered list replaces default decimal markers with a generated counter wrapped in square brackets through ::marker content. Catches: A ::marker color-only implementation passes marker styling tests but still emits default 1./2. markers.
- **<li value>** — 100.00%
  - PASS 2.12% <li value>=li-value — `lists-counters-li-value` — The middle li value='7' changes that marker and subsequent items continue from 8. Catches: An ordered-list context that only tracks a running index passes default and start fixtures but ignores per-item value overrides.
- **<ol reversed>** — 100.00%
  - PASS 1.90% <ol reversed>=ol-reversed — `lists-counters-ol-reversed` — A three-item ordered list with the reversed attribute should render 3, 2, 1 markers. Catches: An ol implementation that only handles start passes start fixtures but always counts upward.
- **@counter-style cyclic symbols** — 0.00%
  - FAIL 3.05% @counter-style cyclic symbols=counter-style-cyclic — `lists-counters-counter-style-cyclic` — A custom @counter-style named checks cycles through two literal symbols for list markers. Catches: Ignoring @counter-style passes built-in marker tests but falls back to disc for author-defined styles.
- **@counter-style extends decimal pad prefix suffix** — 0.00%
  - FAIL 3.86% @counter-style extends decimal pad prefix suffix=counter-style-pad-prefix-suffix — `lists-counters-counter-style-pad-prefix-suffix` — A custom counter style extends decimal with pad:2 '0', prefix '[' and suffix '] '. Catches: A decimal-only formatter passes decimal-leading-zero and roman fixtures but ignores custom style descriptors.
- **@counter-style negative descriptor** — 0.00%
  - FAIL 7.07% @counter-style negative descriptor=counter-style-negative — `lists-counters-counter-style-negative` — A generated counter starts at -2 and uses a custom negative descriptor to wrap negative values. Catches: A counter implementation that only formats positive decimal/roman cases passes current fixtures but mishandles negative custom styles.
- **counter(list-item)** — 100.00%
  - PASS 3.05% counter(list-item)=counter-list-item-after — `lists-counters-counter-list-item-after` — Each li appends generated text using counter(list-item), matching its ordered-list marker number. Catches: A renderer with an internal list index can paint markers but still resolves counter(list-item) to 0 in content.
- **counter-continuity** — 100.00%
  - PASS 2.22% counter-continuity=ol-across-page-break — `ol-continues-across-page-break` — An <ol> of ten 40px items spans two pages (CSS Lists 3 §4): counters resolve in document order, not visual layout, so page 2 markers read 6-10 (not a reset to 1). The multi-page harness asserts the page COUNT (2) and diffs both pages; a per-page counter reset would fail.
- **counter-increment:list-item 2** — 100.00%
  - PASS 1.55% counter-increment:list-item 2=list-item-increment-step — `lists-counters-list-item-increment-step` — An ordered list increments list-item by 2 so markers advance 2, 4, 6. Catches: A list renderer with hard-coded +1 indices passes normal lists but ignores author counter-increment on list-item.
- **counter-set** — 100.00%
  - PASS 2.61% counter-set=assign-existing-counter — `lists-counters-counter-set` — counter-set:item 7 assigns the existing counter so generated labels read 1, 7, 8; catches counter-reset/increment-only support.
- **counters** — 100.00%
  - PASS 1.19% counters=counter-style-argument — `counter-content-roman` — content:counter(x, upper-roman) rendering counter values in an explicit counter style.
  - PASS 2.37% counters=reset-increment-content — `counter-reset-increment` — counter-reset and counter-increment feeding content:counter() in ::before across three rows.
  - PASS 0.31% counters=counters-function-nested — `counters-nested` — Nested ordered lists numbered with content:counters(x, '.') producing 1, 2, 2.1, 2.2, 3.
- **list-style** — 100.00%
  - PASS 3.35% list-style=shorthand-type-position — `list-style-shorthand` — list-style shorthand setting lower-alpha type and inside position in one declaration.
- **list-style-image** — 100.00%
  - PASS 3.69% list-style-image=data-uri-png — `list-style-image-data-uri` — Unordered list using a data-URI PNG as the marker image (list-style-image:url()).
  - PASS 0.31% list-style-image=data-png-marker — `lists-counters-list-style-image-data-uri` — list-style-image:url(data:png) replaces disc bullets with the intrinsic image marker; catches fallback to list-style-type markers.
- **list-style-position** — 100.00%
  - PASS 3.95% list-style-position=inside — `list-style-position-inside` — Ordered list with markers flowed inside the content box (list-style-position:inside).
  - PASS 4.44% list-style-position=outside — `list-style-position-outside` — Ordered list with markers hung outside the content box (list-style-position:outside).
- **list-style-type** — 100.00%
  - PASS 2.43% list-style-type=circle — `list-style-type-circle` — Unordered list rendering hollow circle markers on three short text items.
  - PASS 2.44% list-style-type=decimal — `list-style-type-decimal` — Ordered list rendering 1./2./3. decimal markers on three short text items.
  - PASS 2.87% list-style-type=decimal-leading-zero — `list-style-type-decimal-leading-zero` — Ordered list rendering 01./02./03. zero-padded decimal markers on three items.
  - PASS 2.58% list-style-type=disc — `list-style-type-disc` — Unordered list rendering filled disc markers on three short text items.
  - PASS 2.92% list-style-type=lower-alpha — `list-style-type-lower-alpha` — Ordered list rendering a./b./c. lower-alpha markers on three short text items.
  - PASS 2.52% list-style-type=lower-roman — `list-style-type-lower-roman` — Ordered list rendering i./ii./iii. lower-roman markers on three short text items.
  - PASS 2.91% list-style-type=none — `list-style-type-none` — Unordered list with markers suppressed via list-style-type:none.
  - PASS 2.73% list-style-type=square — `list-style-type-square` — Unordered list rendering filled square markers on three short text items.
  - PASS 2.98% list-style-type=upper-alpha — `list-style-type-upper-alpha` — Ordered list rendering A./B./C. upper-alpha markers on three short text items.
  - PASS 2.47% list-style-type=upper-roman — `list-style-type-upper-roman` — Ordered list rendering I./II./III. upper-roman markers on three short text items.
  - PASS 2.69% list-style-type=ol-default-decimal — `ol-decimal-markers` — An <ol> with no explicit list-style renders the default decimal markers (1./2./3.); a nested <ol> restarts its own decimal sequence (UA stylesheet ol { list-style-type: decimal }).
  - PASS 2.54% list-style-type=ol-start — `ol-start-attribute` — Ordered list whose markers begin at 5 via the <ol start> attribute (5./6./7.).
- **list-style-type:<string>** — 100.00%
  - PASS 2.08% list-style-type:<string>=list-style-type-string — `lists-counters-list-style-type-string` — A ul sets list-style-type:'-- ' so each marker is a literal two-dash string instead of a disc. Catches: A built-in-only list-style parser falls unknown types back to disc and passes disc/circle/square fixtures.
- **list-style-type:cjk-decimal** — 100.00%
  - PASS 2.42% list-style-type:cjk-decimal=list-style-type-cjk-decimal — `lists-counters-list-style-type-cjk-decimal` — An ordered list uses cjk-decimal markers, which render CJK decimal digits rather than western 1/2/3. Catches: An implementation limited to latin/roman built-ins passes existing list-style-type coverage but falls back to disc or decimal for international styles.
- **marker-pseudo** — 100.00%
  - PASS 4.24% marker-pseudo=color — `marker-pseudo-color` — Ordered list whose markers are recolored and bolded via the ::marker pseudo-element.
- **marker-side:match-parent** — 0.00%
  - FAIL 39.13% marker-side:match-parent=marker-side-match-parent — `lists-counters-marker-side-match-parent` — A vertical list sets marker-side:match-parent so markers use the parent's side rather than each list item's self side. Catches: A list renderer with fixed outside-left markers passes horizontal list fixtures but places markers on the wrong side in vertical writing flows.

### multicol — 100.00%
- **break-after** — 100.00%
  - PASS 0.73% break-after=column — `multicol-break-after-column` — break-after:column inside a fixed-height multicol container starts the following block at the top of the next column; catches multicol layout that only supports automatic fill or break-before:column.
  - PASS 0.00% break-after=column-outside-multicol-noop — `multicol-break-column-outside-noop` — break-after:column outside a column fragmentation context must not force a page break; catches implementations that alias all forced break values to page breaks.
- **break-before** — 100.00%
  - PASS 0.13% break-before=avoid-column — `multicol-break-before-avoid-column` — break-before:avoid-column keeps a heading with the following paragraph in the same column when possible; catches parsers that ignore avoid-column and split adjacent blocks across columns.
  - PASS 0.54% break-before=column — `multicol-break-before-column` — break-before:column inside a column-fill:auto multicol container must move the block to the top of the next column; catches multicol layout that ignores forced column break values.
- **break-inside** — 100.00%
  - PASS 0.00% break-inside=avoid — `multicol-break-inside-avoid` — Six fixed-height cards with break-inside: avoid flowed across two columns; each atomic card stays whole within a column.
  - PASS 0.00% break-inside=avoid-column — `multicol-break-inside-avoid-column` — break-inside:avoid-column pushes a straddling card whole to the next column; catches column fragmenters that geometrically slice every crossing block.
- **column-count** — 100.00%
  - PASS 5.28% column-count=text-flow — `multicol-column-count-text` — Four short ParitySans paragraphs balanced across a two-column container (column-count: 2); tests text fragmentation and column balancing.
  - PASS 0.00% column-count=three — `multicol-column-count-three` — Six fixed-height bordered blocks flowed into a three-column container (column-count: 3); tests column fragmentation into equal-width tracks.
  - PASS 0.00% column-count=count-and-width — `multicol-count-and-width` — column-count: 2 with column-width: 100px; count caps the used columns (max), width is the minimum, so two columns are used.
- **column-fill** — 100.00%
  - PASS 0.33% column-fill=auto — `multicol-column-fill-auto` — column-fill: auto with a definite height fills each column to the box height in turn, leaving the last column short.
  - PASS 0.37% column-fill=balance — `multicol-column-fill-balance` — column-fill: balance (explicit) distributes content into equal-height columns.
  - PASS 0.00% column-fill=balance-all — `multicol-column-fill-balance-all` — column-fill:balance-all balances every page fragment of a multicol flow, not only the final fragment; catches parsers that collapse balance-all to balance or auto.
- **column-fragmentation** — 100.00%
  - PASS 0.00% column-fragmentation=wide-gap-rule-page-break — `multicol-column-rule-page-break` — A two-column container with a wide column-gap and a centered column-rule spanning a page break: the gap and rule are reproduced identically in the fresh row of columns on page 2 (CSS Multicol L1 §2/§3). Twelve 50px blocks (six per page) span two pages. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.36% column-fragmentation=forced-overflow-columns — `multicol-forced-overflow-columns` — Forced break-after:column values can create an actual third overflow column beyond the two used columns; catches layouts that cap physical columns or ignore forced column breaks.
  - PASS 0.00% column-fragmentation=overflow-columns-auto-fill — `multicol-overflow-columns-auto` — A fixed-height two-column container with column-fill:auto creates additional overflow columns inline instead of vertical overflow; catches layouts that let the last used column absorb all remaining content.
  - PASS 0.00% column-fragmentation=page-break-flow — `multicol-page-break-flow` — An auto-height two-column container (column-count: 2; column-fill: auto) with twelve 50px blocks on a 150px page: the columns fill page 1, then continue as a fresh row of column boxes on page 2 (CSS Multicol L1 §2), rather than overflowing and being clipped. Multi-page reference (page-count gate).
  - PASS 0.00% column-fragmentation=three-cols-page-break — `multicol-three-cols-page-break` — A column-count:3, column-fill:auto container taller than the page: the three columns fill page 1, then content continues as a fresh row of three columns on page 2 (CSS Multicol L1 §2). Eighteen 40px blocks (nine per page) span two pages. The multi-page harness asserts the page COUNT (2) and diffs both pages.
- **column-gap** — 100.00%
  - PASS 0.30% column-gap=px — `multicol-column-gap` — Three-column container with an explicit 40px column-gap separating the tracks; tests inter-column spacing.
  - PASS 0.00% column-gap=normal — `multicol-column-gap-normal` — Three-column container with no explicit column-gap; the normal default (1em = font-size) separates the tracks.
  - PASS 0.24% column-gap=percentage — `multicol-column-gap-percentage` — column-gap:10% must resolve against the multicol container and narrow the three columns accordingly; catches parsers that store percentages but drop them during column layout.
- **column-progression** — 100.00%
  - PASS 0.00% column-progression=direction-rtl — `multicol-direction-rtl-columns` — direction:rtl places the first multicol column on the right side of the container; catches physical left-to-right column offset code.
  - PASS 1.93% column-progression=writing-mode-vertical-rl — `multicol-writing-mode-vertical-rl` — writing-mode:vertical-rl changes multicol line boxes and column progression instead of rendering ordinary horizontal left-to-right columns; catches multicol layout that ignores writing mode.
- **column-rule** — 100.00%
  - PASS 0.64% column-rule=solid — `multicol-column-rule` — Three-column container with a 6px solid column-rule painted in each gap, spanning the full content-box height.
  - PASS 0.00% column-rule=empty-adjacent-columns — `multicol-column-rule-empty-suppressed` — Column rules must not paint beside empty columns when only the first column has content; catches renderers that draw rules in every column gap whenever column-count is greater than one.
  - PASS 0.43% column-rule=longhands — `multicol-column-rule-longhands` — column-rule-width/style/color set as separate longhands (thick solid blue).
  - PASS 0.25% column-rule=wider-than-gap — `multicol-column-rule-wider-than-gap` — A column rule wider than the gap is centered in the gap and overlaps columns without widening the gutter; catches layouts that add rule width to column placement.
- **column-rule-style** — 100.00%
  - PASS 0.59% column-rule-style=dashed — `multicol-column-rule-dashed` — Three-column container with a 6px dashed column-rule painted in each gap.
  - PASS 0.57% column-rule-style=double — `multicol-column-rule-double` — Three-column container with a 9px double column-rule (two thin parallel lines) in each gap.
  - PASS 0.30% column-rule-style=none — `multicol-column-rule-none` — column-rule-style: none draws no rule and consumes no space; the gap matches a plain column-gap.
- **column-span** — 100.00%
  - PASS 0.27% column-span=all — `multicol-column-span-all` — A banner element with column-span: all spanning the full width above six blocks distributed into three columns.
- **column-width** — 100.00%
  - PASS 0.00% column-width=px — `multicol-column-width` — Container using column-width: 140px so the column count is derived from the available inline size.
  - PASS 0.00% column-width=wider-than-container — `multicol-column-width-wide` — column-width larger than the container width collapses to a single column.
- **columns** — 100.00%
  - PASS 0.00% columns=count-only — `multicol-columns-count-only` — columns: 4 shorthand with only a column-count; four equal-width tracks.
  - PASS 0.00% columns=shorthand — `multicol-columns-shorthand` — columns: 120px 3 shorthand setting both column-width and column-count at once.
  - PASS 0.00% columns=width-only — `multicol-columns-width-only` — columns: 140px shorthand with only a column-width; the used column count is derived from the inline size.

### overflow-clipping — 94.74%
- **line-clamp** — 100.00%
  - PASS 0.89% line-clamp=two-line-webkit-clamp — `overflow-line-clamp-two-lines` — -webkit-line-clamp:2 with vertical box layout clips static text to two lines before the red block; catches unsupported line-clamp rendering all lines.
- **overflow** — 93.33%
  - FAIL 0.74% overflow=visible-hidden-axis-coercion — `overflow-axis-visible-hidden-coercion` — overflow-x:visible plus overflow-y:hidden coerces the x axis to non-visible clipping; catches preserving visible overflow on the x axis.
  - PASS 0.00% overflow=clip — `overflow-clip` — overflow:clip clips the oversized child to the clip box with no scroll container (modern clip keyword).
  - PASS 0.00% overflow=clip-no-float-containment — `overflow-clip-float-no-bfc` — overflow:clip clips overflow but does not establish a new formatting context, so a following red block is not pushed below the float; catches treating clip like hidden for BFC creation.
  - PASS 0.05% overflow=hidden-with-border-radius — `overflow-hidden-border-radius` — overflow:hidden combined with border-radius clips the overflowing child to the rounded corners of the clip box.
  - PASS 0.00% overflow=hidden — `overflow-hidden-clip` — overflow:hidden clips an oversized in-flow child to the clip box; the child is cut off at the right and bottom edges.
  - PASS 0.00% overflow=hidden-on-flex-item — `overflow-hidden-flex-item` — A flex item with overflow:hidden clips its oversized inner block to the flex item box while the sibling item is unaffected.
  - PASS 0.00% overflow=hidden-on-grid-item — `overflow-hidden-grid-item` — A grid cell with overflow:hidden clips its oversized inner block to the cell box while the sibling cell is unaffected.
  - PASS 0.00% overflow=nested-clip-intersection — `overflow-hidden-nested` — Nested overflow:hidden boxes; the visible region is the intersection of the outer and inner clip rectangles, with the grandchild clipped by both.
  - PASS 0.00% overflow=hidden-clips-text — `overflow-hidden-text-clip` — overflow:hidden on a short fixed-height box clips overflowing text lines below the box edge (bundled ParitySans).
  - PASS 0.00% overflow=logical-inline-block-axes — `overflow-logical-inline-clip` — overflow-inline:clip and overflow-block:visible clip only inline-axis overflow in horizontal writing mode; catches ignoring logical overflow or clipping both axes.
  - PASS 0.00% overflow=padding-box-clip-edge — `overflow-padding-box-clip` — overflow:hidden on a padded box clips its oversized child at the PADDING box: the child fills the padding box out to the inner border edge and is cut there, leaving the border fully visible and the top/left padding band showing.
  - PASS 0.65% overflow=scroll — `overflow-scroll-print-clip` — overflow:scroll produces no scrollbars in print; the oversized child is clipped to the box edges with no interactive scroll affordance.
  - PASS 0.00% overflow=visible-x-clip-y — `overflow-visible-clip-axis` — overflow-x:visible with overflow-y:clip preserves horizontal overflow while clipping vertical overflow; catches single-rectangle clipping of both axes.
  - PASS 0.00% overflow=visible — `overflow-visible-no-clip` — overflow:visible (explicit) does not clip; the oversized child paints beyond the parent box on the right and bottom.
  - PASS 0.44% overflow=overflow-x-overflow-y — `overflow-x-y-separate` — overflow-x:hidden clips horizontally while overflow-y:visible lets the child overflow downward only.
- **overflow-clip-margin** — 100.00%
  - PASS 0.00% overflow-clip-margin=clip-edge-extension — `overflow-clip-margin` — overflow:clip with overflow-clip-margin:18px lets the child bleed past the box edge; catches clipping exactly at the padding box.
- **scrollbar-gutter** — 100.00%
  - PASS 0.00% scrollbar-gutter=stable-both-edges — `overflow-scrollbar-gutter-both-edges` — scrollbar-gutter:stable both-edges reserves symmetric inline gutters around the child; catches treating both-edges as plain stable or ignoring scrollbar-gutter entirely.
  - PASS 0.00% scrollbar-gutter=stable — `overflow-scrollbar-gutter-stable` — scrollbar-gutter:stable reserves inline gutter space in a non-overflowing auto scroll container; catches ignoring the gutter and letting the child fill the full red box.

### paged-media — 84.15%
- **bleed** — 100.00%
  - PASS 0.77% bleed=background-into-bleed — `paged-bleed-crop-background` — @page bleed with crop marks paints the page background into the bleed area; catches renderers that clip backgrounds to the trim page box.
- **break-after** — 100.00%
  - PASS 0.00% break-after=page-mid-document-real — `break-after-page-mid-document` — Modern break-after:page on the SECOND of three blocks forces the third block onto page 2 while the first two share page 1 — proving the modern break-after path fires mid-flow, not only on the first/last element. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% break-after=page-modern-real-pagination — `break-after-page-modern` — Modern CSS Fragmentation break-after:page on the first block forces the following content onto page 2 — a genuine TWO-page document. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.18% break-after=avoid-keep-with-next — `paged-break-after-avoid-keep-next` — break-after:avoid keeps a bottom-of-page heading with the following paragraph; catches implementations that parse avoid but treat it as a no-op for automatic page breaks.
  - PASS 0.00% break-after=left-from-left-page — `paged-break-after-left-from-left` — break-after:left on page 2, a left page, must insert a blank right page so following content starts on page 4; catches implementations that alias left to an ordinary page break.
  - PASS 0.00% break-after=right-parity-blank — `paged-break-after-right-blank` — break-after:right after a full first page must insert a blank left page so the next real block starts on a right page; catches implementations that alias right to an ordinary page break.
- **break-before** — 100.00%
  - PASS 0.00% break-before=page-modern-real-pagination — `break-before-page-modern-real` — Modern CSS Fragmentation break-before:page (no legacy fallback) on the second block forces it onto page 2 — a genuine TWO-page document. The multi-page harness asserts the page COUNT (2) and diffs both pages (page 1 = blue box, page 2 = green box). Proves the modern break-* family is now parsed and wired into the forced-break path.
  - PASS 0.18% break-before=avoid-page-keep-with-previous — `paged-break-before-avoid-page-keep-prev` — break-before:avoid-page keeps a heading and following paragraph together across a page boundary; catches paginators that ignore break-before avoidance during automatic breaks.
  - PASS 0.00% break-before=page-leading-noop — `paged-break-before-page-modern` — Modern break-before:page on the first block is a no-op (no preceding content): the forced break is suppressed at the document start, so a single page renders. The modern break-* family is now parsed and wired into the forced-break path (see break-before-page-modern-real for the real two-page case).
  - PASS 0.00% break-before=recto-leading-noop — `paged-break-before-recto-leading` — break-before:recto on the first in-flow block must not create a leading blank page; catches implementations that insert parity pages before suppressing leading breaks.
  - PASS 0.00% break-before=verso-left-page — `paged-break-before-verso-left` — break-before:verso after two full pages must skip the natural right page and land on the next left page; catches parsers that treat verso as page or only recognize left/right.
- **break-inside** — 100.00%
  - PASS 0.00% break-inside=avoid-figure-keep-together — `break-inside-avoid-figure` — break-inside:avoid on a <figure> (image + caption) that would straddle a boundary: a 200px spacer leaves too little room, so Chrome keeps the figure together and pushes it WHOLE to page 2 (image and caption stay on the same page). The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% break-inside=avoid-real-keep-together — `break-inside-avoid-real` — break-inside:avoid keeps a bordered card together across a page boundary: a tall spacer fills most of page 1, so the card would straddle the boundary and is pushed WHOLE onto page 2 (not split). The multi-page harness asserts the page COUNT (2): page 1 = spacer, page 2 = intact card.
  - PASS 0.00% break-inside=avoid — `paged-break-inside-avoid` — Modern break-inside:avoid keeps a card intact on a single page. Content already fits, so the rendered result equals an unbroken nested box. The modern break-inside property is now parsed and honored (see break-inside-avoid-real for the cross-page case).
  - PASS 0.00% break-inside=avoid-page-card — `paged-break-inside-avoid-page-card` — break-inside:avoid-page keeps a page-fitting card intact when it would straddle a page boundary; catches parsers that support avoid but not avoid-page.
- **break-precedence** — 100.00%
  - PASS 0.07% break-precedence=break-before-left-over-break-after-right — `paged-break-precedence-before-left-wins` — At a shared class-A break point, break-before:left on the following block must override break-after:right on the preceding block; catches independent-break implementations that insert a blank page for the earlier value.
  - PASS 0.00% break-precedence=break-before-over-break-after — `paged-break-precedence-before-wins` — At a shared class-A break point, break-before:right on the following block must win over break-after:left on the preceding block; catches paginators that emit independent breaks and suppress the later side request.
- **break-propagation** — 100.00%
  - PASS 0.00% break-propagation=last-child-break-after — `paged-child-break-after-propagates` — break-after:page on a wrapper's last in-flow child propagates after the wrapper; catches layouts that fragment the wrapper decoration onto the following page.
  - PASS 0.00% break-propagation=first-child-break-before — `paged-child-break-before-propagates` — break-before:page on a wrapper's first in-flow child propagates to before the wrapper; catches layouts that break inside the wrapper and leave wrapper decoration on the previous page.
- **flex-fragmentation** — 100.00%
  - PASS 0.14% flex-fragmentation=column-children-across-pages — `paged-flex-column-fragmentation` — A vertical flex container taller than one page should continue its child items on later pages; catches paginators that treat flex containers as unsplittable atoms.
- **footnote** — 14.29%
  - PASS 0.26% footnote=float-footnote — `footnote-float` — CSS GCPM float: footnote pulls a box into the page's footnote area with an auto-numbered call marker left in the body text. Validated against a WeasyPrint oracle because Chrome --print-to-pdf does not implement GCPM footnotes.
  - FAIL 2.36% footnote=counter-reset — `paged-footnote-counter-reset` — counter-reset:footnote changes generated footnote call and marker numbers; catches hard-coded sequential footnote markers starting at 1.
  - FAIL 4.58% footnote=display-compact — `paged-footnote-display-compact` — footnote-display:compact places fitting short footnotes inline in the footnote area; catches footnote implementations that always stack notes as blocks.
  - FAIL 100.00% footnote=area-max-height — `paged-footnote-max-height` — @footnote max-height constrains note placement; catches footnote extractors that ignore @footnote sizing and let notes consume arbitrary page height.
  - FAIL 5.64% footnote=policy-block — `paged-footnote-policy-block` — footnote-policy:block moves the whole owning paragraph with its footnote when the note cannot fit; catches auto-policy-only footnote placement.
  - FAIL 4.41% footnote=policy-line-display-inline — `paged-footnote-policy-inline` — footnote-policy:line with footnote-display:inline keeps the owning line with its notes and lays the note bodies inline; catches footnote support that only extracts block notes with auto policy.
  - FAIL 6.77% footnote=styling-and-pseudo-elements — `paged-footnote-styling-pseudos` — @footnote styling plus ::footnote-call and ::footnote-marker custom content affect footnote calls and markers; catches basic footnote extractors that keep default markers and no separator styling.
- **fragmentation** — 100.00%
  - PASS 3.05% fragmentation=multicol-span-all-page-break — `multicol-span-all-page-break` — A column-span: all heading inside a multi-column block interrupts the columns; column flow resumes and fragments across page boundaries.
  - PASS 4.89% fragmentation=table-row-slice — `table-row-taller-than-page` — A single table row whose cell is taller than the page content box slices its cell content across page fragments.
- **grid-fragmentation** — 100.00%
  - PASS 0.15% grid-fragmentation=rows-across-pages — `paged-grid-fragmentation-rows` — Rows in a one-column grid continue across page fragments instead of clipping as one atomic grid; catches paginators that lose lower grid rows while block/table fragmentation works.
- **marks** — 100.00%
  - PASS 0.52% marks=crop-cross — `paged-marks-crop-cross` — @page marks:crop cross emits printer crop and registration marks around the page; catches renderers that implement only page size, margin, and background.
- **named-page** — 100.00%
  - PASS 0.00% named-page=page-property-leading-noop — `paged-named-page` — The page property names an @page rule (page: cover) on the document's FIRST block, with no matching @page cover rule. Named pages are now parsed (CSS Paged Media 3 §3.4): the property forces a page break before the box, but a forced break at the very start of the fragmentation flow is suppressed, so a single page renders with the section on the default page — matching Chrome. The named-page MARGIN application is covered by paged-named-page-margin.
  - PASS 0.27% named-page=continuation-geometry — `paged-named-page-continuation` — A tall element assigned page:chapter keeps the chapter page size and margin on continuation pages; catches named-page implementations that apply geometry only to the first fragment.
  - PASS 0.34% named-page=page-margin-override — `paged-named-page-margin` — CSS Paged Media 3 §3.4 named pages: page: wide on a .cover block forces a page break and the page it starts (page 2) adopts the @page wide margin (0.3cm) instead of the default @page margin (1cm). ironpress now implements named-page MARGINS; Chrome --print-to-pdf honors them too (verified: page 1 content at 1cm, page 2 at 0.3cm). The multi-page harness asserts the page COUNT (2) and diffs both pages: page 1 box at the 1cm default margin, page 2 cover at the 0.3cm wide margin.
  - PASS 0.34% named-page=page-size-override — `paged-named-page-size` — CSS Paged Media 3 §3.4 named-page SIZE: @page big { size: A3 } selected by .big { page: big } makes page 2 physically A3-sized while page 1 keeps the default 400px by 300px size. The multi-page harness asserts the page COUNT (2) and the page 2 MediaBox dimensions.
- **orphans-widows** — 100.00%
  - PASS 2.52% orphans-widows=orphans-3 — `orphans-3` — orphans:3 requires at least three line boxes at the bottom of a fragment before a break. The five-line paragraph fragments across two pages; the break leaves 3 lines on page 1 and 2 on page 2 (Chrome). The harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% orphans-widows=orphans-4 — `orphans-4` — orphans:4 requires at least four line boxes left at the bottom of a fragment. A 100px spacer leaves only three line-slots on page 1, so the five-line paragraph cannot satisfy orphans:4 there and is pushed WHOLE to page 2 (Chrome); the initial orphans:2 would instead split it. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 2.61% orphans-widows=default-2-real-fragmentation — `orphans-widows-default` — A five-line paragraph taller than the @page content box (which fits four lines) is fragmented across two pages. With the initial orphans:2/widows:2, a naive greedy fill (4 lines on page 1, a single widow on page 2) is corrected so each fragment keeps >=2 lines: Chrome puts 3 lines on page 1 and 2 on page 2. The multi-page harness asserts the page COUNT (2) and diffs both pages — a real orphans/widows test, not a single-page no-op.
  - PASS 2.00% orphans-widows=single-page-noop — `paged-orphans-widows` — orphans:3/widows:3 on a paragraph whose lines all fit on one page have no visible effect; a single-page guard that the orphans/widows properties parse and never regress the no-break case (real cross-page fragmentation is covered by orphans-widows-default, orphans-3, widows-3, widows-4, orphans-4).
  - PASS 3.37% orphans-widows=widows-3 — `widows-3` — widows:3 requires at least three line boxes at the top of the next fragment after a break. The five-line paragraph fragments across two pages; the break moves earlier so the continuation starts with 3 lines, leaving 2 on page 1 (Chrome). The harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% orphans-widows=widows-4 — `widows-4` — widows:4 forces at least four line boxes onto the continuation page. An eight-line paragraph in a six-line page would naively split 6/2, but widows:4 moves the break two lines earlier to 4/4 (Chrome). The multi-page harness asserts the page COUNT (2) and diffs both pages.
- **page-background** — 100.00%
  - PASS 0.00% page-background=bleed-area-full-page — `at-page-background-bleed` — A background declared on @page paints the bleed area — the entire page box INCLUDING the @page margins (CSS Paged Media 3 §3.1). The 40px margin band is filled; a white bordered card sits inset in the content box.
  - PASS 0.00% page-background=canvas-confined-to-content — `root-background-content-box` — A propagated :root/canvas background is the document canvas, confined to the page content box — it does NOT paint the @page margin band (CSS Paged Media 3 §3.1). Same geometry as at-page-background-bleed, but the 40px margins stay white; the blue fills only inside them.
- **page-break-after** — 100.00%
  - PASS 0.00% page-break-after=always-trailing-noop — `paged-page-break-after-always-single` — page-break-after:always on the LAST in-flow block (nothing follows) must NOT emit a trailing blank page — Chrome renders one page, not two. The multi-page harness asserts the page COUNT (1) as well as page-1 content, which catches the trailing-blank-page bug the page-1-only comparison used to hide.
- **page-break-before** — 100.00%
  - PASS 0.00% page-break-before=always-mid-document-real — `paged-break-before-third-block` — Legacy page-break-before:always on the THIRD of three blocks moves it to page 2 while the first two share page 1 — proving the legacy forced break fires mid-flow. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% page-break-before=forced-break-real-pagination — `paged-forced-break-two-pages` — page-break-before:always puts the second box on page 2, so the document is genuinely TWO pages. The multi-page harness asserts the page COUNT (2) and diffs BOTH pages against Chrome (page 1 = blue box, page 2 = green box) — a real pagination test, not a single-page no-op.
  - PASS 0.00% page-break-before=avoid-noop — `paged-page-break-before-avoid-noop` — page-break-before:avoid on content that already fits on one page is a no-op; both blocks remain stacked. Tracks the legacy avoid-value gap.
- **page-break-inside** — 100.00%
  - PASS 0.00% page-break-inside=avoid-card-straddle-real — `page-break-inside-avoid-card-legacy` — Legacy page-break-inside:avoid on a bordered CARD that would straddle a boundary: a 220px spacer leaves too little room, so Chrome (and ironpress) push the whole card to page 2. Confirms the legacy alias is honored for block containers. The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.54% page-break-inside=avoid-table-straddle-real — `page-break-inside-avoid-table-straddle` — Legacy page-break-inside:avoid on a TABLE that would straddle a page boundary: a 150px spacer leaves too little room for the 160px table. Chrome keeps the table whole and pushes it to page 2; ironpress now honors table-level keep-together too (the avoid-inside table fits a full page, so the whole row run is moved to page 2 instead of split between rows, matching the block-level keep-together in break-inside-avoid-real). The multi-page harness asserts the page COUNT (2) and diffs both pages.
  - PASS 0.00% page-break-inside=avoid-table — `paged-page-break-inside-avoid-table` — page-break-inside:avoid on a table that already fits keeps the whole table on one page. Tracks the legacy page-break-inside gap with a geometry-deterministic table.
- **page-counter** — 50.00%
  - PASS 0.00% page-counter=counter-page-in-content — `paged-page-counter-content` — content: counter(page) used in NORMAL DOCUMENT FLOW (.pagenum::after) is an ordinary undefined counter that resolves to 0 in both Chrome and ironpress (the page/pages counters are special only inside @page margin boxes, which ironpress now supports — see the render_at_page_margin_box_counters_three_pages PDF-text test). This fixture locks in the in-flow 0-resolution parity with Chrome.
  - FAIL 0.30% page-counter=counter-increment-page-two — `paged-page-counter-increment-two` — @page counter-increment:page 2 changes counter(page) in margin boxes; catches renderers that substitute the physical one-based page index.
  - PASS 0.54% page-counter=counter-reset-page-seven — `paged-page-counter-reset-seven` — @page counter-reset:page 7 changes the origin of counter(page) in margin boxes; catches renderers that ignore page-context counter reset.
  - FAIL 0.51% page-counter=pages-counter-immutable — `paged-pages-counter-immutable` — counter-reset:pages in the page context must not alter counter(pages), which remains the total page count; catches over-eager page counter implementations.
- **page-margin** — 100.00%
  - PASS 0.00% page-margin=via-body-margin — `paged-body-margin-box` — Page margin exercised through an explicit 40px body margin (fixtures may not use @page); a filled, bordered content box is inset uniformly inside the single Letter page.
  - PASS 0.40% page-margin=percentage — `paged-page-margin-percentage` — @page margin:10% creates a visible inset from the page box; catches page margin parsers that accept only absolute lengths.
  - PASS 0.00% page-margin=three-value-shorthand — `paged-page-margin-three-values` — @page margin with three values resolves top, horizontal, and bottom margins; catches parsers that ignore this shorthand arity.
- **page-margin-box** — 71.43%
  - FAIL 17.92% page-margin-box=blank-over-right-specificity — `paged-blank-margin-box-specificity` — On a blank page inserted by break-after:left, @page :blank top-center content overrides @page :right; catches engines that ignore blank-page margin boxes or page selector specificity.
  - PASS 0.34% page-margin-box=top-left-corner-position — `paged-corner-margin-box-position` — @top-left-corner occupies the page corner while @top-left starts at the content edge; catches simplified top-band renderers that overlap or drop corner margin boxes.
  - PASS 0.64% page-margin-box=first-page-override — `paged-first-margin-box-override` — @page :first replaces the default top-center header on page one only; catches engines that collect margin boxes only from unselected @page rules.
  - PASS 0.42% page-margin-box=styling — `paged-margin-box-styling` — A top-center page margin box applies its own color, background, and font size; catches content-only margin box renderers with hard-coded text styling.
  - FAIL 0.74% page-margin-box=named-page-override — `paged-named-page-margin-box` — A block assigned page:chapter must use the chapter @page top-center margin box instead of the default one; catches engines that apply named-page geometry but collect only unqualified margin boxes.
  - PASS 0.70% page-margin-box=left-right-middle — `paged-side-margin-boxes` — @left-middle and @right-middle page-margin boxes must paint in the side margin bands; catches renderers that only implement top and bottom margin boxes.
  - PASS 0.64% page-margin-box=top-bottom-literal-content — `paged-top-bottom-margin-boxes` — Default @top-left, @top-center, @top-right, and @bottom-center page margin boxes render literal strings; catches visual parity gaps that only cover PDF-text margin box assertions.
- **page-orientation** — 100.00%
  - PASS 0.00% page-orientation=rotate-left — `paged-page-orientation-rotate-left` — @page page-orientation:rotate-left rotates the rendered page after layout; catches size-only implementations that leave portrait output upright.
- **page-selector** — 100.00%
  - PASS 0.00% page-selector=blank-background — `paged-blank-selector-background` — @page :blank must style the blank page inserted by a sided break; catches implementations that insert the parity page but leave it with the default page rule.
  - PASS 0.00% page-selector=first-plus-right-cascade — `paged-first-right-cascade` — The first page also matches :right, so declarations from @page :first and @page :right both apply; catches resolvers that pick only one pseudo-page rule.
  - PASS 0.00% page-selector=named-left-right — `paged-named-page-left-right` — Named page selectors combined with :left and :right apply spread-specific margins only to matching chapter pages; catches named-only matching that drops pseudo-page selectors.
  - PASS 0.00% page-selector=named-selector-list — `paged-page-selector-list-named` — A comma-separated @page selector list applies one margin rule to title and chapter named pages; catches selector classifiers that store the whole list as one page name.
- **page-selectors** — 100.00%
  - PASS 0.22% page-selectors=first-page-margin — `paged-first-page-margin` — CSS Paged Media 3 §3.3: @page :first sets a larger top margin on page 1 only; page 2 uses the default @page margin. Catches the bug where :first folded into the document-global margin and was wrongly applied to EVERY page (page 2 would sit at the 4cm margin too). The harness asserts the page COUNT (2) and diffs both pages — page 1 box at 4cm, page 2 box at 1cm.
  - PASS 0.17% page-selectors=spread-left-right-margins — `paged-spread-left-right-margins` — CSS Paged Media 3 §3.2 :left/:right spread margins (right page wide left margin, left page wide right margin). Chrome honors the spread pseudo-classes; ironpress now differentiates per spread too, tagging pages by parity (LTR page 1 = right) so each spread's content box shifts toward the gutter: page 1 (right) sits 3cm from the left, page 2 (left) sits at the 1cm left edge. The multi-page harness asserts the page COUNT (2) and diffs both pages.
- **page-size** — 100.00%
  - PASS 0.00% page-size=jis-b4-landscape — `paged-size-jis-b4-landscape` — @page size:jis-b4 landscape selects the named JIS B4 sheet and swaps axes; catches parsers missing the JIS keyword or orientation combination.
  - PASS 0.00% page-size=ledger-keyword — `paged-size-ledger-keyword` — @page size:ledger selects the 11in by 17in page box; catches page-size parsers that fall back after supporting only common sizes.
- **replaced-fragmentation** — 100.00%
  - PASS 0.00% replaced-fragmentation=bordered-tall-image — `paged-bordered-tall-image-fragment` — A bordered tall replaced image must continue its image content and border decoration across pages; catches splitters that only handle undecorated images.
  - PASS 0.00% replaced-fragmentation=tall-object-fit-fill-image — `paged-tall-image-object-fit-fill-fragment` — A tall object-fit:fill image must show continued colored bands across later pages; catches renderers that treat replaced content as monolithic and clip it after page one.
- **running-element** — 75.00%
  - PASS 0.18% running-element=element-first-except — `paged-element-first-except` — element(head, first-except) suppresses the running element on the page where it first appears but allows it later; catches parsers that only support element(name).
  - PASS 0.18% running-element=first-page-margin-box — `paged-running-first-margin-box` — position:running() referenced from @page :first appears in the first-page header only; catches running-element support that ignores selected page margin boxes.
  - FAIL 23.36% running-element=named-page-margin-box — `paged-running-named-page-margin-box` — position:running() referenced from a named @page chapter margin box appears only on chapter pages; catches named-page geometry support that discards named margin boxes.
  - PASS 0.21% running-element=position-running — `running-element-header` — CSS GCPM running elements: position: running(h) removes a box from normal flow, stores it in a named slot, and @top-center { content: element(h) } renders it as a running header in the page margin box. Validated against a WeasyPrint oracle because Chrome --print-to-pdf does not implement GCPM running elements.
- **running-header** — 0.00%
  - FAIL 5.39% running-header=string-last-element-last — `paged-gcpm-string-element-last` — string(section,last) and element(head,last) in page margin boxes must use the last heading on the page; catches GCPM implementations that ignore named strings or only support element(name)'s default selection.
  - FAIL 3.59% running-header=string-set-attr-start — `paged-string-set-attr-start` — string-set with attr() feeds string(section,start) in the page header; catches margin header renderers without named strings or attr() support.
- **table-fragmentation** — 100.00%
  - PASS 1.12% table-fragmentation=repeat-header-footer — `paged-table-header-footer-repeat` — A paginated table must repeat the thead and tfoot bands on each page fragment; integer-raster page geometry catches table splitting that fragments rows but forgets header/footer group repetition.

### positioning — 100.00%
- **clear** — 100.00%
  - PASS 0.00% clear=both — `positioning-clear-both` — clear:both pushes the cleared block below preceding floats instead of wrapping beside them.
- **containing-block** — 100.00%
  - PASS 0.00% containing-block=nearest-positioned-ancestor — `positioning-absolute-containing-block-relative-ancestor` — Absolute box resolves against the nearest positioned ancestor, skipping intermediate static parents.
  - PASS 0.00% containing-block=transform-establishes — `positioning-transform-establishes-containing-block` — A transform on a non-positioned ancestor makes it the containing block for an absolute descendant.
- **float** — 100.00%
  - PASS 0.00% float=left — `positioning-float-left` — float:left removes the box from flow and shifts it left; the following block wraps around its right side.
- **inset** — 100.00%
  - PASS 0.00% inset=absolute-percent-insets — `positioning-absolute-percent-insets` — Absolute top/right/bottom/left percentages resolve against the containing block and stretch the child to the centered half-size box; catches pixel interpretation or wrong percentage bases.
  - PASS 0.00% inset=negative-offset — `positioning-inset-negative-offset` — position:relative with negative top/left pulls the box up and left, overlapping prior flow.
  - PASS 0.00% inset=shorthand-stretch — `positioning-inset-shorthand-stretch` — The inset shorthand stretches an absolutely positioned box with inset:20px 30px 40px 50px; catches parsers that ignore the shorthand while longhands work.
  - PASS 0.00% inset=logical-insets-horizontal — `positioning-logical-insets-horizontal` — Logical inset longhands map to physical top/right/bottom/left in horizontal-tb writing mode; catches ignoring logical inset properties or mapping axes incorrectly.
  - PASS 0.00% inset=all-four-stretch — `positioning-position-absolute-stretch-inset` — All four insets set with auto size make an absolute box stretch to fill the containing block inset by 30px.
- **overflow** — 100.00%
  - PASS 0.00% overflow=hidden-clips-absolute — `positioning-overflow-hidden-absolute-child-clip` — overflow:hidden on a positioned ancestor clips an absolutely positioned descendant past the clip edges.
  - PASS 0.00% overflow=hidden — `positioning-overflow-hidden-clip` — overflow:hidden clips an oversized in-flow child to the parent box.
  - PASS 0.00% overflow=visible — `positioning-overflow-visible-no-clip` — overflow:visible (default) does not clip; the child overflows the parent box.
- **position** — 100.00%
  - PASS 0.00% position=fixed-repeats-paged-media — `positioning-fixed-repeats-pages` — position:fixed header repeats at the top of each printed page; catches treating fixed as a one-page absolute box.
  - PASS 0.00% position=fixed — `positioning-fixed-top-left` — position:fixed box placed by top/left relative to the page box on a single non-scrolling page.
  - PASS 0.00% position=absolute-bottom-right — `positioning-position-absolute-bottom-right` — position:absolute box anchored to bottom/right edges of its containing block.
  - PASS 0.00% position=absolute — `positioning-position-absolute-top-left` — position:absolute box placed by top/left within a relative containing block.
  - PASS 0.00% position=relative — `positioning-position-relative-offset` — position:relative shifts the box by top/left from its in-flow position; flow space is preserved.
  - PASS 0.00% position=static — `positioning-position-static` — position:static box ignores top/left offsets and stays in normal flow.
  - PASS 0.00% position=relative-percent-offset — `positioning-relative-percent-offset` — position:relative percentage top/left offsets resolve against the containing block and cover the red marker; catches own-size percentage bases or ignored relative percentages.
- **z-index** — 100.00%
  - PASS 0.00% z-index=negative-behind-flow — `positioning-z-index-negative-behind-flow` — A positioned z-index:-1 child in a local stacking context stays behind an overlapping in-flow sibling; catches painting all positioned descendants above normal flow.
  - PASS 0.00% z-index=source-order — `positioning-z-index-source-order-overlap` — Without z-index, positioned siblings stack in source order; the later box paints over the earlier.
  - PASS 0.00% z-index=stacking — `positioning-z-index-stacking` — Higher z-index paints on top of a later-in-source sibling with lower z-index.

### probes — 100.00%
- **background-color** — 100.00%
  - PASS 0.00% background-color=solid-paint — `probe-color-swatch` — Substrate probe: a pure background-color rectangle. Trust anchor for background-color paint.
- **block-flow** — 100.00%
  - PASS 0.00% block-flow=stacked-blocks — `probe-block-flow` — Substrate probe: two stacked solid blocks. Trust anchor for block box generation + normal vertical flow.
- **border** — 100.00%
  - PASS 0.00% border=thick-solid — `probe-border-box` — Substrate probe: one box with a thick solid border and transparent fill. Trust anchor for border paint.
- **box-sizing** — 100.00%
  - PASS 0.00% box-sizing=solid-fill-geometry — `probe-fill-box` — Substrate probe: one solid-filled box of known px size. Trust anchor for box-sizing + fill paint + page geometry.
- **font-metrics** — 100.00%
  - PASS 3.65% font-metrics=baseline — `probe-text-baseline` — Substrate probe: a short line of ParitySans text at a known size on a baseline rule. Trust anchor for font metrics + baseline placement. The text is correct; the residual is the irreducible cross-rasterizer glyph-edge AA floor (ironpress glyph rasterization vs Chrome FreeType) plus a ~1px baseline-rule offset, magnified here because the probe is a tiny text-dominated page (on a normal page the same floor is <1%). floor_pct admits that measured substrate floor (the FAIL bounds are unchanged, so a real regression beyond it still FAILs).
- **image** — 100.00%
  - PASS 0.00% image=data-uri-png — `probe-image-render` — Substrate probe: a small data:-URI PNG placed at a known size. Trust anchor for raster image decode + placement.

### selectors-cascade — 98.21%
- **at-rule** — 100.00%
  - PASS 0.00% at-rule=media-print — `selectors-cascade-media-print` — @media print { } applies in the print rendering context, turning the box green; ignoring print media queries leaves it gray.
  - PASS 0.00% at-rule=supports — `selectors-cascade-supports-rule` — @supports (display:block) { } gates a green override that a correct engine applies; engines without @supports parsing drop it and leave the box gray. Aspirational known-gap.
- **attribute-selector** — 100.00%
  - PASS 0.00% attribute-selector=case-insensitive — `selectors-cascade-attribute-case-insensitive` — [data-id="yes" i] case-insensitive flag colors YES and Yes green; a different value stays gray.
  - PASS 0.00% attribute-selector=equals — `selectors-cascade-attribute-equals` — [data-pick="yes"] colors only the box whose attribute equals the value; boxes with a different value or no attribute stay gray.
  - PASS 0.00% attribute-selector=hyphen — `selectors-cascade-attribute-hyphen` — [lang|="en"] hyphen match colors boxes whose value is en or begins with en-; the value english stays gray.
  - PASS 0.00% attribute-selector=compound — `selectors-cascade-attribute-multiple` — Two chained attribute selectors [data-a="1"][data-b="2"] require BOTH to match on the same box; only the box with both attributes turns green.
  - PASS 0.00% attribute-selector=prefix — `selectors-cascade-attribute-prefix` — [data-id^="abc"] prefix match colors boxes whose value starts with abc; a value containing but not starting with abc stays gray.
  - PASS 0.00% attribute-selector=presence — `selectors-cascade-attribute-presence` — [data-pick] presence selector matches any box carrying the attribute regardless of value; two boxes turn green, the bare box stays gray.
  - PASS 0.00% attribute-selector=substring — `selectors-cascade-attribute-substring` — [data-id*="mid"] substring match colors boxes whose value contains mid anywhere; an unrelated value stays gray.
  - PASS 0.00% attribute-selector=suffix — `selectors-cascade-attribute-suffix` — [data-id$="png"] suffix match colors boxes whose value ends with png; a value starting with png stays gray.
  - PASS 0.00% attribute-selector=whitespace-list — `selectors-cascade-attribute-word` — [data-tags~="hot"] whitespace-list match colors boxes where hot is a full space-separated word; a substring like hotter stays gray.
- **cascade** — 100.00%
  - PASS 0.00% cascade=all-initial-reset — `selectors-cascade-all-initial-reset` — all:initial resets earlier padding before later declarations rebuild the box; catches treating all as a no-op or failing to reset box properties.
  - PASS 0.00% cascade=important-before-normal — `selectors-cascade-important-before-normal` — A class !important rule placed BEFORE a normal id rule must override the higher-specificity id declaration; only the .winner box turns green.
  - PASS 0.00% cascade=important-over-id — `selectors-cascade-important-overrides-specificity` — A class rule with !important overrides an id selector that would otherwise win on specificity; only the !important box turns green.
  - PASS 0.00% cascade=inherit-keyword — `selectors-cascade-inherit-keyword` — background: inherit forces a non-inherited property to copy the parent .wrap green; the box renders green.
  - PASS 0.00% cascade=inheritance — `selectors-cascade-inherited-property-flows` — An inherited green background painted on the parent .wrap shows through a transparent child box.
  - PASS 0.00% cascade=initial-keyword — `selectors-cascade-initial-keyword` — background: initial resets the box background to its initial transparent value, revealing the green .wrap behind it.
  - PASS 0.00% cascade=inline-beats-selector — `selectors-cascade-inline-beats-selector` — An inline style attribute outranks any selector (id rule), so only the inline-styled box turns green.
  - PASS 0.00% cascade=non-inherited — `selectors-cascade-non-inherited-border` — A thick border on the parent .outer does NOT inherit to the child box, which keeps its own 2px border (child filled green for unambiguous geometry).
  - PASS 0.00% cascade=source-order-tiebreak — `selectors-cascade-source-order-tiebreak` — Two equal-specificity rules target the same element; the later declaration wins, so the box renders green not gray.
  - PASS 0.00% cascade=unset-keyword — `selectors-cascade-unset-keyword` — background: unset on a non-inherited property behaves like initial (transparent), revealing the green .wrap behind the box.
- **cascade-layers** — 100.00%
  - PASS 0.00% cascade-layers=layer-order-over-source-order — `selectors-cascade-layers-order` — @layer reset, base, theme gives the later-declared theme layer priority over a later source-order base rule; catches source-order-only cascade or dropped layer at-rules.
- **combinator** — 100.00%
  - PASS 0.00% combinator=adjacent-sibling — `selectors-cascade-adjacent-sibling` — The adjacent-sibling combinator (+) matches only the box immediately following .marker; the marker and the third box stay gray.
  - PASS 0.00% combinator=child — `selectors-cascade-child-combinator` — The child combinator (>) matches only immediate children of #parent; a nested .box stays gray while direct-child boxes turn green.
  - PASS 0.00% combinator=descendant — `selectors-cascade-descendant-combinator` — The descendant combinator (space) matches every .box inside #parent at any depth; a .box outside #parent stays gray.
  - PASS 0.00% combinator=general-sibling — `selectors-cascade-general-sibling` — The general-sibling combinator (~) matches every later .box sibling of .marker, not just the next one; the marker stays gray.
- **pseudo-class** — 100.00%
  - PASS 0.00% pseudo-class=defined — `selectors-cascade-defined` — :defined matches built-in HTML elements, turning the div green; catches missing Selectors Level 4 defined pseudo-class support.
  - PASS 0.00% pseudo-class=dir-rtl — `selectors-cascade-dir-rtl` — :dir(rtl) matches an element with static dir=rtl; catches missing direction pseudo-class support.
  - PASS 0.13% pseudo-class=empty — `selectors-cascade-empty` — :empty colors boxes with no children green; a box containing text stays gray.
  - PASS 0.00% pseudo-class=first-child-last-child — `selectors-cascade-first-last-child` — :first-child paints the first box green and :last-child paints the last box blue; the middle box stays gray.
  - PASS 0.00% pseudo-class=first-of-type — `selectors-cascade-first-of-type` — p:first-of-type colors the first <p> green even though a <div> precedes it.
  - PASS 0.00% pseudo-class=has — `selectors-cascade-has-relational` — :has(+ .flag) colors a box that is immediately followed by a .flag sibling green.
  - PASS 0.00% pseudo-class=is — `selectors-cascade-is-selector` — :is(.a, .c) colors boxes carrying class a OR c green; the .b box stays gray.
  - PASS 0.00% pseudo-class=lang-inherited — `selectors-cascade-lang-inherited` — :lang(en) matches an element inheriting the document language from html lang=en; catches checking only the element's own lang attribute.
  - PASS 0.00% pseudo-class=last-of-type — `selectors-cascade-last-of-type` — p:last-of-type colors the last <p> green; a trailing <div> does not affect it.
  - PASS 0.00% pseudo-class=not-list — `selectors-cascade-not-list` — :not(.a, .b) with a selector list colors every box except those carrying class a OR b.
  - PASS 0.00% pseudo-class=not-negation — `selectors-cascade-not-negation` — :not(.skip) colors every .box except the one carrying .skip; the skipped box stays gray.
  - PASS 0.00% pseudo-class=nth-child-even — `selectors-cascade-nth-child-even` — :nth-child(even) colors the 2nd and 4th of four boxes green.
  - PASS 0.00% pseudo-class=nth-child-formula — `selectors-cascade-nth-child-formula` — :nth-child(3n+1) colors the 1st and 4th of five boxes green; the others stay gray.
  - PASS 0.00% pseudo-class=nth-child-odd — `selectors-cascade-nth-child-odd` — :nth-child(odd) colors the 1st, 3rd and 5th of five boxes green; even-position boxes stay gray.
  - PASS 0.00% pseudo-class=nth-last-child — `selectors-cascade-nth-last-child` — :nth-last-child(2) colors the box that is 2nd from the end green (3rd of four).
  - PASS 0.00% pseudo-class=nth-last-of-type — `selectors-cascade-nth-last-of-type` — :nth-last-of-type(2) counts from the end among same element names, ignoring the intervening div; catches using nth-last-child or forward nth-of-type semantics.
  - PASS 0.00% pseudo-class=nth-of-type — `selectors-cascade-nth-of-type` — p:nth-of-type(2) colors the 2nd <p> among mixed div/p siblings green.
  - PASS 0.00% pseudo-class=only-child — `selectors-cascade-only-child` — :only-child colors a box green only when it is the sole child of its panel; a box with a sibling stays gray.
  - PASS 0.00% pseudo-class=only-of-type — `selectors-cascade-only-of-type` — p:only-of-type colors the sole <p> among <div> siblings green.
  - PASS 0.00% pseudo-class=root — `selectors-cascade-root-element` — :root paints the page background green behind a centered white bordered panel; if :root is unmatched the page stays white.
  - PASS 0.00% pseudo-class=scope-root — `selectors-cascade-scope-root` — :scope in a stylesheet matches the document scope root so :scope body .box turns the box green; catches unsupported :scope selector matching.
  - PASS 0.00% pseudo-class=where — `selectors-cascade-where-zero-specificity` — :where() contributes zero specificity, so a later (0,1,0) class rule wins over an earlier :where(.box) rule; the box renders green.
- **selectors-4** — 50.00%
  - PASS 0.00% selectors-4=has-descendant — `selectors-cascade-has-descendant` — :has(.flag) matches a descendant witness nested below the card; catches :has implementations limited to sibling or child relative selectors.
  - FAIL 8.27% selectors-4=supports-has-nth-child-of — `selectors-has-nth-of-supports` — @supports selector(), child :has(> .flag), and :nth-child(2 of .flagged) make the intended boxes green; catches sibling-only :has and nth-child without the of-selector clause.
- **specificity** — 100.00%
  - PASS 0.00% specificity=class-over-type — `selectors-cascade-specificity-class-over-type` — A class selector (0,1,0) overrides a type selector (0,0,1) regardless of source order; only the .pick box turns green.
  - PASS 0.00% specificity=id-before-class — `selectors-cascade-specificity-id-before-class` — An id rule (1,0,0) placed BEFORE a class rule (0,1,0) in source order must still win on specificity; only the #pick box turns green (regression guard against pure source-order cascade).
  - PASS 0.00% specificity=id-over-class — `selectors-cascade-specificity-id-over-class` — An id selector (1,0,0) overrides a more numerous class selector (0,2,0) on the same element; only the box carrying the id turns green.
- **supports** — 100.00%
  - PASS 0.00% supports=boolean-and-not — `selectors-cascade-supports-boolean-logic` — @supports boolean logic with and/not selects the green branch; catches parsers that handle only a single simple feature query.
- **type-selector** — 100.00%
  - PASS 0.00% type-selector=type — `selectors-cascade-type-selector` — A type selector p.box colors only <p> boxes green; <div> boxes stay gray.
  - PASS 0.00% type-selector=universal — `selectors-cascade-universal-selector` — The universal selector * as the discriminating rule paints every child box green.

### tables — 96.39%
- **anonymous-table-fixup** — 100.00%
  - PASS 0.00% anonymous-table-fixup=non-cell-row-child — `tables-anonymous-cell-in-row` — A CSS table-row contains one explicit table-cell and one plain div; the div should be wrapped in an anonymous table-cell and appear as the second cell. Catches wrong implementations where a renderer filters row children to explicit table-cell boxes and silently drops the green div.
  - PASS 0.00% anonymous-table-fixup=non-row-row-group-child — `tables-anonymous-row-group-child` — A CSS table-row-group contains a plain div before a real table-row; the div should become an anonymous row with one cell. Catches wrong implementations where a renderer that only collects explicit table-row children from row groups drops the blue anonymous row.
  - PASS 0.00% anonymous-table-fixup=orphan-table-cell-siblings — `tables-anonymous-table-cell-siblings` — Two inline siblings are display:table-cell without an explicit table or row; fixup creates an anonymous table and row. Catches wrong implementations where a renderer without fixup treats them as normal inline/block boxes and never applies table-cell equal-row layout.
  - PASS 0.00% anonymous-table-fixup=whitespace-between-rows — `tables-whitespace-row-fixup` — Whitespace text appears between two display:table-row boxes inside a CSS display table. Catches wrong implementations where a naive fixup implementation wraps whitespace into an anonymous row/cell and creates an extra blank band.
- **background-layering** — 100.00%
  - PASS 0.00% background-layering=column-group-background — `tables-colgroup-background` — A colgroup with span=2 has a blue background under two transparent columns and a third column remains yellow table background. Catches wrong implementations where a renderer that ignores colgroup backgrounds shows all three columns as yellow or only applies widths.
  - PASS 0.00% background-layering=column-background — `tables-column-background` — Two col elements provide red and green backgrounds while all cells are transparent. Catches wrong implementations where a width-only col implementation passes col sizing tests but leaves transparent cells showing the table background.
  - PASS 0.00% background-layering=row-group-background — `tables-row-group-background` — A tbody has a blue background, cells are transparent, and the table background is yellow. Catches wrong implementations where a renderer that only falls back to direct row backgrounds shows yellow table background through the cells instead of the tbody layer.
  - PASS 0.00% background-layering=row-over-column — `tables-row-over-column-background` — A red column background and a green row background overlap under transparent cells; row layer should paint above column layer. Catches wrong implementations where a renderer that implements only columns or paints layers in the wrong order shows red where the row should be green.
- **border-collapse** — 88.89%
  - PASS 0.02% border-collapse=collapse — `tables-border-collapse` — border-collapse:collapse merges adjacent 4px cell borders into shared single edges.
  - PASS 0.00% border-collapse=hidden-conflict — `tables-border-hidden-conflict` — Collapsed-border conflict where a hidden right border must suppress the neighboring solid left border; catches engines that parse hidden as none or paint both cell edges.
  - PASS 0.00% border-collapse=separate — `tables-border-separate` — border-collapse:separate with zero spacing keeps each cell's 4px border doubled at shared edges.
  - PASS 0.00% border-collapse=style-conflict — `tables-border-style-conflict` — Collapsed-border conflict where equal-width double beats solid on the shared edge; catches renderers that overpaint the later solid edge.
  - PASS 0.00% border-collapse=width-conflict — `tables-border-width-conflict` — Collapsed-border conflict where the wider blue shared border wins over a narrower red neighbor; catches DOM-order overpainting instead of width precedence.
  - PASS 0.00% border-collapse=cell-over-row-origin — `tables-collapse-cell-over-row` — A row and its cells declare equal-width collapsed top borders with different colors, so cell-origin precedence should choose the blue cell border. Catches wrong implementations where a renderer without origin precedence paints the red row border or paints both.
  - PASS 0.00% border-collapse=directional-tie-break — `tables-collapse-directional-tie` — Equal-width equal-style borders of different colors meet at a collapsed shared vertical edge; the directional tie-break determines the winner. Catches wrong implementations where a renderer that uses DOM paint order can pass width/style conflict tests but choose the opposite color in exact ties.
  - PASS 0.00% border-collapse=none-loses-to-solid — `tables-collapse-none-loses` — The left cell's right border is none and the right cell's left border is a thick red solid border on the same collapsed edge. Catches wrong implementations where a renderer that treats none as an instruction to erase the shared edge or paints only the first cell edge will omit the red border.
  - FAIL 7.84% border-collapse=outer-table-border-conflict — `tables-collapse-outer-table-border` — The table has a thick red collapsed border while the outer cells have thinner blue borders on the same outer grid edges. Catches wrong implementations where an implementation that paints the table border box independently or samples only cell borders shows the wrong outer color/thickness.
- **border-spacing** — 100.00%
  - PASS 0.00% border-spacing=uniform-12px — `tables-border-spacing` — border-collapse:separate with 12px border-spacing exposes the table background between cells.
  - PASS 0.00% border-spacing=two-value — `tables-border-spacing-two-value` — border-spacing:24px 6px applies distinct horizontal and vertical gaps between cells.
  - PASS 0.00% border-spacing=zero — `tables-border-spacing-zero` — border-spacing:0 in separate mode makes cells touch with their own (non-merged) borders.
  - PASS 0.05% border-spacing=ignored-when-collapsed — `tables-collapse-ignores-spacing` — A collapsed-border table ignores a large border-spacing value; catches combined-case engines that apply spacing despite border-collapse:collapse.
- **caption** — 100.00%
  - PASS 0.01% caption=caption-side-top — `tables-caption` — A top-side caption renders as a full-width banner above the table body.
  - PASS 0.16% caption=caption-min-width-auto-layout — `tables-caption-min-width` — A shrink-to-fit auto table has tiny cells but a wide nowrap caption that should expand the table wrapper to the caption minimum width. Catches wrong implementations where a renderer that sizes the table only from cell columns wraps or overflows the caption above a too-narrow two-cell table.
  - PASS 0.00% caption=caption-side-bottom — `tables-caption-side-bottom` — caption-side:bottom renders the caption banner below the table body instead of above.
  - PASS 1.60% caption=caption-side-on-caption — `tables-caption-side-on-caption` — The table has no caption-side declaration, but the caption element itself sets caption-side:bottom. Catches wrong implementations where a renderer that only reads caption-side from the table style will place this caption above, while table-level bottom fixtures still pass.
  - PASS 0.55% caption=multiple-caption-boxes — `tables-multiple-captions` — Two CSS table-caption boxes on the same CSS table use distinct colors and should both participate in the table wrapper before the rows. Catches wrong implementations where an implementation that stores only the first caption passes single-caption fixtures but drops the second caption entirely.
- **cell-padding** — 100.00%
  - PASS 0.01% cell-padding=padding-20px — `tables-cell-padding` — 20px cell padding insets a filled inner box from the cell border on all sides.
- **colspan** — 100.00%
  - PASS 0.34% colspan=colspan-2 — `tables-colspan` — A header cell with colspan=2 spans two fixed columns above a normal three-cell row.
- **column-width** — 100.00%
  - PASS 0.00% column-width=col-span-fixed — `tables-colgroup-span-fixed` — Fixed table layout applies a col span=2 width to two equal columns before a wider third column; catches engines that ignore col span width assignment.
  - PASS 0.00% column-width=colgroup-span-width — `tables-colgroup-span-width` — A childless colgroup span=2 with width:50px should create two 50px columns followed by a 100px column. Catches wrong implementations where a renderer that supports only col child widths passes existing colgroup tests but ignores bare colgroup spans.
  - PASS 0.30% column-width=percent — `tables-width-percent-columns` — Fixed-layout columns at 25%/50%/25% of a 400px table resolve to proportional widths.
- **direction** — 100.00%
  - PASS 0.00% direction=rtl-column-order — `tables-direction-rtl-columns` — A direction:rtl table has red first cell and green second cell; the first column should be visually on the right. Catches wrong implementations where a left-to-right cell placer shows red on the left even though text bidi support may work elsewhere.
- **display** — 80.00%
  - PASS 0.00% display=css-table-roles — `tables-css-display-table` — display:table/table-row/table-cell on divs lays out side-by-side cells with separated spacing; catches tag-only table layout that stacks CSS table boxes.
  - PASS 0.00% display=css-row-group-reorder — `tables-css-row-groups-reorder` — A div-based CSS table declares footer before body and header last; table display semantics put the header first and footer last. Catches wrong implementations where a renderer that only supports table-row/table-cell or follows DOM block order will show red footer first or stack all divs.
  - PASS 0.33% display=css-table-caption — `tables-css-table-caption` — A non-caption div inside a CSS table is made display:table-caption and should become a full-width caption above the row grid. Catches wrong implementations where a renderer that only recognizes the HTML caption tag ignores the div caption or lays it as a normal block inside the table.
  - PASS 0.00% display=css-table-column-widths — `tables-css-table-columns-fixed` — A CSS display table uses non-col elements as table-column boxes to make a narrow red column and wide green column. Catches wrong implementations where a renderer without CSS table-column support sizes both cells equally or treats the column divs as visible blocks.
  - FAIL 47.35% display=inline-table-inline-flow — `tables-inline-table-inline-flow` — Text before and after an inline-table should remain on the same line while the inline-table internally lays out two cells side by side. Catches wrong implementations where a renderer that treats inline-table as inline text or block flow will either drop the table grid or force the trailing text to a new line.
- **empty-cells** — 100.00%
  - PASS 0.10% empty-cells=ignored-in-collapse — `tables-empty-cells-collapse-ignored` — A collapsed-border table sets empty-cells:hide on an empty red cell next to a filled green cell. Catches wrong implementations where a renderer that applies empty-cells in collapsed mode suppresses the empty cell background/border, even though existing separated-mode hide tests pass.
  - PASS 0.44% empty-cells=visibility-hidden — `tables-empty-cells-hidden-content` — empty-cells:hide treats visibility:hidden cell content as no visible content and removes an all-empty row; catches DOM-presence emptiness tests.
  - PASS 0.00% empty-cells=hide — `tables-empty-cells-hide` — empty-cells:hide suppresses borders and backgrounds of cells with no content in a separated table.
  - PASS 0.00% empty-cells=whitespace-and-empty — `tables-empty-cells-whitespace` — empty-cells:hide treats whitespace-only and fully-empty cells as empty; only the cell with real text paints.
- **html-table-attributes** — 75.00%
  - FAIL 3.07% html-table-attributes=border — `tables-border-attribute` — A table uses border=4 without CSS borders, so browser presentational hints draw visible table/cell borders. Catches wrong implementations where a CSS-only renderer ignores the attribute and paints only the cell backgrounds.
  - PASS 0.00% html-table-attributes=cellpadding — `tables-cellpadding-attribute` — A legacy table uses cellpadding=16 with no CSS padding; an inner marker should be inset from the cell border. Catches wrong implementations where a CSS-only implementation ignores cellpadding and places the marker against the border.
  - PASS 0.00% html-table-attributes=cellspacing — `tables-cellspacing-attribute` — A legacy table uses cellspacing=14 with no CSS border-spacing and a dark table background. Catches wrong implementations where a CSS-only implementation renders adjacent cells without the dark spacing gutters.
  - PASS 0.00% html-table-attributes=colspan-max-clamp — `tables-colspan-max-clamp` — A cell declares colspan=1001 before a normal cell; browsers clamp overly large colspan values to the HTML maximum rather than creating an unbounded grid. Catches wrong implementations where a renderer that accepts the raw integer creates a huge table width or drops later cells, while ordinary small colspan tests pass.
- **row-height** — 100.00%
  - PASS 0.00% row-height=row-min-height — `tables-row-height-minimum` — A tr declares height:80px while its cells contain only a 20px marker. Catches wrong implementations where a renderer that derives row height only from cell content paints a short row, while ordinary cell-height tests still pass.
- **rowspan** — 100.00%
  - PASS 0.01% rowspan=rowspan-2 — `tables-rowspan` — A first-column cell with rowspan=2 spans both rows while later columns stay single-row.
  - PASS 0.00% rowspan=extra-columns-from-occupancy — `tables-rowspan-extra-columns` — The first row has a rowspan in column one plus one normal cell; the second row has two new cells, so the grid needs three columns. Catches wrong implementations where a renderer that determines column count from authored cells per row caps the table at two columns and drops or overlays the second-row trailing cell.
  - PASS 0.00% rowspan=height-distribution — `tables-rowspan-height-distribution` — A cell with rowspan=2 and height:100px spans two otherwise short rows. Catches wrong implementations where a renderer that computes each row height from only non-spanning cells lets the tall spanning cell overflow beyond the two-row table area.
  - PASS 0.01% rowspan=stagger-shift — `tables-rowspan-stagger` — A rowspan=2 first-column cell shifts later rows' cells into the remaining columns.
  - PASS 0.00% rowspan=row-group-boundary-clip — `tables-rowspan-thead-clipped` — A thead cell has rowspan=3 but the thead has one row; it must not occupy the first tbody column. Catches wrong implementations where a global rowspan occupancy implementation makes the first tbody row start in column two, even though positive-rowspan tests inside a group pass.
  - PASS 0.00% rowspan=rowspan-zero — `tables-rowspan-zero` — rowspan=0 spans the remaining rows in the row group; catches implementations that clamp zero to one and shift later rows left.
- **spanning** — 100.00%
  - PASS 0.31% spanning=colspan-rowspan-combined — `tables-colspan-rowspan` — A single cell with both colspan=2 and rowspan=2 occupies a 2x2 block; surrounding cells flow around it.
- **table** — 100.00%
  - PASS 0.01% table=basic-grid — `tables-basic-grid` — 2x3 collapsed-border table of fixed 120x60px cells forming a basic grid.
- **table-fragmentation** — 100.00%
  - PASS 0.00% table-fragmentation=break-before-row — `tables-break-before-row` — The second table row declares break-before:page on a short page and should begin page two. Catches wrong implementations where a paginator that only honors breaks on block elements inside cells keeps both rows on page one.
  - PASS 0.00% table-fragmentation=tbody-break-inside-avoid — `tables-break-inside-avoid-tbody` — A tbody with two rows has break-inside:avoid and fits on a fresh page but not in the remaining space after a header row. Catches wrong implementations where a row-only paginator splits the tbody after its first row even though moving the group would preserve it.
  - PASS 0.57% table-fragmentation=caption-once-across-pages — `tables-multipage-caption` — A captioned table spanning two pages: the caption is part of the table wrapper box and appears ONCE (page 1 only) while the <thead> repeats at each page top. The multi-page harness asserts the page COUNT (2) and diffs both pages; a caption wrongly repeated on page 2 would fail.
  - PASS 0.60% table-fragmentation=tfoot-before-tbody — `tables-multipage-tfoot-before-tbody` — Same multi-page thead+tfoot table but with <tfoot> written BEFORE <tbody> in source. CSS 2.1 §17 mandates section render order thead->tbody->tfoot regardless of DOM order, so Chrome paints the footer at the bottom (its reference is byte-identical to tables-multipage-tfoot-repeat). Pins the section-order reorder; the pre-fix DOM-order behavior would paint the footer band directly under the header.
  - PASS 0.60% table-fragmentation=tfoot-repeat — `tables-multipage-tfoot-repeat` — A long table with both <thead> and <tfoot> spanning pages: Chrome repeats the thead as a running header at the top and the tfoot as a running footer directly after the last body row at the bottom of every spanned page, reserving the footer height so body rows never collide with it. 320x240 page, 40px header+footer leave four 40px body rows per page; seven rows span TWO pages. Fails before the tfoot-repeat fix (footer shown once).
  - PASS 0.58% table-fragmentation=thead-repeat — `tables-multipage-thead-repeat` — A long table whose <thead> repeats at the top of every page it spans (CSS 2.1 §17.2.1; Chrome --print-to-pdf via LayoutNG). 320x240 page: a 40px header plus eight 40px body rows span TWO pages with the header re-emitted at each page top. The multi-page harness asserts the page COUNT (2) and diffs every page, so a missing or mis-placed repeated header fails.
  - PASS 0.63% table-fragmentation=thead-tfoot-three-pages — `tables-multipage-three-page` — A thead+tfoot table spanning THREE pages: with header (40px) and footer (40px) reserved per page, four body rows fit per page, so eleven body rows span three pages (4+4+3) with header repeated at every top and footer at every bottom. The multi-page harness asserts the page COUNT (3) and diffs all three pages.
  - PASS 0.25% table-fragmentation=second-header-group-normal — `tables-second-header-group-not-repeat` — A CSS display table owns two table-header-group boxes; only the first is a header, the second is a normal row group. Catches wrong implementations where a renderer that repeats every header-group or cannot model display header groups passes single thead repetition but repeats the blue subheader incorrectly.
- **table-height** — 100.00%
  - PASS 0.00% table-height=table-min-height — `tables-table-height-minimum` — A table declares height:120px but contains a single short row and has a visible table background. Catches wrong implementations where a renderer that ignores table height paints the table background only around the short row.
- **table-layout** — 100.00%
  - PASS 0.10% table-layout=auto-colspan-distribution — `tables-auto-colspan-distribution` — Auto table layout distributes a colspan width contribution over unequal existing column baselines; catches naive even-split spanning-cell sizing.
  - PASS 0.00% table-layout=fixed-first-row-colspan-width — `tables-fixed-colspan-width-split` — A fixed table has no colgroup; the first row's colspan=2 cell has width:120px and the third cell has width:60px. Catches wrong implementations where a renderer that ignores first-row colspan widths can pass colgroup-based fixed layout but produce equal or content-based columns here.
  - PASS 2.64% table-layout=fixed-overflow-clip — `tables-fixed-overflow-clip` — table-layout:fixed clips nowrap overflow:hidden text inside its fixed-width cell; catches width-only fixed layout that lets text spill into the next cell.
  - PASS 0.01% table-layout=auto — `tables-layout-auto` — table-layout:auto sizes columns to the widest cell intrinsic width (50/100/160px cells).
  - PASS 3.47% table-layout=auto-content-exceeds-width — `tables-layout-auto-overflow` — table-layout:auto grows past its declared width when nowrap content needs more (aspirational: ironpress currently clamps to the declared width).
  - PASS 0.01% table-layout=fixed — `tables-layout-fixed` — table-layout:fixed honors explicit colgroup col widths (60/120/180px) for column sizing.
  - PASS 0.50% table-layout=fixed-remainder-distribution — `tables-layout-fixed-remainder` — table-layout:fixed distributes surplus width proportionally (40/80/120 over 360px -> 60/120/180).
- **table-sections** — 100.00%
  - PASS 0.01% table-sections=thead-tbody-tfoot — `tables-thead-tbody-tfoot` — thead, tbody and tfoot row groups render in document order with distinct section backgrounds.
- **text-align** — 100.00%
  - PASS 0.66% text-align=left-center-right — `tables-cell-text-align` — Short monospace text aligned left, center and right within three equal cells.
- **th-cell** — 100.00%
  - PASS 0.05% th-cell=ua-center-bold — `tables-th-default` — th cells get the UA-stylesheet defaults text-align:center + font-weight:bold; td cells stay left/normal.
  - PASS 0.23% th-cell=header-default — `tables-th-header` — th header cells in the first row vs td data cells, with distinct fills and text.
- **vertical-align** — 100.00%
  - PASS 0.09% vertical-align=top-middle-bottom — `tables-cell-vertical-align` — A 24px marker box aligns to top, middle and bottom within tall 120px cells.
  - PASS 0.42% vertical-align=baseline-uniform — `tables-vertical-align-baseline` — vertical-align:baseline aligns same-size cell text on a shared row baseline.
  - PASS 0.17% vertical-align=baseline-mixed-sizes — `tables-vertical-align-baseline-sizes` — vertical-align:baseline aligns cells with differing font sizes on one common baseline (taller cells shift shorter text down).
  - PASS 0.78% vertical-align=text-bottom-fallback — `tables-vertical-align-text-bottom` — vertical-align:text-bottom does not apply to table cells and should use baseline behavior; catches engines that treat it as bottom alignment.
- **visibility-collapse** — 100.00%
  - PASS 0.00% visibility-collapse=cell — `tables-visibility-collapse-cell` — One cell in a two-cell row is visibility:collapse; its grid slot remains but its red paint and text disappear. Catches wrong implementations where a renderer that ignores cell visibility paints the red cell, while a renderer that removes the slot shifts the green cell left.
  - PASS 0.00% visibility-collapse=column-group — `tables-visibility-collapse-colgroup` — A two-column colgroup is collapsed, leaving only the trailing green column visible. Catches wrong implementations where a renderer that ignores colgroup collapse renders the blue and red grouped columns as normal.
  - PASS 0.00% visibility-collapse=column — `tables-visibility-collapse-column` — The middle col is visibility:collapse in a three-column fixed table. Catches wrong implementations where a renderer without column collapse leaves the red middle column visible and keeps the full three-column width.
  - PASS 0.00% visibility-collapse=row — `tables-visibility-collapse-row` — The middle row is visibility:collapse in a separated table with vertical border-spacing. Catches wrong implementations where a renderer that hides content but leaves the row height or both adjacent spacings would still pass ordinary visibility:hidden tests.
  - PASS 0.00% visibility-collapse=row-group — `tables-visibility-collapse-row-group` — The tbody is visibility:collapse and should remove all body rows while leaving the footer row directly below the header. Catches wrong implementations where a row-only collapse implementation ignores section visibility and still renders the red tbody rows.

### text-advanced — 74.47%
- **dir=auto** — 0.00%
  - FAIL 3.71% dir=auto=dir-auto — `text-advanced-dir-auto` — Two boxes use dir=auto; the Hebrew-first row aligns right while the Latin-first row aligns left. Catches: Supporting only explicit ltr/rtl dir values passes fixed-direction fixtures but treats auto as the inherited/default direction.
- **direction** — 100.00%
  - PASS 0.83% direction=ltr — `text-advanced-direction-ltr` — direction:ltr is the explicit left-to-right baseline counterpart to direction:rtl; inline content flows left-to-right and left-aligns (css-writing-modes-4 §2.1).
  - PASS 0.83% direction=rtl — `text-advanced-direction-rtl` — direction:rtl right-aligns the inline content and reverses run order; aspirational, no RTL/bidi support in ironpress.
- **direction:rtl basic inline paragraph** — 0.00%
  - FAIL 5.32% direction:rtl basic inline paragraph=direction-rtl-mixed-bidi — `text-advanced-direction-rtl-mixed-bidi` — An RTL block containing Hebrew followed by Latin starts from the right edge and orders the mixed text by the Unicode bidi algorithm. Catches: A left-to-right-only renderer passes Latin fixtures and even text-align:right tests but paints mixed RTL text in logical order.
- **hyphens** — 50.00%
  - PASS 2.63% hyphens=auto — `text-advanced-hyphens-auto` — hyphens:auto inserts soft hyphens at language-aware break points; aspirational, no CSS hyphens support in ironpress.
  - FAIL 4.16% hyphens=manual-soft-hyphen — `text-advanced-hyphens-manual-soft-hyphen` — hyphens:manual honors a soft hyphen break and paints a hyphen at the line end; catches wrappers that only break at spaces or overflow points.
- **hyphens:none with U+00AD** — 100.00%
  - PASS 5.37% hyphens:none with U+00AD=hyphens-none-soft-hyphen — `text-advanced-hyphens-none-soft-hyphen` — A narrow word contains a soft hyphen but hyphens:none forbids breaking at it. Catches: Supporting only manual soft-hyphen breaks passes manual hyphenation tests while violating the none value.
- **line-break:anywhere** — 100.00%
  - PASS 4.38% line-break:anywhere=line-break-anywhere — `text-advanced-line-break-anywhere` — A no-space punctuation-heavy run fits a very narrow column only when line-break:anywhere creates breaks around punctuation. Catches: Ignoring line-break passes overflow-wrap and word-break tests but leaves this string overflowing as a single line.
- **line-break:strict** — 100.00%
  - PASS 5.59% line-break:strict=line-break-strict-cjk-punctuation — `text-advanced-line-break-strict-cjk-punctuation` — A narrow Japanese phrase uses strict line breaking so closing punctuation stays with the preceding ideograph rather than starting a line. Catches: Treating all line-break values as auto passes Latin wrapping fixtures but misplaces CJK punctuation at line starts/ends.
- **mixed-script Unicode bidi visual order** — 0.00%
  - FAIL 5.89% mixed-script Unicode bidi visual order=mixed-script-bidi — `text-advanced-mixed-script-bidi` — A left-to-right paragraph contains a Hebrew segment between Latin words and should reorder the RTL segment visually without reversing Latin text. Catches: A run-order-only renderer passes pure LTR and pure RTL tests but leaves mixed scripts in source order.
- **overflow-wrap** — 100.00%
  - PASS 3.59% overflow-wrap=anywhere — `text-advanced-overflow-wrap-anywhere` — overflow-wrap:anywhere breaks an over-long word only as a last resort, keeping the preceding whole word alone on its line before breaking the long word at the box edge (css-text-3 §5.2).
  - PASS 3.59% overflow-wrap=break-word — `text-advanced-overflow-wrap-break-word` — overflow-wrap:break-word breaks an over-long word so it does not overflow the narrow fixed-width box.
- **overflow-wrap:anywhere min-content contribution** — 0.00%
  - FAIL 13.23% overflow-wrap:anywhere min-content contribution=overflow-wrap-anywhere-min-content — `text-advanced-overflow-wrap-anywhere-min-content` — Two inline-blocks with the same long word compare anywhere against break-word; anywhere can shrink much narrower. Catches: Mapping anywhere and break-word to the same visual breaker passes simple wrapping tests but misses intrinsic sizing differences.
- **soft wrap after U+002D hyphen-minus** — 100.00%
  - PASS 6.61% soft wrap after U+002D hyphen-minus=hyphen-minus-wrap — `text-advanced-hyphen-minus-wrap` — A narrow box wraps pseudo-element after the hyphen and keeps the hyphen at the end of the first line. Catches: A wrapper that only breaks at spaces passes existing normal wrapping tests but overflows or arbitrarily splits hyphenated words.
- **tab-size** — 100.00%
  - PASS 0.81% tab-size=eight — `text-advanced-tab-size` — tab-size:8 with white-space:pre aligns tab stops at eight character widths; aspirational, no tab-size support in ironpress.
  - PASS 0.38% tab-size=number-four-pre — `text-advanced-tab-size-four` — tab-size:4 with white-space:pre aligns the tabbed B with the four-column text row; catches default-width or collapsed tab handling.
- **text-align** — 100.00%
  - PASS 0.77% text-align=end-rtl — `text-advanced-text-align-end-rtl` — text-align:end in an RTL block aligns the short run to the physical left edge; catches treating end as left/right independent of direction.
- **text-align-last:center** — 0.00%
  - FAIL 4.60% text-align-last:center=text-align-last-center — `text-advanced-text-align-last-center` — A justified paragraph has a short final line that should be centered by text-align-last:center. Catches: A justify-only renderer passes multiline justify tests while leaving the last line left-aligned.
- **text-align:match-parent** — 100.00%
  - PASS 1.14% text-align:match-parent=text-align-match-parent — `text-advanced-text-align-match-parent` — A child inherits a parent's logical end alignment and resolves it against the parent's RTL direction. Catches: Treating match-parent as left passes current physical alignment tests but loses inherited logical resolution.
- **text-align:start with direction:rtl** — 100.00%
  - PASS 1.31% text-align:start with direction:rtl=text-align-start-rtl — `text-advanced-text-align-start-rtl` — An RTL block with text-align:start places a short Latin run against the physical right edge. Catches: A parser that treats unknown text-align as left passes left/right/center fixtures but flips logical alignment.
- **text-combine-upright:digits 2** — 0.00%
  - FAIL 5.40% text-combine-upright:digits 2=text-combine-upright-digits — `text-advanced-text-combine-upright-digits` — A vertical date combines two-digit numbers horizontally inside a single vertical character slot. Catches: A vertical-writing implementation can pass plain vertical text while rendering each digit as a separate stacked glyph.
- **text-indent** — 100.00%
  - PASS 2.57% text-indent=length — `text-advanced-text-indent` — text-indent:40px indents only the first line of a wrapped paragraph while later lines start at the content edge.
- **text-orientation:upright** — 0.00%
  - FAIL 4.21% text-orientation:upright=text-orientation-upright — `text-advanced-text-orientation-upright` — A vertical-rl box with text-orientation:upright stacks upright Latin letters instead of rotating the word sideways. Catches: A hard-coded vertical-rl mixed orientation passes default vertical text tests but cannot honor upright orientation.
- **text-overflow** — 100.00%
  - PASS 0.82% text-overflow=clip — `text-advanced-text-overflow-clip` — text-overflow:clip on an overflow:hidden nowrap fixed-width box hard-clips overflowing text with no ellipsis.
  - PASS 1.02% text-overflow=ellipsis — `text-advanced-text-overflow-ellipsis` — text-overflow:ellipsis on an overflow:hidden nowrap fixed-width box renders a trailing ellipsis where text is clipped.
  - PASS 0.82% text-overflow=string — `text-advanced-text-overflow-string` — text-overflow:">" truncates overflowing text with a custom string instead of an ellipsis; aspirational, ironpress does not yet parse the <string> form (css-ui-4 §6.2).
- **text-overflow:ellipsis with direction:rtl** — 100.00%
  - PASS 4.95% text-overflow:ellipsis with direction:rtl=text-overflow-rtl-ellipsis — `text-advanced-text-overflow-rtl-ellipsis` — An RTL nowrap overflow box should place the ellipsis at the left inline end when content overflows toward that side. Catches: A left-to-right truncator appends ellipsis to the string and passes LTR ellipsis fixtures while painting it on the wrong physical edge for RTL.
- **text-shadow offset color** — 100.00%
  - PASS 5.02% text-shadow offset color=text-shadow-offset — `text-advanced-text-shadow-offset` — A word has a non-blurred red shadow offset down and right behind black foreground glyphs. Catches: A renderer that ignores text-shadow passes ordinary text color/layout fixtures but lacks the second offset glyph silhouette.
- **text-wrap-mode:nowrap** — 0.00%
  - FAIL 35.16% text-wrap-mode:nowrap=text-wrap-mode-nowrap — `text-advanced-text-wrap-mode-nowrap` — A long phrase with text-wrap-mode:nowrap overflows a narrow box even though white-space is otherwise normal. Catches: Only recognizing white-space:nowrap passes shorthand tests while ignoring the longhand added by CSS Text 4.
- **text-wrap:balance** — 100.00%
  - PASS 3.24% text-wrap:balance=text-wrap-balance — `text-advanced-text-wrap-balance` — A heading constrained to two lines should balance line lengths instead of leaving a very short final line. Catches: A normal greedy wrapper passes ordinary wrapping fixtures but produces an obviously ragged title.
- **text-wrap:pretty** — 100.00%
  - PASS 0.00% text-wrap:pretty=text-wrap-pretty — `text-advanced-text-wrap-pretty` — A paragraph width that would strand one short word uses pretty wrapping to improve the final line. Catches: A greedy-only wrapper passes normal wrapping but leaves a single-word last line when pretty should rebalance.
- **unicode-bidi** — 100.00%
  - PASS 0.83% unicode-bidi=bidi-override — `text-advanced-unicode-bidi-override` — unicode-bidi:bidi-override with direction:rtl forces visual right-to-left ordering of glyphs; aspirational, no bidi support in ironpress.
- **unicode-bidi:plaintext** — 100.00%
  - PASS 2.79% unicode-bidi:plaintext=unicode-bidi-plaintext — `text-advanced-unicode-bidi-plaintext` — A pre-line block has one English line and one Hebrew line; plaintext chooses base direction separately for each line. Catches: A paragraph-level bidi implementation passes single-paragraph tests but gives both lines the same base direction.
- **white-space** — 100.00%
  - PASS 2.33% white-space=break-spaces — `text-advanced-white-space-break-spaces` — white-space:break-spaces preserves spaces and forced segment breaks while still soft-wrapping; preserved trailing spaces occupy width at the line end and cannot hang (css-text-3 §3).
  - PASS 2.95% white-space=normal — `text-advanced-white-space-normal` — white-space:normal collapses runs of spaces and newlines into single soft-wrappable spaces.
  - PASS 0.81% white-space=nowrap — `text-advanced-white-space-nowrap` — white-space:nowrap forces a single line that is clipped by overflow:hidden in a narrow box.
  - PASS 0.59% white-space=pre — `text-advanced-white-space-pre` — white-space:pre preserves runs of spaces and explicit newlines in monospace text with no wrapping.
  - PASS 1.93% white-space=pre-line — `text-advanced-white-space-pre-line` — white-space:pre-line collapses spaces but preserves explicit newlines, wrapping at the box edge.
  - PASS 2.40% white-space=pre-wrap — `text-advanced-white-space-pre-wrap` — white-space:pre-wrap preserves spaces while still soft-wrapping at the box edge.
- **white-space-collapse:preserve** — 100.00%
  - PASS 0.73% white-space-collapse:preserve=white-space-collapse-preserve — `text-advanced-white-space-collapse-preserve` — A normal wrapping line uses the longhand white-space-collapse:preserve so three spaces between A and B stay visible. Catches: Only recognizing white-space:pre/pre-wrap passes shorthand fixtures while collapsing spaces when the new longhand is used.
- **word-break** — 100.00%
  - PASS 2.57% word-break=break-all — `text-advanced-word-break-break-all` — word-break:break-all breaks the line at any glyph boundary; aspirational, not implemented in ironpress.
  - PASS 2.37% word-break=keep-all — `text-advanced-word-break-keep-all` — word-break:keep-all forbids breaking within words; for spaced Latin text it wraps only at the inter-word spaces (css-text-3 §5.1).
- **word-break:keep-all with CJK text** — 0.00%
  - FAIL 7.68% word-break:keep-all with CJK text=word-break-keep-all-cjk — `text-advanced-word-break-keep-all-cjk` — A narrow CJK phrase with keep-all should avoid ordinary CJK line breaks and overflow/clamp as one unbroken run. Catches: Leaving keep-all as normal passes Latin keep-all smoke tests because spaced Latin text wraps the same either way.
- **writing-mode** — 50.00%
  - PASS 0.00% writing-mode=vertical-rl — `text-advanced-writing-mode-vertical-rl` — writing-mode:vertical-rl lays out glyphs top-to-bottom in a right-to-left column; aspirational, no writing-mode support in ironpress.
  - FAIL 4.33% writing-mode=vertical-rl-columns — `text-advanced-writing-mode-vertical-rl-columns` — writing-mode:vertical-rl lays Latin inline text sideways from the top of the right column; catches horizontal fallback rendering.
- **writing-mode:sideways-rl** — 100.00%
  - PASS 1.13% writing-mode:sideways-rl=writing-mode-sideways-rl — `text-advanced-writing-mode-sideways-rl` — A sideways-rl block rotates Latin text as a sideways vertical line instead of horizontal text. Catches: Supporting only horizontal-tb and vertical-rl passes current fixtures while ignoring sideways modes.
- **writing-mode:vertical-lr** — 0.00%
  - FAIL 5.01% writing-mode:vertical-lr=writing-mode-vertical-lr — `text-advanced-writing-mode-vertical-lr` — A vertical-lr block should set text vertically with columns progressing left-to-right. Catches: A vertical-rl-only implementation passes its one vertical fixture but falls back to horizontal for vertical-lr.

### transforms — 100.00%
- **individual-transform** — 100.00%
  - PASS 0.05% individual-transform=translate-rotate-transform-box-content — `transforms-individual-transform-box-content` — Individual translate and rotate properties compose around transform-box:content-box with transform-origin:0 0; catches shorthand-only and border-box-origin transform implementations.
- **transform** — 100.00%
  - PASS 0.00% transform=compound-order — `transforms-compound-order` — Compound order dependence: translate() rotate() composes differently from rotate() translate(); the two boxes land in distinct places.
  - PASS 0.00% transform=compound-rotate-translate — `transforms-compound-rotate-translate` — Chained transform functions translate(...) rotate(...) compose left-to-right in the box coordinate space. Aspirational: parse_transform returns a single Transform, so chaining is unsupported.
  - PASS 0.00% transform=compound-scale-translate — `transforms-compound-scale-translate` — scale(2) translate(40px, 20px): the translation that follows the scale is itself scaled, and the box is twice as large.
  - PASS 0.06% transform=matrix — `transforms-matrix` — transform: matrix(a,b,c,d,e,f) applies a 2D affine matrix combining scale, shear, and translate in one function. Aspirational: matrix() is not parsed by ironpress.
  - PASS 0.00% transform=none-reset — `transforms-none-reset` — transform: none is an explicit reset: a later rule overriding an earlier transform with none leaves the box untransformed in its in-flow slot.
  - PASS 0.00% transform=rotate — `transforms-rotate` — transform: rotate() turns the box clockwise about its center (default transform-origin) without disturbing layout flow.
  - PASS 0.00% transform=rotate-negative — `transforms-rotate-negative` — transform: rotate(-30deg) turns the box counter-clockwise about its center.
  - PASS 0.00% transform=rotate-rad — `transforms-rotate-rad` — transform: rotate(0.5236rad) == rotate(30deg); the radian angle unit is converted before rotating.
  - PASS 0.18% transform=rotate-turn — `transforms-rotate-turn` — transform: rotate(0.25turn) == rotate(90deg); the turn angle unit is converted before rotating about the box center.
  - PASS 0.21% transform=scale — `transforms-scale` — transform: scale() enlarges the box uniformly about its center (default transform-origin) without changing layout flow.
  - PASS 0.13% transform=scale-negative — `transforms-scale-negative` — transform: scale(-1, 1) mirrors the box horizontally about its center, swapping its left/right halves.
  - PASS 0.30% transform=scale-single-arg — `transforms-scale-omitted` — transform: scale(1.5) with the second argument omitted scales both axes uniformly.
  - PASS 0.00% transform=scale-percent — `transforms-scale-percent` — transform:scale(150%,50%) accepts percentage scale factors and scales from a top-left origin; catches parsers that accept only numeric scale factors and drop the transform.
  - PASS 0.29% transform=scaleX — `transforms-scale-x` — transform: scaleX() stretches the box horizontally about its center.
  - PASS 0.14% transform=scaleY — `transforms-scale-y` — transform: scaleY() stretches the box vertically about its center.
  - PASS 0.00% transform=skew — `transforms-skew` — transform: skew() shears the box along X and Y about its center, turning the rectangle into a parallelogram. Aspirational: skew is absent from the ironpress Transform enum and parser.
  - PASS 0.00% transform=skewX — `transforms-skew-x` — transform: skewX(20deg) shears the box horizontally about its center into a parallelogram.
  - PASS 0.00% transform=skewY — `transforms-skew-y` — transform: skewY(20deg) shears the box vertically about its center into a parallelogram.
  - PASS 0.00% transform=translate — `transforms-translate` — transform: translate(x, y) shifts a positioned box right and down from its in-flow slot without affecting surrounding layout.
  - PASS 0.00% transform=translate-em — `transforms-translate-em` — transform: translate(3em, 2em) resolves em length args against the element's own font-size.
  - PASS 0.00% transform=translate-percent — `transforms-translate-percent` — transform: translate(50%, 50%) resolves percentages against the element's OWN border box (width for X, height for Y), shifting the box by half its own size.
  - PASS 0.00% transform=translateX — `transforms-translate-x` — transform: translateX() shifts the box horizontally only, leaving its vertical position unchanged.
  - PASS 0.00% transform=translateX-percent — `transforms-translate-x-percent` — transform: translateX(100%) shifts the box right by its own width (percentage relative to border-box width).
  - PASS 0.00% transform=translateY — `transforms-translate-y` — transform: translateY() shifts the box vertically only, leaving its horizontal position unchanged.
  - PASS 0.00% transform=translateY-percent — `transforms-translate-y-percent` — transform: translateY(100%) shifts the box down by its own height (percentage relative to border-box height).
- **transform-3d** — 100.00%
  - PASS 0.00% transform-3d=matrix3d-translate — `transforms-matrix3d-translate` — matrix3d() translation components move the green box down and right from the red reference; catches 2D-only parsers that drop matrix3d.
  - PASS 0.00% transform-3d=transform-origin-z-offset — `transforms-origin-z-offset` — A three-value transform-origin with a Z offset changes the rotateY hinge; catches ignoring the third transform-origin component.
  - PASS 0.00% transform-3d=perspective-origin — `transforms-perspective-origin` — perspective-origin changes the projection center for otherwise identical rotateY boxes; catches treating both scenes as the same projection.
  - PASS 0.00% transform-3d=perspective-rotate-x — `transforms-perspective-rotate-x` — perspective() rotateX() projects the box as a 3D quadrilateral; catches 2D flattening, perspective drops, or treating rotateX as a plain scale.
  - PASS 0.00% transform-3d=perspective-translate-z — `transforms-perspective-translate-z` — A parent perspective makes a translateZ child render larger than the red reference; catches ignoring either the perspective property or the Z translation.
  - PASS 0.00% transform-3d=preserve-3d-depth-order — `transforms-preserve-3d-depth-order` — transform-style:preserve-3d lets an earlier translateZ child paint above a later flat sibling; catches flattening or source-order-only painting.
  - PASS 0.00% transform-3d=rotate3d-axis — `transforms-rotate3d-axis` — rotate3d(1,1,0,62deg) rotates around an arbitrary 3D axis under perspective; catches dropping rotate3d or rendering the element as an unprojected rectangle.
  - PASS 0.00% transform-3d=scale3d-rotate-y — `transforms-scale3d-rotate-y` — scale3d's Z component changes the projected shape of a rotateY plane; catches dropping scale3d or using a flat 2D projection.
  - PASS 0.00% transform-3d=translate3d-perspective — `transforms-translate3d-perspective` — translate3d(x,y,z) combines planar movement with perspective enlargement; catches dropping translate3d or ignoring its Z component.
- **transform-origin** — 100.00%
  - PASS 0.00% transform-origin=bottom-right — `transforms-origin-bottom-right` — transform-origin: bottom right pivots a rotation about the box's bottom-right corner.
  - PASS 0.00% transform-origin=keyword-length — `transforms-origin-keyword-length` — Two-value transform-origin mixing a keyword and a length (left 20px) pivots a rotation about that anchor on the left edge.
  - PASS 0.26% transform-origin=length — `transforms-origin-length` — transform-origin with px length values pivots a scale about a point measured from the box top-left.
  - PASS 0.11% transform-origin=percent — `transforms-origin-percent` — transform-origin: 100% 0 pivots a scale about the box's top-right corner.
  - PASS 0.21% transform-origin=scale-pivot — `transforms-origin-scale-corner` — transform-origin affects the scale pivot: with top-left origin the box grows down/right while its top-left corner stays anchored.
  - PASS 0.00% transform-origin=top-left — `transforms-origin-top-left` — transform-origin: top left moves the rotation pivot from the box center to its top-left corner, changing where a rotate() lands. Aspirational: ironpress has no transform-origin parse and always pivots about the default origin.

### typography — 85.71%
- **font-family** — 100.00%
  - PASS 2.76% font-family=monospace — `typography-font-family-monospace` — Generic monospace family fallback at 28px with fixed-advance glyphs.
  - PASS 3.18% font-family=sans-serif — `typography-font-family-sans-serif` — Generic sans-serif family fallback at 28px.
  - PASS 3.99% font-family=serif — `typography-font-family-serif` — Generic serif family fallback at 28px.
- **font-size** — 100.00%
  - PASS 0.00% font-size=em — `typography-font-size-em` — font-size 2em resolved against the parent's 16px font-size.
  - PASS 1.51% font-size=percent — `typography-font-size-percent` — font-size 150% resolved against the parent's 16px font-size.
  - PASS 0.00% font-size=pt — `typography-font-size-pt` — Text rendered at an explicit 24pt font-size to verify pt-to-px conversion.
  - PASS 0.00% font-size=px — `typography-font-size-px` — Text rendered at an explicit 32px font-size in a bordered, filled region.
  - PASS 0.00% font-size=rem — `typography-font-size-rem` — font-size 2rem resolved against the root 16px font-size, not the local 10px.
- **font-style** — 100.00%
  - PASS 3.94% font-style=italic — `typography-font-style-italic` — Italic serif text to verify slanted glyph selection and metrics.
- **font-weight** — 100.00%
  - PASS 3.99% font-weight=bold — `typography-font-weight-bold` — Bold-weight text so heavier glyph strokes show up as pixel differences.
  - PASS 3.55% font-weight=normal — `typography-font-weight-normal` — Normal-weight text as the regular-stroke counterpart to the bold fixture.
- **heading-default-size** — 100.00%
  - PASS 1.36% heading-default-size=h1-h3 — `typography-heading-default-sizes` — Default user-agent font-sizes for h1, h2 and h3 stacked in one region.
- **initial-letter:2** — 0.00%
  - FAIL 16.18% initial-letter:2=initial-letter-two-line — `typography-initial-letter-two-line` — A paragraph uses p::first-letter{initial-letter:2} to create a dropped initial spanning two lines without manual float sizing. Catches: A renderer with only custom ::first-letter font-size/float drop caps passes handcrafted drop-cap fixtures but ignores the initial-letter property.
- **line-height** — 100.00%
  - PASS 0.87% line-height=length — `typography-line-height-length` — Length line-height 36px across three stacked lines to verify fixed line-box height.
  - PASS 0.97% line-height=numeric — `typography-line-height-numeric` — Unitless line-height 2 across three stacked lines to verify line-box spacing.
  - PASS 0.00% line-height=percentage-font-size — `typography-line-height-percent` — line-height:200% resolves from the element font-size to 40px line boxes; catches percentage line-height being ignored or inherited as 1.5.
- **line-height:<length em> inheritance** — 100.00%
  - PASS 0.36% line-height:<length em> inheritance=line-height-em-inheritance — `typography-line-height-em-inheritance` — A parent sets font-size:20px and line-height:2em; a smaller child inherits a 40px computed line-height, not a unitless factor of 2. Catches: Treating line-height:2em as the number 2 passes simple same-element line-height tests but compresses descendants with smaller fonts.
- **mixed inline font-size line box metrics** — 0.00%
  - FAIL 20.26% mixed inline font-size line box metrics=mixed-inline-font-size-linebox — `typography-mixed-inline-font-size-linebox` — A paragraph with a 42px span in the middle must produce a taller first line than the following normal line. Catches: A block-level line-height-only engine passes uniform line-height tests while clipping or overlapping large inline runs.
- **text-emphasis filled dot** — 0.00%
  - FAIL 39.69% text-emphasis filled dot=text-emphasis-filled-dot — `typography-text-emphasis-filled-dot` — Japanese text uses filled red emphasis dots above the characters. Catches: An implementation with only text-decoration lines passes underline/overline tests but renders no emphasis marks.
- **vertical-align** — 100.00%
  - PASS 0.38% vertical-align=sub — `typography-vertical-align-sub` — Subscript element (H2O) lowered below the baseline with reduced font-size.
  - PASS 1.09% vertical-align=sup — `typography-vertical-align-sup` — Superscript element (x squared) raised above the baseline with reduced font-size.

### units-values — 100.00%
- **aspect-ratio** — 100.00%
  - PASS 0.00% aspect-ratio=auto-width-from-height — `units-aspect-ratio-height-derived-width` — A box with definite height and width:auto derives its width from aspect-ratio; catches only implementing width-to-height derivation.
- **calc** — 100.00%
  - PASS 0.00% calc=division — `units-calc-division` — calc(50% / 1) x calc(100% / 2) against a 480x240px parent resolves to 240x120px; verifies division of a dimension by a unitless number (css-values-4 §10.9).
  - PASS 0.56% calc=mixed-percent-px — `units-calc-mixed-percent` — calc(50% - 40px) and calc(100% - 60px) against a 400x160px parent resolve to a 160x100px child; verifies mixed percent/px subtraction.
  - PASS 0.00% calc=nested-parens — `units-calc-nested-parens` — calc(2 * (10% + 20px)) x calc(2 * (10% + 10px)) against a 400x200px parent resolves to 120x60px; verifies parenthesised sub-expressions in calc() (css-values-4 §10.9). The percentage defeats lightningcss folding so the grouping is honoured end-to-end.
  - PASS 0.00% calc=precedence-mul — `units-calc-precedence` — calc(50% + 10px * 2) x calc(50% + 5px * 4) against a 400x200px parent resolves to 220x120px; verifies * binds tighter than + (css-values-4 §10.9) through ironpress's own evaluator (percentage defeats lightningcss folding).
- **custom-properties** — 100.00%
  - PASS 0.00% custom-properties=var-resolution — `units-var-custom-property` — Box dimensions and colors driven entirely by :root custom properties via var() (240x120px blue box); verifies var() resolution for lengths and colors.
  - PASS 0.00% custom-properties=var-fallback — `units-var-fallback` — var() with undefined custom properties falls back to literal length/color fallbacks (240x120px green box); verifies the var() fallback argument path.
  - PASS 0.00% custom-properties=var-nested-fallback — `units-var-nested-fallback` — var(--w, var(--w2, 240px)) and nested var() fallbacks for height/colors with all custom properties undefined resolve to a 240x120px blue box; verifies nested var() fallback (css-values-4 var() fallback).
- **font-relative-length** — 100.00%
  - PASS 0.00% font-relative-length=cap — `units-cap-height` — The cap unit resolves from font cap height and is visibly shorter than a 10em reference; catches treating cap as em or unsupported.
  - PASS 0.00% font-relative-length=ch-width — `units-ch-layout-width` — width:20ch in ParityMono uses the zero glyph advance and is wider than a 10em reference; catches hard-coded 0.5em ch fallback.
  - PASS 0.00% font-relative-length=ex-x-height — `units-length-ex-x-height` — The ex unit uses the font x-height in layout lengths; catches treating ex as em, unsupported, or a hard-coded unrelated ratio.
  - PASS 0.00% font-relative-length=lh — `units-lh-line-height` — The lh unit resolves against the element's computed line-height, so width:4lh with line-height:30px is 120px; catches unsupported lh units.
- **intrinsic-sizing** — 100.00%
  - PASS 0.00% intrinsic-sizing=min-max-content-vmin-aspect-ratio — `units-intrinsic-vmin-aspect-ratio` — min-content and max-content text widths plus a 40vmin aspect-ratio box lock intrinsic sizing and viewport-unit resolution; catches auto intrinsic widths, default viewport units, or dropped aspect-ratio.
- **length-units** — 100.00%
  - PASS 0.00% length-units=em-on-font-size — `units-em-font-size` — Parent font-size:20px; .box font-size:2em resolves against the PARENT (=40px), then 6em x 3em = 240x120px and a 0.1em border; verifies the em-on-font-* rule (css-values-4 §6.1.1).
  - PASS 0.27% length-units=cm — `units-length-cm` — Box sized in centimetres (5cm x 3cm ~= 189x113px) verifying absolute metric cm unit resolution.
  - PASS 0.00% length-units=em — `units-length-em` — Box with font-size:20px sized 12em x 6em (=240x120px) and 0.2em border; verifies em resolves against element font-size.
  - PASS 0.00% length-units=in — `units-length-in` — Box sized in inches (2.5in x 1.25in = 240x120px) verifying absolute inch unit resolution (1in=96px).
  - PASS 0.00% length-units=mm — `units-length-mm` — Box sized in millimetres (63.5mm x 31.75mm = 240x120px) verifying the absolute metric mm unit (css-values-4 §6.2, 1mm=cm/10).
  - PASS 0.00% length-units=pc — `units-length-pc` — Box sized in picas (15pc x 7.5pc = 240x120px) with a 0.25pc border; verifies the absolute pica unit (css-values-4 §6.2, 1pc=16px).
  - PASS 0.00% length-units=pt — `units-length-pt` — Box sized in pt (180pt x 90pt = 240x120px) with a 3pt solid border; verifies pt->px length resolution.
  - PASS 0.00% length-units=Q — `units-length-q` — Box sized in quarter-millimetres (254Q x 127Q = 240x120px) verifying the absolute Q unit (css-values-4 §6.2, 1Q=cm/40).
  - PASS 0.00% length-units=rem — `units-length-rem` — Box sized 15rem x 7.5rem against root font-size:16px (=240x120px) inside a 40px-font ancestor; rem ignores local font-size.
- **math-functions** — 100.00%
  - PASS 0.00% math-functions=clamp — `units-clamp` — clamp(120px,50%,240px) x clamp(80px,50%,200px) against a 600x160px parent resolves to a 240x80px child; verifies clamp() (css-values-4 §10.2).
  - PASS 0.00% math-functions=clamp-min-wins — `units-clamp-min` — clamp(200px, 10%, 240px) x clamp(100px, 10%, 200px) against a 400x200px parent resolves to 200x100px (MIN bound wins); verifies clamp() lower-bound clamping (css-values-4 §10.2).
  - PASS 0.00% math-functions=min-max — `units-min-max` — width:max(150px,240px) and height:min(120px,300px) resolve to 240x120px; verifies standalone min()/max() value functions (css-values-4 §10.2).
  - PASS 0.00% math-functions=mod — `units-mod-function` — mod(50px,30px) contributes 20px inside calc(), making a 120px bar; catches dropping CSS Values math mod().
  - PASS 0.00% math-functions=round — `units-round-function` — round(down,137px,20px) snaps a length to the 120px step; catches parsers that drop CSS Values math round().
  - PASS 0.00% math-functions=sin — `units-sin-function` — sin(30deg) resolves to 0.5 in calc(), making a 120px bar; catches dropping CSS Values trigonometric math functions.
- **number** — 100.00%
  - PASS 0.00% number=scientific-notation — `units-number-scientific` — Box sized 2.4e2px x 1.2e2px = 240x120px; verifies scientific-notation numbers in lengths (css-values-4 §5.2).
- **percentage** — 100.00%
  - PASS 0.00% percentage=margin-cb-width — `units-percent-margin` — margin-left:25% and margin-top:25% (both 25% of the 400px parent width = 100px) offset a 200x60px child; verifies margin percentages resolve against containing-block WIDTH (css-values-4 §5.5).
  - PASS 0.00% percentage=padding-cb-width — `units-percent-padding` — padding:25% on a 200px-wide content-box child of a 400px parent paints a 400x200px box; verifies padding percentages resolve against containing-block WIDTH on both axes (css-values-4 §5.5).
  - PASS 0.00% percentage=width-height-resolution — `units-percent-width-height` — Child width:75% / height:50% of a 400x200px parent resolves to 300x100px; verifies percentage length resolution against the containing block.
- **viewport-units** — 100.00%
  - PASS 0.00% viewport-units=svw-svh — `units-viewport-svw-svh` — Small viewport units resolve in static print layout, making a 50svw by 25svh box on a fixed page; catches parsers limited to vw/vh.
  - PASS 0.00% viewport-units=vmin-vmax — `units-viewport-vmin-vmax` — Aspirational: 40vmin/10vmin and 30vmax/8vmax boxes against the page viewport; ironpress resolves only vw/vh, so vmin/vmax are unsupported (css-values-4 §6.1.2.2).
  - PASS 0.34% viewport-units=vw-vh — `units-viewport-vw-vh` — Aspirational: box sized 30vw x 20vh resolves against the printable page (viewport) box; ironpress vw/vh resolution is only partial.

